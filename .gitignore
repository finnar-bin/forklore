# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
*.local

# Environment configs (real secrets) — keep the .example templates tracked
.env.dev
.env.prod

# Playwright (e2e/) — .env.test itself IS committed (see e2e/README.md): it
# holds no real secret, only a fake Supabase URL every request to which is
# intercepted by e2e/mocks/mockBackend.ts.
/test-results/
/playwright-report/
/playwright/.cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
