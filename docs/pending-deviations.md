# Pending spec deviations

Deviations from `schema.md` / `rpcs.md` / `frontend-architecture.md` / `design-system.md` / `routes.md` accumulate here during Phase 1. Review and fold into the core docs after Phase 1 wraps.

---

## Ticket 2 — Supabase project setup and schema migration

**Deviation:** `schema.md`'s RLS section gives explicit `select`/`insert`/`update` policies only for `ingredients` (with an instruction to copy the same three-policy shape to `recipes` and `log_entries`), plus `select`/`update`/`delete` for `groups`. It does not give any policies at all for `profiles`, `weight_logs`, or `group_invites`, doesn't give `group_members` policies beyond "enable RLS", and doesn't give `delete` policies for `ingredients`/`recipes`/`recipe_ingredients`. The migration (`supabase/migrations/20260819000000_phase1_schema.sql`) fills these gaps with:

- `profiles`: select own row or a fellow group member's row; update own row only.
- `weight_logs`: select/insert/update own rows only (personal, no group scope — matches Progress being individual-only per ticket 18).
- `groups`: added an `insert` policy (`owner_id = auth.uid()`) since none was given and group creation needs one.
- `group_members`: select rows in any group the caller belongs to; delete restricted to the group's owner. No client-side insert policy — membership rows are created via the group-creation flow and the `accept_group_invite` RPC (`security definer`, bypasses RLS).
- `group_invites`: select/insert restricted to the inviting group's owner. No update — `accepted_by`/`accepted_at` are set only via the `accept_group_invite` RPC.
- `ingredients`, `recipes`, `recipe_ingredients`: added `delete` policies mirroring the existing ownership pattern (needed for ingredient delete in ticket 6, and removing an ingredient from a recipe in ticket 7).

**Why:** The acceptance criteria for this ticket require RLS enabled on every table listed in `schema.md`, and the app can't function (profile reads, group creation, ingredient delete, etc.) with RLS enabled but zero policies on these tables. All new policies extend the existing "personal row owned by caller, or caller is a member of the owning group" pattern already established in the doc rather than introducing a new access model.

**Not yet verified:** This migration has not been applied to a live Supabase project or manually tested against two accounts (agent tooling restrictions in this session prohibit connecting to any database). Applying it, running it, and doing the manual cross-user/cross-group RLS check are left as a manual follow-up — see `supabase/README.md`.

**Deviation (env/deploy tooling):** `schema.md`/`phase-1-tickets.md` don't specify how Supabase credentials should be configured or how migrations get pushed to a given environment. Added `.env.dev` / `.env.prod` (gitignored, with `.env.dev.example` / `.env.prod.example` templates committed) holding `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `SUPABASE_PROJECT_REF`, and `SUPABASE_ACCESS_TOKEN` per environment; Vite picks the right file via `--mode dev`/`--mode prod` (wired into `npm run dev` / `build` / `build:dev`); and two new npm commands, `supabase:push:dev` and `supabase:push:prod`, run `scripts/supabase-push.sh` to `supabase link` + `supabase db push` against the matching project. **Why:** requested directly (not part of a numbered ticket) to give dev/prod Supabase projects a repeatable, one-command way to receive schema pushes. Not yet run against a live project for the same database-connection reason as above.

---

## Ticket 3 — Auth flow and profile auto-fill

**Deviation:** `routes.md` doesn't define a root (`/`) path — its list starts at `/login`. Added `/` as a temporary authenticated landing page (`src/routes/HomePage.tsx`) so `RequireAuth` has a real protected route to gate, and so logged-in users land somewhere after signup/login instead of a blank screen. It shows a placeholder card plus a logout button; it isn't one of the Phase 1 feature screens (Pantry/Recipes/Log/Progress) and is expected to be replaced once Ticket 6 (Pantry) or a real default-landing decision exists.

**Deviation:** `frontend-architecture.md` specifies `useAppStore` (`userId`, `activeGroupId`) as part of Ticket 4's scope, not Ticket 3's. Auth-gated routing needs _some_ shared place to hold "is there a logged-in user," so this ticket adds `src/store/useAppStore.ts` now, implementing the interface exactly as documented in `frontend-architecture.md`. `activeGroupId`/`setActiveGroup` are included (unused by this ticket) rather than a partial shape, so Ticket 4 doesn't need to redefine the store — it should only need to add `useSyncStore` and wire Dexie.

**Why:** Both changes exist to satisfy this ticket's "auth-gated routing" acceptance criterion with no upstream ticket yet providing a landing route or a session store; the docs don't specify a normative first draft for either.

**Not yet verified:** `handle_new_user` (name/avatar auto-fill for Google SSO, fallback name for email/password) is implemented in the Ticket 2 migration and the frontend code paths that exercise it (Google OAuth button, email/password signup form, auth-gated redirect) were exercised locally against the dev Supabase project far enough to confirm the Auth API call reaches Supabase and errors round-trip correctly into the UI (a deliberately-invalid signup returned a real Supabase 400 and rendered correctly). Completing an actual signup and inspecting the resulting `profiles` row was not done in this session — agent tooling restrictions prohibit connecting to any database, and a completed signup writes real rows to `auth.users`/`profiles` via the trigger. Manual follow-up needed (see `supabase/README.md`): sign up via Google, confirm `profiles.name`/`avatar_url` are populated from the Google account; sign up via email/password, confirm `profiles.name` falls back to the email's local part; confirm a logged-out request to `/` redirects to `/login` and a logged-in visit to `/login`/`/signup` redirects to `/`.

---

## Ticket 5 — Onboarding flow

**Deviation:** `rpcs.md` has no entry for onboarding submission. Onboarding writes span two tables — `profiles` (name/height/goal fields) and `weight_logs` (the first weight entry) — which must succeed or fail together, matching this doc's own decision rule ("multi-table writes that must succeed or fail together -> Postgres RPC function"). Added `complete_onboarding(p_name, p_height_cm, p_weight_kg, p_goal_weight_kg, p_goal_type)` as a new `security definer` RPC (`supabase/migrations/20260820000000_complete_onboarding_rpc.sql`), following the same shape as the existing `accept_group_invite`/`copy_ingredient` functions.

**Deviation:** The onboarding submit calls this RPC directly against Supabase rather than going through the Dexie/outbox write path described in `frontend-architecture.md`. This mirrors how the auth feature (Ticket 3) already writes straight to Supabase (`signUpWithEmail`, `signInWithGoogle`, etc.) — onboarding happens immediately post-signup, before there's any reason to assume the device is offline, and `weight_logs` isn't part of the Dexie mirror in `frontend-architecture.md` in the first place (it's only needed for Progress, a separate ticket). The local `profiles` Dexie table is also not updated here, since the pull/sync engine (`src/sync/`) doesn't exist yet as of this ticket.

**Deviation:** `frontend-architecture.md` documents `useAppStore`'s `AppState` as `{ userId, activeGroupId }` plus their setters. Added `onboardingComplete: boolean | null` and `setOnboardingComplete` so `App.tsx` has a shared place to gate `/` vs `/onboarding` without every route re-querying Supabase. `null` means "not yet checked"; `setSession` resets it to `null` only when the userId actually changes (not on same-user auth events like token refresh), so `useOnboardingGate` (`src/features/onboarding/useOnboardingGate.ts`) re-derives it once per login/logout/account-switch, not on every auth state change.

**Deviation:** `routes.md` doesn't define a root (`/`) path (see Ticket 3's note above) — this ticket's `RequireOnboarded` guard sits on that same ad hoc `/` route, not a documented Phase 1 screen.

**Why:** All four exist to satisfy this ticket's acceptance criteria (route to onboarding before any other screen, atomic profile+weight_logs write, returning users skip onboarding) with no upstream ticket providing the RPC, store shape, or a real post-onboarding landing screen yet.

**Not yet verified:** Same database-connection restriction as Tickets 2/3 — this session's tooling can't connect to a database, so the migration hasn't been pushed or exercised against a live project. Manual follow-up needed (see `supabase/README.md`): push this migration; sign up a new user and confirm they're routed to `/onboarding` (not `/`); submit the form and confirm the `profiles` row picks up `name`/`height_cm`/`goal_weight_kg`/`goal_type` and a single `weight_logs` row is inserted; reload and confirm the same user now lands on `/` directly; visit `/onboarding` directly as that user and confirm it redirects to `/`.

**Deviation (added after initial review — calorie target calculation):** Requested directly: onboarding now also computes and stores a daily calorie target (Mifflin-St Jeor BMR -> activity-scaled TDEE -> goal-adjusted target), and is presented as an MUI `Stepper` (5 steps: about you, body metrics, activity level, goal, calorie target) instead of the single flat form described above. This required collecting three fields beyond the original ticket's scope (`height`/`weight`/`goal weight`/`goal type`):

- `birthdate` (replaces a raw "age" input — stored so a future yearly cron can recompute `daily_kcal_target` as age changes, without the user re-entering anything; no such cron is built yet, this only lays the data model for it).
- `sex` (`biological_sex` enum: `male`/`female`) — not requested, but Mifflin-St Jeor requires it for a correct BMR; omitting it would silently produce wrong calorie targets for one sex.
- `activity_level` (enum: `sedentary`/`light`/`moderate`/`very_active`/`extremely_active`) — needed to scale BMR into TDEE.

For `lose`/`gain` goals, the calorie-target step presents two computed presets — "Steady" (TDEE × 0.85 lose / × 1.15 gain) and "Aggressive" (TDEE × 0.75 lose / × 1.25 gain) — each clamped to a safe floor for `lose` (1500 kcal male / 1200 kcal female per profile sex) with a UI note when clamped. For `maintain`, a single computed maintenance number (TDEE) is shown. In all three goal types, the user can instead pick "set your own target" and enter a custom value (800-10000 kcal range check), which is stored with `goal_pace = 'custom'`. Accepting the plain maintenance number (not custom) stores `goal_pace = null`, since `goal_pace`'s enum (`steady`/`aggressive`/`custom`) has no value representing "the one computed maintenance option" — `null` was judged more accurate than overloading `steady` for a goal type that isn't a deficit/surplus at all.

Also converted `profiles.goal_type` from `text check (...)` to a proper `goal_type` enum (requested directly), same closed-set pattern as `ingredient_unit`. Since this changes an already-existing column's type (not a fresh migration adding new columns), the conversion drops the old check constraint and does an `alter column ... type ... using ...` cast in the new migration (`supabase/migrations/20260824000000_onboarding_profile_and_calorie_target.sql`) rather than editing the original Ticket 2 migration.

`complete_onboarding`'s parameter list grew from 5 to 10 args (name/birthdate/sex/height/weight/activity_level/goal_type/goal_weight/goal_pace/daily_kcal_target) in the same new migration; the old 5-arg signature is explicitly `drop function`-ed first since Postgres would otherwise treat the new signature as an overload rather than a replacement. `daily_kcal_target` is computed entirely client-side (`src/features/onboarding/calorieCalc.ts`) and passed straight through to the RPC with no server-side recalculation — same "client computes, RPC just persists" pattern as every other numeric field already in this function (e.g. `weight_kg`).

`useOnboardingGate`/`RequireOnboarded`/`RedirectIfOnboarded` switched their "has this user onboarded" check from `profiles.height_cm != null` to `profiles.daily_kcal_target != null` — both are set atomically in the same RPC call, but `daily_kcal_target` is the field the whole flow exists to produce, so it's the more accurate completeness signal going forward.

**Why:** Requested directly (stepper flow, calorie target, goal_type enum); `sex` and `activity_level` were added because the requested calorie math is incorrect without them, not because the original request named them.

**Not yet verified:** Same database-connection restriction as above (org policy: must not connect to databases) — the new migration hasn't been pushed or exercised against a live project. Manual follow-up needed (see `supabase/README.md`): push this migration; walk through the stepper for all three goal types (lose/gain/maintain) plus the custom-override path and confirm the resulting `profiles` row picks up `birthdate`/`sex`/`activity_level`/`goal_pace`/`daily_kcal_target` correctly and a single `weight_logs` row is still inserted; confirm a hand-computed BMR/TDEE example matches what the app shows before submitting; confirm the old `goal_type` check-constraint values still round-trip correctly as the new enum.

**Deviation (added after second review):** Requested directly, four refinements to the stepper above:

- Swapped the vertical `Stepper`/`StepContent` layout (all 5 steps stacked, each expanding in place) for `MobileStepper` (`variant="progress"`) — a slim "Step X of 5" label plus a progress bar, with only the active step's content rendered below it. Matches "use a progress stepper" and better fits the app's mobile-first, one-thing-at-a-time layout than the original accordion-style list.
- Biological sex (About you step) changed from two `SelectableCard`s to a horizontal MUI `RadioGroup` (`row`) — a more conventional control for a two-way exclusive choice than the card pattern, which stays reserved for the larger option sets (activity level, goal type, calorie pace).
- Goal step now validates the entered goal weight against current weight and blocks Continue until it's consistent with the selected goal: a `lose` goal weight must be strictly below current weight, a `gain` goal weight strictly above it (`isGoalWeightValid`, `src/features/onboarding/calorieCalc.ts`), with an inline `TextField` error explaining which direction is expected.
- The custom calorie-target input's valid range is no longer a flat 800-10000 kcal — `getCustomKcalBounds` (same file) now scopes the ceiling to the person's own numbers (1.75× their computed maintenance/TDEE, capped absolutely at 10000 as a backstop against typos). At this point the floor was also the same per-sex safe minimum used to clamp the `lose` presets (1500 male / 1200 female) — **superseded by the fourth-review entry below**, which relaxed that floor to a flat 500 kcal sanity minimum plus a non-blocking warning, since the per-sex number turned out to be too strict for a deliberate self-chosen override. The field shows the resulting range in its helper text. Also added the requested disclaimer footnote on the calorie-target step ("estimates... not medical advice... consider checking in with a doctor or registered dietitian").

**Why:** Requested directly.

**Deviation (added after third review):** Requested directly — the birthdate field in the About You step now uses MUI X's `DatePicker` (a real calendar picker) instead of a plain `TextField type="date"`. This adds two new dependencies not listed in `frontend-architecture.md`'s tech stack table: `@mui/x-date-pickers` (pinned to `7.29.4`, the latest release whose peer range still accepts the installed `@mui/material@6.5.0` — the current `8.x`/`9.x` lines require MUI v7/v9) and `dayjs` as its date adapter (no date library existed in the project before this). `LocalizationProvider`/`AdapterDayjs` is wired once at the app root (`src/main.tsx`), not scoped to onboarding, so any future date picker elsewhere in the app (e.g. a weight-log entry date on Progress) can reuse the same provider.

**Why:** Requested directly.

**Deviation (added after fourth review):** Requested directly — the per-sex "general guidance" minimum (1500 male / 1200 female kcal/day, `MIN_SAFE_KCAL` in `calorieCalc.ts`) is a population-level unsupervised-diet guideline, not a personalized or biological floor, so it no longer hard-blocks the custom calorie-target input (real people — smaller frames, medically supervised diets — do legitimately go lower). `getCustomKcalBounds`'s hard minimum dropped from that per-sex value to a flat `500` kcal/day absolute sanity floor (`CUSTOM_KCAL_ABSOLUTE_MIN`) shared across both sexes — below this, no real protocol (even inpatient-supervised) goes lower, so it stays a hard block. Between 500 and the per-sex guidance number, entry is now allowed but `CalorieTargetStep` shows a non-blocking `Alert severity="warning"` ("consider checking in with a doctor or dietitian"). The per-sex value still hard-clamps the computed `lose` presets (`calculateCalorieOptions`) unchanged — only the custom-entry path was relaxed.

**Why:** Requested directly, after the min-kcal rationale was questioned — the original 1200/1500 hard block was appropriate for the app's own suggestions but too strict for an explicit self-chosen override.

**Deviation (added after fifth review):** Requested directly — added a mirrored, non-blocking note on the high end of a custom `gain` target. Unlike the low-end minimum (which maps to real acute-health-risk guidance), there's no equivalent clinical "max safe kcal" for a surplus — going too high just means proportionally more fat gain, not a safety issue, since muscle protein synthesis has a rate limit independent of intake (commonly cited as diminishing returns past roughly 10-20% above maintenance in evidence-based coaching sources, not a formal medical guideline). `isAboveDiminishingReturnsSurplus` (`calorieCalc.ts`) flags a custom `gain` entry that exceeds the **aggressive preset's own computed value** (already the biggest surplus the app suggests, TDEE × 1.25) and `CalorieTargetStep` shows a non-blocking `Alert severity="info"` (distinct from the low-end warning's `severity="warning"`, since this is an efficiency note rather than a health-risk one) — "most of the extra is likely to be stored as fat rather than built as muscle." The existing hard ceiling (1.75× maintenance, capped at 10000) is unchanged and still purely a typo/sanity backstop, not treated as itself being a safety line.

**Why:** Requested directly, as a follow-up to the same min-kcal conversation — once the low end became a soft warning instead of a hard block, the high end's existing hard cap needed the same "is this actually a safety line, or just a made-up sanity number" scrutiny.

---

## Fix — `group_members` RLS infinite recursion (found during Ticket 5 verification)

**Deviation:** Two of Ticket 2's RLS policies caused Postgres to report "infinite recursion detected in policy for relation group_members" on any read that touched `profiles` or `group_members` (first observed via `GET /rest/v1/profiles?select=height_cm`, i.e. `useOnboardingGate`). `"members read group membership"` (on `group_members`) queried `group_members` from inside its own `USING` clause; `"read own or group-mate profiles"` (on `profiles`) self-joined `group_members` twice, which hit that same recursive policy. Fixed in `supabase/migrations/20260821000000_fix_group_members_rls_recursion.sql` by adding `is_group_member`/`shares_group_with` `security definer` helper functions (same table-owner-bypasses-RLS pattern already used by `accept_group_invite`) and rewriting those two policies via `alter policy ... using (...)` to call them instead of self-referencing subqueries.

**Why:** Not a spec deviation so much as a correctness bug in the Ticket 2 migration's RLS gap-fill — logged here rather than editing that ticket's entry above, since this is a later migration, not a rewrite of history.

**Not yet verified:** Same database-connection restriction as above. Manual follow-up: push this migration, then retry the failing `profiles` read (e.g. reload `/onboarding` as a fresh signup) and confirm it succeeds; also spot-check a `groups`/`ingredients` read for a user in at least one group, since those policies transitively query `group_members` too.

---

## Ticket 6 — Ingredient CRUD (pantry, personal only)

**Deviation:** `check_ingredient_usage` (rpcs.md) hadn't been deployed by any prior migration — `rpcs.md`/`phase-1-tickets.md` group deploying "Phase 1 RPCs... if not already deployed alongside Ticket 2" under Ticket 11's scope, but this ticket's delete flow needs it now. Added `supabase/migrations/20260822000000_check_ingredient_usage_rpc.sql`, deploying the function exactly as specified in `rpcs.md`, no changes to its shape.

**Deviation:** `routes.md` lists only `/pantry` and `/pantry/:ingredientId` for this ticket — no creation route. Rather than inventing an undocumented URL (e.g. `/pantry/new`), the FAB on `/pantry` opens an in-place create dialog (`CreateIngredientDialog`); `/pantry/:ingredientId` is used for viewing, editing, and deleting an existing ingredient.

**Deviation:** Removed the temporary `HomePage`/`"/"` landing screen added in Ticket 3 and flagged in Tickets 3 and 5 as expected to be replaced once Pantry existed. `"/"` now redirects to `/pantry`. `design-system.md` documents profile/account actions (logout, theme toggle) as living behind a header avatar icon that opens `/profile`, but that screen is Ticket 17 and doesn't exist yet — so, matching `HomePage`'s prior treatment, the new shared `AppHeader` (`src/components/AppHeader.tsx`, used by both Pantry routes) inlines a theme-toggle button and a logout button directly instead. Expected to be replaced by the avatar-icon pattern once Ticket 17 lands.

**Deviation:** `design-system.md`'s 4-tab bottom navigation (Pantry/Recipes/Log/Progress) is not implemented yet. Only Pantry has a real screen at this point — Recipes, Log, and Progress don't exist until their own tickets (7, 8, 18) — so a nav bar linking to them would either 404 or link nowhere useful. Expected to land incrementally as those tickets are completed, fully wired by Ticket 16 (navigation animations).

**Why:** All four exist to satisfy this ticket's acceptance criteria (working create/edit/delete, delete confirmation naming affected recipes) given routes/screens that later tickets haven't built yet, without inventing undocumented routes or full nav structure ahead of the screens it would point to.

**Not yet verified:** Same database-connection restriction as Tickets 2/3/5 — this session's tooling can't connect to a database (org policy: must not connect to databases), so the new `check_ingredient_usage` migration hasn't been pushed, and the full CRUD flow hasn't been exercised against the live dev Supabase project. Manual follow-up needed (see `supabase/README.md`): push this migration; create a personal ingredient and confirm it appears only in the creating user's pantry (not another user's); edit it and confirm the change persists; add it to a recipe (once Ticket 7 exists) and confirm deleting it shows the recipe-usage confirmation before proceeding; confirm deleting an ingredient with no recipe usage skips straight to a plain confirmation.

---

## Ticket 7 — Recipe CRUD (personal only)

**Deviation:** This ticket's scope says "Create/edit a recipe" (unlike Ticket 6's explicit "Create/read/update/**delete**" wording for ingredients), and the Ticket 2 migration's own `recipes` RLS block only defines `select`/`insert`/`update` policies — no `delete` policy exists for `public.recipes` (confirmed by inspecting the migration directly; a note under Ticket 2 above claims a delete policy was added for `recipes`, but that's inaccurate — only `ingredients` and `recipe_ingredients` actually got one). Given both the scope wording and the missing RLS policy point the same direction, this ticket does **not** add a "delete recipe" feature — recipe deletion is left for a future ticket, along with adding the corresponding RLS policy. Removing an individual ingredient _from_ a recipe (`recipe_ingredients` delete) is implemented, since that policy does exist and the acceptance criteria explicitly require verifying the kcal-recalc trigger on ingredient removal.

**Deviation:** `routes.md` lists only `/recipes` and `/recipes/:recipeId` — no creation route, same gap as Ticket 6's pantry routes. Matching that ticket's precedent, the FAB on `/recipes` opens an in-place `CreateRecipeDialog` (name/servings/photo only); on success it navigates straight to `/recipes/:recipeId` rather than back to the list, since a brand-new recipe has no ingredients yet and adding them is the very next step. `/recipes/:recipeId` handles viewing, editing recipe fields, and managing its ingredient lines.

**Deviation:** Recipe detail combines "view" and "edit" into one screen (the recipe fields render directly in an editable form, same as `IngredientDetail`) rather than a separate read-only view matching `docs/mocks/recipe-detail-*.png` pixel-for-pixel. The mockup's hero-photo + total/per-serving kcal stat tiles + ingredient list pattern is followed for layout and information hierarchy, but the "Log this recipe" CTA shown in the mockup is omitted — logging doesn't exist yet (Ticket 8).

**Why:** All three exist to satisfy this ticket's acceptance criteria (create/edit a recipe, add/remove ingredients with quantities, confirm `total_kcal` recalculates via the trigger) given routes/features later tickets haven't built yet (logging) or a schema gap the ticket's own scope suggests is intentional (recipe delete), without inventing undocumented routes or an RLS policy beyond what's specified.

**Not yet verified:** Same database-connection restriction as prior tickets — this session's tooling can't connect to a database. Manual follow-up needed (see `supabase/README.md`): create a personal recipe; add ingredients from the personal pantry with quantities and confirm `total_kcal` and per-serving kcal are correct and match a manual calculation; edit an ingredient's quantity and confirm `total_kcal` updates automatically (verifies the trigger, not client math); remove an ingredient and confirm the same; confirm the unit shown next to the quantity input in "Add ingredient" is always the selected ingredient's own unit and cannot be changed.

**Deviation (added after initial review):** The recipe detail screen (`RecipeDetail.tsx`) was reworked from "each field/ingredient change writes to Supabase immediately" to a staged-draft model: editing the name/servings/photo fields or adding/editing-quantity/removing an ingredient only updates local component state, and a single page-level "Save changes" button diffs the draft against the last-persisted snapshot and writes just the changed pieces in one batch (`Promise.allSettled` over the changed `updateRecipe`/`addRecipeIngredient`/`updateRecipeIngredientQuantity`/`removeRecipeIngredient` calls). The total/per-serving kcal stat tiles are now computed client-side from the draft ingredient list on every change (using the same formula as the `recalculate_recipe_kcal` trigger) so they update live before anything is saved, rather than reading `recipes.total_kcal` directly. After a successful save, both fields and ingredients are refetched from Supabase (so `total_kcal` still reflects the trigger's authoritative recalculation, not the client formula) and become the new baseline. This also fixed a real bug carried over from `IngredientForm`'s pattern: `submitting`/`saving` was only ever reset to `false` in the `catch` branch, never on success, so a save button that didn't navigate away afterward (as `RecipeDetail` doesn't) would stay stuck showing "Saving…" forever even after success — fixed here with `try/finally`, plus an explicit success `Snackbar` ("Recipe saved") and a persistent error `Alert` on failure, both absent before.

**Why:** Requested directly — batched/staged edits with a single explicit save, non-stuck loading state, save-result feedback, and a live-updating calorie counter are not addressed by `rpcs.md`/`frontend-architecture.md` as written for this screen.

**Not yet verified (atomicity caveat):** The batch is not transactional — `rpcs.md`'s own decision rule says multi-table/multi-row writes that must succeed or fail together should be a Postgres RPC function, but building one for this was judged out of scope for a client-side UX request. If some ops in a batch fail and others succeed, the client re-fetches the persisted state as the new baseline (so a retry only reapplies what's actually still outstanding) rather than silently losing track of a partial write, but a genuinely atomic "save recipe" RPC (mirroring `copy_recipe`'s shape) would be a more correct fix and should be considered in a follow-up ticket. Not exercised against a live Supabase project for the same database-connection-restriction reason as above.

**Deviation (added after initial review):** The "Add ingredient" dialog (`AddRecipeIngredientDialog.tsx`) gained a second mode — a "From pantry" / "New ingredient" toggle. "New ingredient" reuses the Pantry feature's own `IngredientForm` component directly (not a duplicate copy of its fields) for step one — defining and creating the ingredient (name, quantity, unit, kcal, photo), an immediate write to the personal pantry, same as `CreateIngredientDialog`. Step two, shown only after that succeeds, asks for a distinct "quantity used in this recipe" value with the now-known unit shown read-only — `IngredientForm` itself has no reason to know about this, since it's recipe-specific, not part of an ingredient's own definition. Only step two's result is staged onto the recipe draft via the same `onAdd` path "From pantry" uses, so it's still subject to the page's batched Save; the ingredient itself is already persisted regardless of whether the recipe is ultimately saved.

**Why:** Requested directly, to avoid the friction of leaving the recipe screen, creating the ingredient in Pantry, and coming back to add it.

**Not yet verified:** Same database-connection restriction as above — not exercised against a live Supabase project. Manual follow-up: creating a new ingredient from this dialog should make it show up in `/pantry` afterward (it's a real personal ingredient, not recipe-scoped), and should immediately appear in the recipe's draft ingredient list with the given "quantity used," contributing to the realtime kcal total before Save is even clicked.

**Deviation (added after initial review):** Implements the "delete recipe" feature this ticket's own log above explicitly deferred, for the reason stated there (no `delete` RLS policy existed for `public.recipes`). Added `supabase/migrations/20260825000000_recipes_delete_policy.sql` (mirrors the existing recipes select/insert/update policies' ownership shape), `deleteRecipe(id)` in `src/features/recipes/api.ts`, and `DeleteRecipeDialog.tsx` — a plain confirm (no usage check, unlike ingredient delete) since deleting a recipe only cascades its own `recipe_ingredients` rows and nulls `log_entries.source_recipe_id` on entries logged from it, per `schema.md`'s "Deleting an ingredient or recipe never affects existing log entries." Wired into `RecipeDetail.tsx` as an outlined error-colored "Delete recipe" button below Save, navigating to `/recipes` on success.

**Deviation (added after initial review):** Removed the "Log this recipe" button and its dialog from `RecipeDetail.tsx` (added in Ticket 8 as a second logging entry point) at the user's direction — logging should only happen from the log screens (`/log`'s FAB), not recipe detail. `LogRecipeStep.tsx` itself is untouched since `AddLogEntryDialog` (the `/log` FAB's dialog) still uses it.

**Why:** The recipe-delete gap was a real, user-facing omission from "Recipe CRUD" with no other ticket owning it (same situation Ticket 8 addressed for log entries); the "Log this recipe" removal was a direct product-scope correction, not a technical necessity.

**Not yet verified:** Migration not applied to any live Supabase project (same database-connection restriction as every prior ticket). Manual follow-up: push the migration, then confirm deleting a recipe from `/recipes/:recipeId` removes it, redirects to `/recipes`, and that a log entry previously logged from that recipe still shows its original snapshot values with no source link.

---

## Ticket 8 — Logging (daily log + all-time log, personal only)

**Deviation:** Neither `schema.md` nor `rpcs.md`/`phase-1-tickets.md` specify what quantity gets snapshotted when logging an existing ingredient or recipe — the ticket's wording ("copy name/kcal/quantity at insert time") could mean copying the source's own defined kcal/quantity verbatim as one fixed-size entry, with no user input at all. Instead, logging an ingredient asks how much of it was eaten (in the ingredient's own unit, read-only/inherited — same rule as `recipe_ingredients.quantity_used`) and scales `snapshot_kcal` proportionally from `kcal / quantity`; logging a recipe asks how many servings were eaten and scales `snapshot_kcal` from `total_kcal / servings`. `snapshot_quantity` stores the entered amount, not the source's base quantity. This was chosen over a literal "log the source's full defined batch as one entry" reading because it mirrors the quantity-scaling pattern already established for `recipe_ingredients` in Ticket 7, and because always logging an ingredient's/recipe's entire defined quantity (e.g. a whole 500 g pantry package) would produce calorie totals that don't reflect what was actually eaten in most real use.

**Deviation:** `routes.md` doesn't define a creation entry point for `/log` (same gap as Tickets 6/7's pantry/recipe creation routes). Matching that precedent, a FAB on `/log` opens an in-place `AddLogEntryDialog` (toggle between logging an ingredient or a recipe, select from the personal pantry/recipes, then the quantity step above). Separately, `design-system.md`'s copy conventions section documents a "Log this recipe" button as a naming example and Ticket 7's entry above explicitly notes that CTA was omitted from `RecipeDetail` pending this ticket — added here as a second entry point (a button on the recipe detail screen that opens the same quantity step directly, skipping recipe selection since it's already known).

**Deviation:** `routes.md` doesn't say how `/logs` is reached from `/log` (it's a peer route, not nested under it, and isn't one of the four bottom tabs per `routes.md`'s navigation section — those aren't wired up yet regardless, see Ticket 6's note). Added a plain "View all-time history" text button on `/log` linking to `/logs`, and `/logs`' `AppHeader` back arrow returns to `/log`.

**Why:** All three exist to satisfy this ticket's acceptance criteria (correct snapshot values, a working `/log` and `/logs`, "Logging a recipe or ingredient creates a log_entries row") given genuine ambiguity in the snapshot-quantity semantics and no upstream ticket defining a creation route, navigation path, or the previously-deferred recipe CTA.

**Not yet verified:** Same database-connection restriction as prior tickets (org policy: must not connect to databases) — not exercised against a live Supabase project. Manual follow-up needed (see `supabase/README.md`): log an ingredient and a recipe from `/log`'s FAB and confirm both appear with correct scaled kcal and today's running total updates; use the "Log this recipe" button on `/recipes/:recipeId` and confirm it lands in `/log` the same way; edit or delete the source ingredient/recipe afterward and confirm the already-logged entry's snapshot values are unchanged (source id should go null on delete, per the `on delete set null` FK); confirm `/logs` shows entries across all days with no `group_id` filter applied.

**Deviation (added after initial review):** Implemented the ticket's own "fast-follow if time allows" item — editing an already-logged entry's snapshot values — plus deleting an entry outright (deletion wasn't mentioned at all in the ticket, but there was no other route to correcting a mis-logged entry once editing exists, and no later ticket in `phase-1-tickets.md` owns it either). Tapping a `LogEntryCard` on `/log` or `/logs` opens `EditLogEntryDialog`, which can update `snapshot_name`/`snapshot_kcal`/`snapshot_quantity` directly (`source_ingredient_id`/`source_recipe_id`/`logged_at` are left untouched — this edits the log entry itself, not what it was logged from) or delete the row via a plain confirm dialog (`DeleteLogEntryDialog` — no usage check needed, unlike ingredient delete, since nothing else references a log entry).

**Deviation:** `log_entries` had select/insert/update RLS policies from the Ticket 2 migration (per `schema.md`'s instruction to copy the three-policy shape from `ingredients`) but no delete policy — the same gap `ingredients` had before Ticket 2 filled it in, just never revisited for `log_entries` since deletion wasn't needed until now. Added `supabase/migrations/20260823000000_log_entries_delete_policy.sql`, mirroring the existing "update own or group log entries" policy's shape.

**Why:** Requested directly, since this ticket's own acceptance criteria left snapshot editing unowned and no ticket in the Phase 1 backlog picks it up, and deleting a mis-logged entry is the more common real-world need of the two.

**Not yet verified:** Same database-connection restriction as above — not exercised against a live Supabase project. Manual follow-up in `supabase/README.md` under "Ticket 8 fast-follow manual verification."

---

## Ticket 9 — Outbox pattern with backoff and reconnect retry

**Deviation:** Added a full `/sync-status` screen (list of outbox items by status, manual retry/discard) rather than just a basic sync indicator.

**Why:** Ticket 9 referenced a "sync issues" screen as a fast-follow, but no ticket was ever created for it — folding it in here instead of leaving the gap unaddressed.

**Deviation:** `routes.md` does not actually list `/sync-status` (checked directly — no mention anywhere in the file). Added the route under the same `RequireAuth`/`RequireOnboarded` guard as the four main tabs, reachable at `/sync-status`, with its `AppHeader` back arrow returning to `/pantry` (matching the fixed-parent-route convention `/logs` and the detail routes already use, since there's no single obvious parent tab for a cross-cutting sync screen). `routes.md` itself was left unedited per this ticket's standing instruction not to edit core docs to match code.

**Why:** The route needed to exist somewhere for the screen to be reachable; documenting its actual shape here (rather than assuming the doc already covered it) keeps this log accurate for whoever reconciles `routes.md` after Phase 1.

**Deviation:** Added `dexie-react-hooks` as a new dependency, used by `SyncStatusList` (`useLiveQuery(() => db.outbox.toArray())`) so the screen reflects retries/discards/reconnect-driven changes live. `frontend-architecture.md` names this package for all Dexie reads but it wasn't installed yet — no prior ticket had wired a screen to read from Dexie directly until now (existing CRUD screens still read from Supabase directly; Dexie-backed reads land in Ticket 10).

**Why:** Matches the doc's stated pattern ("Reads go through dexie-react-hooks' useLiveQuery... not manual polling") rather than introducing a one-off polling/manual-refresh mechanism for this screen alone.

**Not yet verified:** No browser automation tooling available in this environment to drive the real auth-gated app — verification was `tsc -b`, `oxlint`, and `vite build`, all clean, plus manual reading of the rendered JSX against `design-system.md`'s card pattern. Manual follow-up: log in, populate `waiting_for_connectivity` and `failed` outbox items (e.g. via DevTools offline mode and a simulated RLS denial), confirm both sections render with the correct copy/color treatment, and confirm "Retry now"/"Discard" behave as described (single attempt, no silent re-retry) and the empty state shows once the outbox is clear.

---

## Ticket 9 — Outbox pattern with backoff and reconnect retry (fix, found during manual testing)

**Deviation:** `frontend-architecture.md`'s Dexie schema sample indexes the `outbox` table as `'id, created_at'` only. `outbox.ts` queries it with `db.outbox.where('status').equals(...)` in three places (draining `pending`/`waiting_for_connectivity` items on reconnect/startup, counting `failed` items for `useSyncStore`) — Dexie requires a field to be indexed to `.where()` on it, so this threw `SchemaError: KeyPath status on object store outbox is not indexed` at runtime (surfaced when reconnecting after an offline test, in the `online`-event handler's `.where('status')` call). Fixed by adding a `db.version(2).stores({ outbox: 'id, created_at, status' })` bump in `src/lib/db.ts`, on top of the existing `version(1)` block — Dexie auto-upgrades a browser's existing v1 database in place, no data loss, no custom upgrade function needed since this only adds an index.

**Why:** The doc's schema sample doesn't anticipate `status`-based queries; without the index, every reconnect and every startup drain throws instead of retrying.

---

## Ticket 9 — Outbox pattern with backoff and reconnect retry (fix, found during manual testing)

**Deviation:** `frontend-architecture.md`'s outbox sample has no protection against the same item being drained twice concurrently. Found via manual testing: toggling network conditions back online after 5 failed auto-retries caused the queued mutation to be inserted twice server-side. Root cause — the browser (confirmed in this case via DevTools' network throttle toggle, but real network reconnects can do the same) fired the `online` event more than once for a single reconnect; `retryWaitingForConnectivity()` ran twice, each call fetched the same `waiting_for_connectivity` item and started its own independent `syncItem` call before either had deleted the row. Fixed by adding a module-level `draining` `Set<string>` of in-flight item ids in `src/sync/outbox.ts` — `drainOutboxWithRetry` and `retryFailedItem` both no-op if the item's id is already in the set, added on entry and removed on every terminal outcome (success, permanent failure, or `waiting_for_connectivity`).

**Why:** Without this, any source of a duplicate trigger (a double-fired browser event, a double-click on "Retry now", or a future caller invoking `drainPendingOutbox`/`retryWaitingForConnectivity` while a chain is already running) can double-submit a mutation with no way to detect or undo it after the fact — worse than the transient-failure problem the outbox exists to solve.

---

## Ticket 9 — Outbox pattern with backoff and reconnect retry (fix, found during review)

**Deviation:** Added a small sync-status icon to `AppHeader` (shared across every route that renders it — Pantry, Recipes, Log, all detail pages, and `/sync-status` itself) that appears only when `useSyncStore().status` is `syncing` or `error`, and navigates to `/sync-status` when tapped. Ticket 9's original scope only listed `useSyncStore` reflecting status "for UI consumption," which was initially implemented as store-only with no rendered element at all — on review, the ticket's own "Out of scope: any UI beyond a basic sync status indicator" line more naturally reads as implying a basic indicator was in scope, and without one, `/sync-status` (added per this same deviation log, above) had no discoverable entry point anywhere in the app — reachable only by typing the URL directly.

**Why:** Closes that gap without overbuilding — the icon is absent entirely in the common `idle` case (no clutter, matches `design-system.md`'s restraint), and gives `/sync-status` an actual way to be found.

---

## Ticket 10 — Wire Dexie + outbox into existing CRUD screens

**Deviation:** `recipe_ingredients` (the ingredients-within-a-recipe join table) stays a live Supabase read/write in `src/features/recipes/api.ts` (`fetchRecipeIngredients`, `addRecipeIngredient`, `updateRecipeIngredientQuantity`, `removeRecipeIngredient`) rather than going through Dexie + the outbox like the recipe's own fields (`name`/`servings`/`photo_url`, via `updateRecipe`) now do. `frontend-architecture.md`'s Dexie schema sample never lists a `recipe_ingredients` table to begin with, and `total_kcal` is only ever correct once `recalculate_recipe_kcal` (schema.md) has run server-side — there's no client-side formula the outbox could apply optimistically that wouldn't risk showing a wrong total until the next pull. `RecipeDetail` is restructured so this is a partial-offline screen rather than an all-or-nothing one: the recipe's own fields load from Dexie (instant, works offline) and save through the outbox independently of the ingredient lines, which load from Supabase in a separate effect with their own loading/error state and still require connectivity to save.

**Why:** Ticket 10's scope says to wire Dexie + outbox into "Pantry/Recipes/Log screens," but doesn't call out the recipe/recipe_ingredients split specifically. Queuing recipe_ingredients edits into the outbox without a client-side total_kcal recalculation would mean the UI shows a stale or guessed total until the next successful sync — worse than clearly requiring connectivity for that one part of the screen while still letting the rest of the ticket's offline guarantees (recipe rename, ingredient CRUD, logging) hold.

**Deviation:** `log_entries` has no `updated_at` column (schema.md) — `updateLogEntry` (Ticket 8's fast-follow) edits `snapshot_name`/`snapshot_kcal`/`snapshot_quantity` without bumping any timestamp column at all. `src/sync/pull.ts`'s incremental pull therefore uses `created_at` as `log_entries`' cursor (the only timestamp column it has). A log entry edited on one device, after another device already pulled it once, will not be re-pulled by that second device until its next full sync (there isn't one, short of clearing Dexie) — the edit is invisible cross-device, though it's correct immediately on the device that made it (read straight from its own Dexie).

**Why:** This is a real gap, but fixing it means adding an `updated_at` column to `log_entries` — a schema change out of scope for a ticket about wiring existing screens to existing tables. Flagging it here rather than silently shipping a pull that looks generically correct but has this specific blind spot. A real fix (schema migration adding `log_entries.updated_at`, bumped on update) is future work.

**Deviation:** Added a `sync_meta` table to Dexie (`src/lib/db.ts`, `db.version(3)`) storing one row per table-and-scope with the ISO timestamp of the last successful pull (`src/sync/pull.ts`'s cursor). `frontend-architecture.md`'s Dexie schema sample has no such table — "fetch changes since last sync per table" (this ticket's own scope line) needs _something_ durable to remember "since" across reloads, and no other doc names where that value should live.

**Deviation:** Added `src/sync/pull.ts` (the pull side named in `frontend-architecture.md`'s file listing but never implemented until now) and `src/sync/useSyncEngine.ts` (not named in any doc) — a hook mounted once in `App.tsx` that resumes the outbox (`drainPendingOutbox`, written in Ticket 9 but never actually called from anywhere until now) and runs an initial pull, then a pull every 60 seconds and again on the browser's `online` event for as long as a user is signed in. The 60-second interval isn't specified anywhere — chosen as a middle ground between catching another device's changes reasonably promptly and not calling Supabase on every tick while the tab just sits open; easy to tune later without any structural change.

**Deviation:** Both `fetchIngredients`/`fetchIngredient` (pantry), `fetchRecipes`/`fetchRecipe` (recipes), and `fetchTodayLogEntries`/`fetchAllLogEntries` (logging) changed from Supabase reads to Dexie reads but kept their exact prior signatures and return shapes (aside from the single-row fetchers now resolving `undefined` instead of throwing on a missing row, since a thrown promise rejection inside `useLiveQuery` has no obvious place to be caught). This let call sites that only need a point-in-time list (`AddRecipeIngredientDialog`'s pantry picker, `AddLogEntryDialog`'s ingredient/recipe pickers) keep their existing `useEffect` + `.then(setState)` shape unchanged — they're still "reads from Dexie" per `frontend-architecture.md`, just not re-wired to `useLiveQuery` individually, since picking a snapshot list once when a dialog opens has no need for live reactivity.

**Why:** All of the above are the minimum needed to satisfy this ticket's three acceptance criteria (offline create/edit appears in the outbox; reconnecting drains it to Supabase; offline reload shows cached data) without rewriting more of the surrounding screens than the ticket asked for.

**Not yet verified:** Same database-connection restriction as prior tickets (org policy: must not connect to databases) — not exercised against a live Supabase project, and no browser automation tooling available in this environment to toggle real network conditions. Verification here was `tsc -b`, `oxlint`, and `vite build`, all clean. Manual follow-up needed — see `supabase/README.md` under "Ticket 10 manual verification."

---

## Ticket 11 — Group CRUD, membership, and invites

**Deviation:** `rpcs.md` has no entry for group creation, and the Ticket 2 migration deliberately left `group_members` with no client-facing insert policy ("membership rows are created via the group-creation flow and the `accept_group_invite` RPC, both of which run as security definer"), which at the time referred to a group-creation flow that didn't exist yet. A plain client insert into `groups` (which does have an RLS insert policy) followed by a plain insert into `group_members` would therefore fail on the second step — creating a group and becoming its owner is a multi-table write that must succeed or fail together, matching `rpcs.md`'s own decision rule. Added `create_group(p_name, p_description)` as a new `security definer` RPC (`supabase/migrations/20260826000000_group_rpcs.sql`, alongside the previously-undeployed `accept_group_invite` from `rpcs.md`, deployed here verbatim), following the same shape as `complete_onboarding` (Ticket 5) and `accept_group_invite` itself.

**Deviation:** Groups are read and written straight against Supabase (`src/features/groups/api.ts`), not through Dexie + the outbox like Pantry/Recipes/Log. Two independent reasons: (1) `frontend-architecture.md`'s Dexie schema lists a `groups` table, but no prior ticket ever pulls into it, and "groups I belong to" is a `group_members`-joined query that `sync/pull.ts`'s generic per-table scope logic doesn't cover — wiring that up is a bigger lift than this ticket's scope (list + create + invite); (2) both group creation (`create_group`) and invite acceptance (`accept_group_invite`) are atomic `security definer` RPCs, which the outbox has no concept of (it only knows single-table insert/update/delete against one row). This mirrors the precedent set in Ticket 5 (onboarding) for network-inherent, atomic operations. `createGroup` does mirror the newly-created row into Dexie's `groups` table after the fact, so the table isn't entirely dead, but reads (`fetchMyGroups`) still go live to Supabase.

**Deviation:** `routes.md` doesn't define a route for invite generation, and this ticket's own "Out of scope" line ("Group settings screen beyond basic creation — Ticket 13") rules out building a `/groups/:groupId` detail/settings route to host it. Generate/share lives entirely on `/groups` instead: each owned `GroupCard` shows an invite icon that opens an in-place `InviteDialog` (`src/features/groups/InviteDialog.tsx`) — a plain `group_invites` insert (already covered by the Ticket 2 migration's "owner creates group invites" policy, no RPC needed) that surfaces the resulting `/invite/:code` link with a copy button.

**Deviation:** `rpcs.md` says `accept_group_invite` should redirect to `/groups/:groupId/pantry` on success — that route doesn't exist until Ticket 12 (group-scoped screens are explicitly out of scope here). `AcceptInvite` (`src/features/groups/AcceptInvite.tsx`) redirects to `/groups` instead, after briefly showing which group was joined (a separate `groups.name` lookup, since `accept_group_invite` only returns the group id).

**Deviation:** `/invite/:inviteCode` is gated by `RequireAuth` only, not `RequireOnboarded` (unlike every other authenticated route). `routes.md`'s own note says this route "needs to work for a logged-in user clicking a link from anywhere" since it can't assume prior navigation context — extending that reasoning, a user who hasn't finished onboarding yet shouldn't be blocked from accepting a time-limited invite link just because `/onboarding` intercepts every other route first.

**Deviation:** `/groups` isn't one of the four bottom tabs (`design-system.md`) and no nav bar exists yet regardless (Ticket 16). Added a "Groups" icon button to the shared `AppHeader` (visible on every screen except `/groups` itself) as its only discoverable entry point — same "would otherwise be reachable only by typing the URL" reasoning as the `/sync-status` icon added in Ticket 9's review fix.

**Why:** All six exist to satisfy this ticket's acceptance criteria (create a group and become its owner atomically, generate/share/accept an invite code, single-use and expiry enforcement) given real gaps in `rpcs.md` (no group-creation RPC) and routes/screens later tickets own (`/groups/:groupId/pantry`, group settings, the nav bar), without inventing a settings route or an offline sync path this ticket doesn't need.

**Not yet verified:** Same database-connection restriction as every prior ticket (org policy: must not connect to databases) — `create_group` and `accept_group_invite` have not been pushed or exercised against a live Supabase project. This ticket's own acceptance criteria explicitly call for testing the `for update` lock under **concurrent** acceptance attempts, which additionally needs two simultaneous requests against a live database — not something provable from the code alone even once connectivity restrictions are lifted. Manual follow-up needed — see `supabase/README.md` under "Ticket 11 manual verification."

**Deviation (fix, found during review):** `AcceptInvite` originally called `accept_group_invite` immediately in a `useEffect` on mount — opening `/invite/:inviteCode` at all silently consumed the single-use code, with no preview of which group it was for and no way to back out. Added `preview_group_invite(p_invite_code)` (`supabase/migrations/20260827000000_preview_group_invite_rpc.sql`) — a read-only, `security definer` function (needed because the invitee isn't a group member yet, so plain RLS blocks both `group_invites`, owner-only select, and `groups`, members-only select, for them) that returns the target group's name without touching `accepted_at`/`accepted_by`. `AcceptInvite` now previews on load, shows "Join `<group name>`?" with explicit "Join group"/"Not now" buttons, and only calls `accept_group_invite` when the user taps "Join group." An invalid/expired code now surfaces the same error at the preview step, before any accept attempt. This also let the mount-time StrictMode double-invoke guard (`attempted` ref) be removed — the effect that remains is a read-only preview (safe to double-fire), and the actual accept call now only ever runs from a button's `onClick`, which StrictMode doesn't double-invoke.

**Why:** Requested directly, after review flagged that a single-use invite code was being burned just by opening the link — before this fix there was no `/invite/:inviteCode` state where the user actually made a choice.

**Not yet verified:** Same database-connection restriction as above — `preview_group_invite` hasn't been pushed or exercised against a live project. Manual follow-up folded into `supabase/README.md`'s existing "Ticket 11 manual verification" section.

---

## Ticket 12 — Group context switcher applied across Pantry/Recipes/Log

**Deviation:** `routes.md`'s own snippet for `/pantry` vs. `/groups/:groupId/pantry` derives `groupId` straight from `useParams` inside the page component and threads it down as a prop — this ticket follows that literally rather than routing it through `useAppStore`'s `activeGroupId` for rendering. `activeGroupId` (already in the store since Ticket 3, unused until now) is still kept in sync with the route via a new `useSyncedActiveGroupId(routeGroupId)` hook (`src/store/useAppStore.ts`), called once per top-level tab/detail route page — but purely as a side effect for consumers outside the route tree (`useSyncEngine`), not as the source of truth `PantryPage`/`RecipesPage`/`LogPage`/`IngredientDetailPage`/`RecipeDetailPage` themselves read from.

**Deviation:** `useSyncEngine` (Ticket 10) pulled exactly one scope — `{ userId, groupId: activeGroupId }` — which was always personal, since nothing ever set `activeGroupId` before this ticket. Once switching into a group context sets it, pulling only that scope would let personal data (and any other group's data) go stale in Dexie for as long as the user stays in that group's screens — a real problem given `/logs` reads across every scope from local Dexie with no filter at all. Changed `runPull` to always pull the personal scope and, additionally, the active group's scope when one is set (`Promise.allSettled` over both — one scope's failure, e.g. offline, doesn't block the other). This does not pull every group the user belongs to, only the one currently being viewed — `pull.ts`'s `PullScope` was already written generically "so Ticket 12 can reuse this unchanged," and calling it per-scope rather than teaching it to fan out over an arbitrary list of groups keeps that reuse literal. A group's data can still go stale in Dexie while the user is browsing a _different_ group's screens (not personal, not that group) — judged an acceptable Phase 1 gap given the same "small trusted household members" premise `frontend-architecture.md` already applies to the sync engine as a whole.

**Deviation:** `fetchIngredients`/`fetchRecipes`/`fetchTodayLogEntries` (previously personal-only, hardcoding `group_id: null` per their own Ticket 6/7/8 comments) now take a `groupId: string | null` parameter. For `groupId !== null` they query Dexie's `group_id` index directly (already present since Ticket 10's `db.version(1)` schema — no Dexie version bump needed) with **no** `created_by`/`logged_by` filter, matching schema.md's RLS shape for group rows ("group_id in (select group_id from group_members where user_id = auth.uid())" — any member sees the whole group's data, not just their own contributions). `createIngredient`/`createRecipe`/`createLogEntry` gained the same `groupId` parameter, written straight to the new row's `group_id` column. Every call site (`CreateIngredientDialog`, `CreateRecipeDialog`, `AddRecipeIngredientDialog`'s "From pantry"/"New ingredient" paths, `AddLogEntryDialog`) now threads the screen's active `groupId` through, so creating/browsing pantry items, recipes, and log entries from inside a group's screens reads and writes that group's shared data rather than the personal one — this is the acceptance criterion "changes are visible to other members."

**Deviation (fix, found during this ticket's own review):** `fetchAllLogEntries` (the `/logs` cross-context view, Ticket 8) filtered rows to `group_id === null` despite its own comment stating "no group_id filter, by design... spans every group." That filter happened to look correct only because no group-scoped log entries existed until this ticket. Removed the filter — the Dexie query (`where('logged_by').equals(userId)`) already matches routes.md's documented `/logs` semantics ("everything the user has logged, personal and every group combined") without it.

**Deviation:** `routes.md` doesn't specify how the context switcher chip navigates on selection (only design-system.md's visual description — "pill-shaped... shows current context name with a dropdown chevron; tapping opens the group/personal picker"). Implemented as `src/components/ContextSwitcher.tsx` — a `Chip` (MUI has no built-in "chip that opens a menu" component; `deleteIcon`/`onDelete` is used as the chevron/click target since `Chip` only exposes one icon slot at the start) opening a `Menu` listing "Personal" plus every group from `fetchMyGroups` (the same live Supabase call `GroupList`/Ticket 11 already uses — not wired to Dexie, per that ticket's own deviation note). Selecting an entry navigates to the equivalent route for the chosen context (`/pantry` ↔ `/groups/:id/pantry`, etc.) rather than mutating any shared state directly.

**Deviation:** `IngredientDetail`/`RecipeDetail` (Tickets 6/7) hardcoded `navigate('/pantry')`/`navigate('/recipes')` after a successful save/delete. Both now take a `backPath` prop, computed by their route page (`/groups/:groupId/pantry` vs. `/pantry`, etc.), so saving or deleting an ingredient/recipe from inside a group's screens returns to that group's list, not the personal one.

**Why:** All of the above are the minimum needed to satisfy this ticket's three acceptance criteria (switching context re-scopes Pantry/Recipes/Log; a group member's changes are visible to other members; personal and group data never bleed into each other) given real gaps in how far Tickets 6/7/8/10 hardcoded the personal-only assumption, plus one real bug (`fetchAllLogEntries`) that would have silently broken the routes.md-documented `/logs` behavior the moment group log entries started existing.

**Not yet verified:** Same database-connection restriction as every prior ticket (org policy: must not connect to databases) — not exercised against a live Supabase project, and no browser automation tooling available in this environment to drive the real auth-gated app end-to-end (creating a second account to verify cross-member visibility needs two authenticated sessions against a live project regardless). Verification here was `tsc -b`, `oxlint`, and `vite build`, all clean, plus manual reading of the rendered JSX against design-system.md's context switcher description. Manual follow-up needed: as a member of at least one group, confirm the context switcher lists "Personal" plus every group and switching updates both the URL and the visible pantry/recipes/log data; add an ingredient/recipe/log entry while in a group's context and confirm a second account in the same group sees it after their next pull (or immediately via their own outbox-drain-then-pull cycle); confirm an ingredient created from personal Pantry never appears in a group's pantry and vice versa; confirm `/logs` now shows entries logged in every group the account belongs to, not personal-only.

**Deviation (added after initial review — creator/last-updated metadata):** Requested directly: group-scoped pantry/recipe screens now show who added an ingredient/recipe, and — once it's actually been edited — who last changed it and when. `schema.md`'s `ingredients`/`recipes` tables have `created_by` but no equivalent "who last edited this" column (only `updated_at`, a timestamp with no actor). Added `updated_by uuid references profiles(id)` to both tables (`supabase/migrations/20260828000000_ingredients_recipes_updated_by.sql`), nullable and left unset at insert time — `created_by` already answers "who created it" for a new row, so `updated_by` staying `null` is itself the signal that a row has never been edited since creation, which is what the client uses to decide whether to show `created_at`/`created_by` or `updated_at`/`updated_by` (never both), matching the exact behavior requested. `updateIngredient`/`updateRecipe` (`src/features/pantry/api.ts`, `src/features/recipes/api.ts`) both gained a `userId` parameter and now write `updated_by` alongside `updated_at` on every plain field edit. A recipe can also be "edited" purely by changing its ingredient list (`recipe_ingredients` add/remove/quantity-change), which never calls `updateRecipe` at all — the existing `recalculate_recipe_kcal` trigger (schema.md) already bumps `updated_at` on that path, so the same migration also updates it to set `updated_by = auth.uid()`, which is available since the trigger runs as the requesting user (no `security definer`), same as any plain client update.

**Deviation:** `fetchMyGroups`-style live Supabase reads are the established pattern for anything not worth mirroring into Dexie (Ticket 11's own note); resolving `created_by`/`updated_by` ids into display names is the same shape — added `fetchProfileNames(userIds)` (`src/features/profiles/api.ts`, a new feature folder — profiles had no reads/writes of their own before this) as a live `profiles` select, plus a `useProfileNames` hook that keys its effect on a sorted/joined string of the ids rather than the array reference (callers pass a freshly-mapped array every render). This relies on the "select own row or a fellow group member's row" `profiles` RLS policy from Ticket 2 — safe here specifically because a user can only ever be looking at a group's ingredients/recipes if they're already a member of that group.

**Deviation:** Design-system.md's card subtitle pattern already anticipates this ("name plus a secondary detail — quantity+who added it, or servings+group"), so `IngredientCard`/`RecipeCard` gained an optional `creatorName` prop appended to their existing quantity/servings subtitle (`· Added by {name}`) — populated by `PantryList`/`RecipeList` only in group context, left `undefined` in personal context (no visual change there) or while still loading. The fuller creator-and-updater-with-dates block (`src/components/ItemMetadata.tsx`) is shown only on `IngredientDetail`/`RecipeDetail`, gated on `groupId`, not on the list cards — keeping list rows to the same one-line-subtitle restraint the design system already uses elsewhere, rather than cramming timestamps into a compact card.

**Why:** All of the above are the minimum needed to add creator/last-editor metadata given a real schema gap (`updated_by` didn't exist) and no prior ticket ever needing to resolve another user's display name.

**Not yet verified:** Same database-connection restriction as above — the new migration hasn't been pushed or exercised against a live project. Manual follow-up folded into `supabase/README.md`'s "Ticket 12 manual verification" section.

**Deviation (fix, found during manual testing — "Added by someone"):** Reported directly: editing a group ingredient/recipe showed "Added by someone" instead of the actual creator's name. Root cause — `IngredientDetail`/`RecipeDetail` built their profile-lookup id list with `.filter((id) => id !== null)`, which doesn't catch `undefined`. A Dexie row cached locally _before_ this ticket's migration added `updated_by` (any ingredient/recipe not touched since — incremental pull only re-fetches rows whose `updated_at` changed, so such a row never picks up the new column) has `updated_by` missing entirely (`undefined` at runtime, regardless of the `string | null` type), not `null`. That `undefined` slipped through the filter into the id list, `Array.prototype.join` stringified it to an empty segment, and Supabase's `.in('id', [...])` filter rejected the _whole_ query as an invalid uuid — which wiped out every id in the batch (via `useProfileNames`'s catch-all `setNames({})`), not just the bad one, so even the perfectly resolvable creator id came back empty. Fixed by switching both call sites to `!= null` (catches `undefined` too), deriving a `wasUpdated` boolean the same way (a legacy row was previously misread as "edited" by `!== null`, since `undefined !== null` is `true`), and hardening `fetchProfileNames`/`useProfileNames` themselves to `.filter(Boolean)` the id list before ever building the key or the query — so any future caller that makes the same `!==`-vs-`!=` mistake degrades to "skip the bad id" instead of failing the whole batch.

**Why:** Real regression against existing local data, not a live-project-only concern — reproducible on any device with ingredients/recipes cached before this migration landed, which is the common case for an app already past several tickets of manual testing.

**Deviation (requested directly — servings → weight):** `schema.md`'s `recipes.servings` (`integer not null default 1`) is replaced with `weight_g` (`numeric not null`, no default) — a recipe is now defined by its total weight in grams rather than a serving count. `supabase/migrations/20260829000000_recipes_servings_to_weight.sql` renames the column in place (`alter table ... rename column`) and retypes it to `numeric`, rather than adding a new column and dropping the old one — there's no meaningful data to migrate (Phase 1 has no production users), and an in-place rename/retype is the smaller, more honest change for what's actually a field-purpose pivot, not a new field addition. The `recalculate_recipe_kcal` trigger (schema.md) doesn't reference `servings`/`weight_g` at all — it only ever computed `total_kcal`, so it needed no change.

`RecipeInput`/`RecipeForm` (`src/types/recipe.ts`, `src/features/recipes/RecipeForm.tsx`) drop the "Servings" number field for a weight entry pair — a number field plus a `g`/`kg` unit select (new `WeightUnit` type) — mirroring `IngredientForm`'s quantity+unit layout. The unit picker is entry convenience only: selecting `kg` multiplies by 1000 before it's ever passed to `onSubmit`/stored, so `weight_g` (both the type field and the column) is always grams, and no unit choice is persisted anywhere — editing an existing recipe always shows its stored gram value with `g` preselected, never reconstructing "0.5 kg" from a value that was originally entered that way. `RecipeDetail.tsx`'s own inline fields (a separate, staged-draft implementation of the same fields — see Ticket 7's deviation log — not the `RecipeForm` component, which only the create dialog uses) got the identical Weight+Unit replacement for its "Servings" field, plus its dirty-check/validity logic (`weightG !== savedRecipe.weight_g`, `weightG > 0`) and its `handleSave` payload updated from `servings` to `weight_g`.

The recipe detail/card kcal-density stat changed from "per serving" to "kcal per gram" everywhere it appeared: `RecipeDetail`'s stat tile (renamed `perServing` → `perGram`, `total_kcal / weight_g`, shown to 2 decimal places instead of 0 — matching `IngredientCard`'s own kcal-per-unit precision, since per-gram values are typically small, e.g. `1.20` not `120`) and `RecipeCard`'s subtitle/secondary-metric (`{weight_g} g` instead of `{servings} serving(s)`; `{perGram.toFixed(2)}/g` instead of `{perServing.toFixed(0)}/serving`).

`LogRecipeStep.tsx` (Ticket 8) asked "how many servings were eaten" and scaled `snapshot_kcal` from `total_kcal / servings`; a recipe no longer has a serving count to divide by, so this now asks "how many grams were eaten" (`total_kcal / weight_g * gramsEaten`), and `snapshot_quantity` now stores grams eaten instead of servings eaten. `log_entries.snapshot_quantity` (schema.md) has no unit column to record which this is (the same is already true of ingredient logging — `LogIngredientStep` stores the ingredient's own unit-quantity with no unit column either), so this isn't a new gap, just the same existing one now also applying to recipe logging.

`docs/rpcs.md`'s undeployed `copy_recipe` example (`insert into public.recipes (..., servings, ...) select ..., servings, ...` — Phase 3, no migration has ever created this function) still references the old column name; left as-is per this ticket's standing instruction not to edit core docs, but whoever implements `copy_recipe` will need to update that example to `weight_g` first.

**Why:** Requested directly.

**Not yet verified:** Same database-connection restriction as above (org policy: must not connect to databases) — the new migration hasn't been pushed or exercised against a live project. Manual follow-up: push the migration; create a recipe with the weight field set in `kg` and confirm the stored `weight_g` is the ×1000 value; edit an existing recipe and confirm its weight shows in `g` with the correct stored value, not reconstructed into `kg`; confirm the detail screen's stat tile and the list card both show "kcal per gram"/`.../g` correctly as ingredients are added/removed; log a recipe from `/log`'s FAB and confirm the entered gram amount scales `snapshot_kcal` correctly against the recipe's `total_kcal`/`weight_g`.

**Deviation (fix, found during manual testing — `RecipeCard` numbers not matching `RecipeDetail`):** Reported directly: `/recipes`' list cards showed a different total-kcal number than the same recipe's own detail screen. `RecipeCard.tsx` interpolated `recipe.total_kcal` raw (`{recipe.total_kcal} kcal`, no rounding), while `RecipeDetail.tsx`'s stat tile always renders `.toFixed(0)`. `total_kcal` is server-computed by the `recalculate_recipe_kcal` trigger as `sum(i.kcal * ri.quantity_used / i.quantity)` — division and summation that very routinely lands on a long trailing decimal (e.g. `245.83999999999997`) even when every input is a clean number, so the card and the detail screen could show visibly different-looking numbers for the exact same, correct underlying value. Fixed by rounding `RecipeCard`'s total-kcal display the same way (`recipe.total_kcal.toFixed(0)`) — matching the precedent already established for another raw DB `numeric` field, `LogEntryCard`'s `entry.snapshot_kcal.toFixed(0)`.

While investigating, found a second, related bug in the same area: `RecipeForm`/`RecipeDetail`'s kg→g conversion (`parsedWeight * 1000`) is itself subject to floating-point error — e.g. `1.005 * 1000 === 1004.9999999999999` in JS — so entering a perfectly clean `kg` value could store and display a similarly ugly gram value. Fixed by rounding the conversion to the nearest gram (`Math.round(parsedWeight * 1000)`) in both files, before the value is ever submitted or compared — this also prevents a subtler consequence in `RecipeDetail`: an unrounded `weightG` would never exactly equal the integer `weight_g` the server actually stored and returned, making the dirty-check spuriously report unsaved changes right after a successful save.

**Why:** Real, reproducible display/precision bugs, not a live-project-only concern — both are pure client-side arithmetic issues, verifiable without a database connection (confirmed the exact floating-point behavior via a plain Node repl).

**Deviation (fix, found via screenshots — recipe totals racing on multi-ingredient saves):** The `.toFixed(0)` fix above wasn't the actual bug — reported directly with screenshots showing a genuinely different number, not a formatting mismatch: a recipe with two ingredients (150 kcal, 40 kcal) showed 190 total kcal on its own detail screen but 40 kcal on the `/recipes` list card for the exact same recipe. Root cause: `RecipeDetail.handleSave`'s ingredient-line batch (`Ticket 7`'s original design, unrelated to this ticket) built its `ops` array as `ops.push(addRecipeIngredient(...))` — calling the async function immediately, so pushing two adds (or any mix of add/remove/quantity-change) started two independent Supabase requests/transactions _concurrently_, then just waited on both via `Promise.allSettled`. The `recalculate_recipe_kcal` trigger (schema.md) recomputes `total_kcal` from scratch on every `recipe_ingredients` change by re-reading every row for that recipe at the moment it fires. Under concurrent transactions (read-committed isolation, the Postgres default), that's a lost-update race: whichever transaction's `UPDATE recipes SET total_kcal = ...` commits last wins the final value, but if _that_ transaction's own `SELECT sum(...)` ran before the other transaction's insert had committed, it overwrites the correct combined total with a partial one — in the reported case, the recompute triggered by inserting the 40-kcal ingredient apparently committed last while only seeing itself, stomping the already-correct 190. `RecipeDetail`'s own stat tile never showed this, because it computes `totalKcal` live from the client's in-memory ingredient list (`ingredients.reduce(...)`), not from the persisted `recipes.total_kcal` the list card has to rely on — so the detail screen was accidentally masking a real server-side data bug.

Fixed by changing `ops` from an array of already-started promises to an array of thunks (`Array<() => Promise<void>>`), run one at a time in a `for` loop with `await` inside try/catch (preserving the original "attempt every op even if one fails, count failures" behavior, just replacing `Promise.allSettled` with a manual sequential loop) — awaiting each Supabase call guarantees its transaction (and the trigger's recompute) has fully committed before the next one starts, so every recompute after the first sees all prior changes and the final `total_kcal` ends up correct regardless of how many ingredient lines changed in one save.

**Not fully closed by this fix:** this only serializes a single save's own batch of ingredient-line ops against each other. Two different users editing the same shared group recipe's ingredients at genuinely the same moment from separate sessions could still hit the identical race — that's an inherent property of "recompute the whole aggregate from scratch on every row change" as specified in schema.md, not something a client-side ordering fix can close; a real fix would need either row-level locking or an atomic increment/decrement in the trigger instead of a full recompute, which is a schema-level change out of scope for this fix.

**Why:** Real, reproducible data-correctness bug (not a live-project-only concern) — reproducible by adding 2+ ingredients to a recipe in one save and comparing the list card to the detail screen, exactly as reported.

**Not yet verified:** Same database-connection restriction as above — not exercised against a live Supabase project. Manual follow-up: create a recipe, add two or more ingredients in a single edit session, click Save once, then confirm the `/recipes` list card's total kcal matches the detail screen's "total kcal" stat tile (and that the Table Editor's `recipes.total_kcal` for that row equals the correct sum, not just one ingredient's contribution).

**Deviation (requested directly — item labels and amount-eaten validation in the log flow):** `AddLogEntryDialog`'s ingredient/recipe picker (`getOptionLabel`) and `LogIngredientStep`/`LogRecipeStep`'s own header line both showed just the item's bare `name`, with no way to tell two similarly-named items apart or confirm which one you'd selected before entering an amount. Added `formatIngredientLabel`/`formatRecipeLabel` (`src/features/logging/formatItemLabel.ts`) — `"<quantity> <unit> <name>"` for ingredients (e.g. "1 sachet Milo") and `"<weight_g>g <name>"` for recipes (e.g. "100g Fried Chicken", no space before `g`, matching the requested format exactly) — and used them in both the Autocomplete option labels and the two step components' header `Typography`.

`LogIngredientStep`'s "Quantity eaten" and `LogRecipeStep`'s "Amount eaten" fields already had HTML `min: 0` (and `canLog` already gated the submit button on `> 0`, so a negative amount could never actually be logged) — requested directly to also surface this as an explicit, visible validation rather than a silently-disabled button: both fields now show `error`/`helperText` ("Enter a positive amount.") once the typed value is negative. `LogRecipeStep`'s field additionally can't exceed the recipe's own `weight_g` — added `exceedsWeight` alongside `isNegative`, folded into both `canLog` and the same error/helperText treatment ("Can't exceed this recipe's total weight (Xg)."), plus an HTML `max` matching the field's `min`. Ingredients intentionally got no equivalent upper bound — only "prevent negative numbers" was requested for that field, and unlike a recipe's `weight_g` (a hard physical total the eaten amount is a fraction of), an ingredient's own `quantity` is just "however much you have on hand," not a ceiling on how much of it you could plausibly eat in one entry.

**Why:** Requested directly.

**Deviation (requested directly — log entry unit):** `LogEntryCard` needed to show the logged amount next to the title (e.g. "1 sachet", "100 g"), but `log_entries` had no unit column at all — `snapshot_quantity` was a bare number with nothing recording what it was a quantity _of_. schema.md's own "snapshotted at creation time, never reads live ingredient/recipe data" rule already applied to `snapshot_name`/`snapshot_kcal`/`snapshot_quantity`; the unit is the same category of "what this entry represents" data and should have been snapshotted from the start (a gap this ticket's own earlier deviation entry — "servings -> weight" — already flagged in passing: "this isn't a new gap, just the same existing one now also applying to recipe logging"). Added `log_entries.snapshot_unit ingredient_unit not null default 'g'` (`supabase/migrations/20260830000000_log_entries_snapshot_unit.sql`) — reusing the existing enum rather than a new type, since every logging path's unit (an ingredient's own unit, or `'g'` for a recipe) is already a member of it. The default only backfills any pre-existing row from before this column existed; `LogIngredientStep`/`LogRecipeStep` (the only two paths that ever create a `log_entries` row) always supply their own value explicitly (`ingredient.unit` / `'g'`).

`LogEntry`/`LogEntryInput` both gained `snapshot_unit: IngredientUnit` (required, not optional — every creation path always has one). `LogEntrySnapshotInput` (the shape `updateLogEntry`, Ticket 8's fast-follow, accepts) deliberately did **not** gain it — same reasoning as `source_ingredient_id`/`source_recipe_id`/`logged_at` already being excluded from that type: the unit is fixed by how the entry was originally logged, not something editing the entry's numbers should be able to change. `EditLogEntryDialog`'s "Quantity" field shows `entry.snapshot_unit` as a read-only end-adornment (mirroring `LogIngredientStep`'s own pattern) so editing the number doesn't lose track of what it's a number of.

`LogEntryCard` shows `{snapshot_quantity} {snapshot_unit}` beside (not below) the title, in a flex row with the title itself allowed to truncate first — the quantity+unit clause is skipped entirely when `snapshot_quantity` is `null` (the type allows it, even though neither current logging path ever actually produces that case).

**Why:** Requested directly.

**Not yet verified:** Same database-connection restriction as above (org policy: must not connect to databases) — the new migration hasn't been pushed or exercised against a live project. Manual follow-up: push the migration; log an ingredient (e.g. a "Milo" sachet ingredient) and confirm its card shows "1 sachet" beside the name matching what was entered; log a recipe and confirm its card shows the grams eaten (e.g. "100 g") beside the name; open Edit on either entry and confirm the Quantity field shows the correct unit as a read-only suffix; edit just the kcal value and save, then confirm the unit/quantity display on the card is unchanged.

**Deviation (requested directly — "logged by" name in group context):** `/groups/:groupId/log` shows a group's shared entries (filtered by `group_id`, per schema.md), but `LogEntryCard` never showed _who_ actually logged each one (`entry.logged_by`) — only the entry's own name/quantity/time. Added an optional `loggerName` prop to `LogEntryCard`, appended to its existing time subtitle ("· Logged by X"), populated by `DailyLog` via the same `useProfileNames` lookup already used for ingredient/recipe creator attribution (Ticket 12) — `groupId ? entries.map(e => e.logged_by) : []`, so it's a no-op (and no extra profiles fetch) in personal context. `AllTimeLog` (`/logs`, cross-context per routes.md) is untouched — it never has a `groupId` to begin with, so `LogEntryCard`'s `loggerName` prop is simply left unset there, same as it always was.

**Why:** Requested directly.

**Deviation (requested directly — `/log` becomes cross-context, context switcher replaced by a group-log picker):** `/log` (personal, no `:groupId`) previously showed only entries with `group_id === null`, same personal-xor-one-group split as Pantry/Recipes. Requested directly: `/log` should show everything the caller has logged today, personal and every group combined — mirroring `/logs`' already-cross-context "all-time" semantics (routes.md) applied to "today" instead of all-time. `fetchTodayLogEntries` (`src/features/logging/api.ts`) dropped its `groupId === null` branch's `group_id === null` filter, so that branch is now just `where('logged_by').equals(userId)` — the same query shape `fetchAllLogEntries` already uses, just additionally filtered to today. The `groupId !== null` branch (a specific group's own shared log, `/groups/:groupId/log`) is unchanged.

`ContextSwitcher` (the pill chip added by this ticket for Pantry/Recipes/Log) is removed from the Log screen specifically — requested directly, replacing it with a link. `LogPage.tsx` no longer renders it; `ContextSwitcher`'s own `tab` prop type narrowed from `'pantry' | 'recipes' | 'log'` to `'pantry' | 'recipes'` since nothing passes `'log'` anymore (Pantry/Recipes keep the switcher unchanged). In its place, `DailyLog.tsx` gained a second button beside the existing "View all-time history" one that flips based on context: on `/log` (personal) it reads "View group logs" and navigates to a new `/logs/groups` route (`GroupLogsPage` → `GroupLogPicker`, a plain list of the caller's groups via the same `fetchMyGroups` call `ContextSwitcher` itself used), picking a group navigating to `/groups/:groupId/log`; on `/groups/:groupId/log` itself it instead reads "View personal logs" and navigates straight back to `/log` (requested directly — "view group logs" doesn't make sense as an option while already viewing one group's log). `/groups/:groupId/log` itself also no longer shows the switcher chip; `LogPage` instead shows the group's name as the header title with a back arrow to `/log` (a small `fetchMyGroups` lookup to resolve the id to a name, same pattern `ContextSwitcher` used).

**Deviation (requested directly — log entry dialog becomes cross-context, with a subtitle showing where each item lives):** `AddLogEntryDialog`'s ingredient/recipe picker previously took a `groupId` prop and only listed items from that one context (personal or the specific group currently being viewed), matching Pantry/Recipes' own scoping. Requested directly: the dialog should let the user pick _any_ ingredient or recipe they can see — personal or any group they're in — regardless of which screen ("/log" or a specific group's log) it was opened from, with each option's subtitle showing where it lives ("Personal" or the group's name), and the resulting log entry should land wherever the selected item itself belongs (its own `group_id`), not wherever the dialog happened to be opened from.

Added `fetchAllIngredients(userId, groupIds)` / `fetchAllRecipes(userId, groupIds)` (`src/features/pantry/api.ts`, `src/features/recipes/api.ts`) — the caller's personal items unioned with every item whose `group_id` is in the given list, via Dexie's existing `group_id` index (`.where('group_id').anyOf(groupIds)`, no schema/version bump needed since that index has existed since Ticket 10), sorted by name. These are new functions alongside the existing `fetchIngredients`/`fetchRecipes` (still used, unchanged, by Pantry/Recipes' own strict personal-xor-one-group screens) rather than a replacement, since those screens' scoping is unaffected by this change.

`AddLogEntryDialog` dropped its `groupId` prop entirely. It now fetches the caller's groups (`fetchMyGroups`) to build the `groupIds` list and resolve a `groupLabel(groupId)` display name, passes that list to the two functions above, and renders each Autocomplete option with a two-line `renderOption` (name/label on top, `groupLabel` — "Personal" or the group's name — as a subtitle underneath) instead of the previous plain single-line option. `handleLog` now takes the selected item's own `group_id` and passes it straight to `createLogEntry`, rather than relying on a page-level `groupId` — so logging a personal snack while viewing a specific group's log correctly lands on the personal log, and vice versa. `DailyLog.tsx` was updated to stop passing `groupId` to the dialog. `LogIngredientStep`/`LogRecipeStep` needed no changes — they already only take an `onLog` callback, unaware of which context it ultimately writes to.

Because the item picker can now surface any group's ingredients/recipes regardless of which one is "active," `useSyncEngine` (Ticket 10/12) needed to actually have that data available in Dexie to find it — its prior scoping (personal + whichever single group was currently active, per this ticket's own "Deviation" above) would silently omit every other group's items from the picker until that group happened to be visited in the same session. Changed `runPull` to fetch the caller's full group membership list (`fetchMyGroups`) on every pull cycle and pull personal plus **every** membership's scope (`Promise.allSettled`, one scope's failure doesn't block the others) — superseding the "just the active group" scoping entirely. Since `useAppStore`'s `activeGroupId` field/`setActiveGroup` setter existed solely to feed that now-removed scoping decision (confirmed by grep — no other reader existed), both were removed from the store; `useSyncedActiveGroupId(routeGroupId)` (still called by `PantryPage`/`RecipesPage`/`LogPage`/the two detail pages, all of which use its _return value_, not the store write) was simplified to a pure `routeGroupId ?? null` derivation with no store side effect at all.

**Deviation (requested directly — group's own all-time history):** `DailyLog`'s first nav button ("View all-time history" → `/logs`) always went to the cross-context all-time view, even from `/groups/:groupId/log`, which had no analogue of `/logs` scoped to just that group. Requested directly, mirroring `/logs`' relationship to `/log`: on `/groups/:groupId/log` this button now reads "View \<group name\>'s all-time history" (requested directly — the button initially just said "View group's all-time history," generic rather than naming the actual group) and navigates to a new `/groups/:groupId/logs` route instead. The group name isn't refetched separately for this button — `LogPage` already resolves it once (for its own `AppHeader` title) and now passes it down as a new optional `groupName` prop on `DailyLog`, falling back to the literal word "group" only in the brief window before that lookup resolves. `LogsPage`/`AllTimeLog` (`src/routes/LogsPage.tsx`, `src/features/logging/AllTimeLog.tsx`) both gained the same `:groupId`-param-driven shape `LogPage`/`DailyLog` already have — `AllTimeLog` takes a `groupId: string | null` prop and, when set, reads via a new `fetchAllGroupLogEntries(groupId)` (`src/features/logging/api.ts`, same `where('group_id').equals(groupId)` shape — no `logged_by` filter — as the group branch of `fetchTodayLogEntries`, i.e. every entry logged into the group by any member, not just the caller's own) instead of the cross-context `fetchAllLogEntries(userId)`. It also shows each entry's logger name in group context (`loggerName`, via the same `useProfileNames` lookup `DailyLog` already uses) — not explicitly requested, but the group's daily log already shows this, and omitting it here would make an all-time view of the same shared data (now with multiple contributors) less informative than the daily one for no reason. `LogsPage`'s `AppHeader` title/back-target flips the same way `LogPage`'s already does (group name + back to `/groups/:groupId/log`, vs. "All-time log" + back to `/log`).

**Deviation (requested directly — hide group-switching UI for a user in no groups):** `ContextSwitcher` (Pantry/Recipes) and `DailyLog`'s "View group logs" button (Log's personal screen) both rendered unconditionally, even for a user who isn't a member of any group — offering a picker whose only real destination is the context already being viewed. Requested directly: `ContextSwitcher` now returns `null` once its own `fetchMyGroups` fetch resolves to zero groups (only when `activeGroupId` is `null` — if a group route is somehow still active despite an empty/not-yet-loaded membership list, the chip stays rendered so there's still a way back to Personal). `LogPage` now fetches `fetchMyGroups` unconditionally (previously it only fetched when `groupId` was set, to resolve the group's own name) and derives a `hasGroups` boolean passed down to `DailyLog`, which renders "View group logs" only when `hasGroups` is true; `hasGroups` is treated as `false` during the brief window before that fetch resolves, so the button doesn't flash in and then disappear for a no-groups user (the tradeoff being a real groups-having user sees it pop in a beat late instead).

**Deviation (requested directly — a title on the group-log picker):** `GroupLogPicker` (`/logs/groups`) originally rendered straight into the loading/empty/list states with no heading of its own inside the page body — just the `AppHeader`'s "Group logs" AppBar title above it. Requested directly: added a body-level "Your groups" title plus a one-line "Pick a group to see its own shared log." subtitle above the list (worded distinctly from the `AppHeader` title above it, rather than repeating "Group logs" verbatim).

**Deviation (requested directly — only the entry's own logger can edit/delete it in group context):** `DailyLog`/`AllTimeLog` unconditionally passed `onClick={() => setEditingEntry(entry)}` to every `LogEntryCard`, so any group member could open `EditLogEntryDialog` on any entry in a group's shared log, not just their own. Requested directly: both now pass `onClick={entry.logged_by === userId ? () => setEditingEntry(entry) : undefined}` instead — `LogEntryCard` already renders `cursor: onClick ? 'pointer' : undefined`, so an entry that isn't the viewer's own also stops looking clickable, not just stops responding. In personal context (`/log`, `/logs`) this is a no-op — every entry shown there already has `logged_by === userId` by construction (`fetchTodayLogEntries`/`fetchAllLogEntries` both query `where('logged_by').equals(userId)`), so the check never blocks anything there; it only has an effect in a group's own log/all-time views (`/groups/:groupId/log`, `/groups/:groupId/logs`), where entries from every member are shown.

**Resolved (was: "Not yet verified (RLS gap)"):** This was a client-side/UI restriction only — `log_entries`' `update`/`delete` RLS policies (per schema.md's "copy the three-policy shape from `ingredients`" instruction, applied in Tickets 2/8) allowed _any_ member of the owning group to update or delete _any_ row in that group, not just their own. A user could call `updateLogEntry`/`deleteLogEntry` directly (e.g. via the browser console or a modified client) against another member's entry and have Supabase accept it — the UI-only check merely stopped the normal path from offering that. Fixed by the issue #34 audit (see the "Fix — Repo audit findings" section below): `supabase/migrations/20260831000000_log_entries_rls_and_updated_at.sql` tightens both policies to `logged_by = auth.uid()`, matching what schema.md now documents. The select policy is unchanged — group members still need to see each other's shared log.

**Why:** Requested directly, in this order: `/log` cross-context first, then the context-switcher replacement, then "View personal logs," then the group's own all-time history (and naming the group in that button's label), then hiding the group-switching UI entirely for a no-groups user, then the group-log picker's title, then only the entry's own logger being able to edit/delete it in group context, then the log entry dialog becoming cross-context (which is what made the `useSyncEngine` scoping change necessary for correctness, not a separately-requested item).

**Not yet verified:** Same database-connection restriction as above (org policy: must not connect to databases) — not exercised against a live Supabase project, and no browser automation tooling available in this environment to drive the real auth-gated app. Verification here was `tsc -b`, `oxlint`, and `vite build`, all clean (one new instance of the same pre-existing `set-state-in-effect` lint warning already present elsewhere in the codebase, in `LogPage`'s group-name lookup). Manual follow-up needed — see `supabase/README.md` under "Ticket 12 manual verification," new steps for: `/log` showing today's entries from every context combined; "View group logs" listing every membership and navigating correctly; the log entry dialog's option subtitles and cross-context routing of the created entry; and confirming a group's pantry/recipe items show up in the dialog even without having visited that group's own screens first in the current session (exercising the broadened sync-engine pull).

---

## Fix — Repo audit findings (issue #34)

**Deviation:** Three mechanical hardening changes with no documented spec to deviate from, added directly from the audit's findings rather than requested feature work:

- `search_path` pinned (`set search_path = public`) on all seven `security definer` functions (`handle_new_user`, `is_group_member`, `shares_group_with`, `create_group`, `accept_group_invite`, `preview_group_invite`, `complete_onboarding`) — flagged by Postgres/Supabase's own linter for this function type. `check_ingredient_usage` is not `security definer` and was left alone.
- Group invite codes (`group_invites.invite_code`) switched from `substr(md5(random()::text), 1, 8)` (~32 bits, non-cryptographic RNG) to `encode(extensions.gen_random_bytes(6), 'hex')` (48 bits, `pgcrypto`'s CSPRNG — schema-qualified because Supabase installs pgcrypto into the `extensions` schema, not `public`, unlike `gen_random_uuid()` which is Postgres core). `schema.md` updated to match.
- `recalculate_recipe_kcal` now takes an explicit `for update` lock on the target recipe row before recomputing `total_kcal`, closing a race where two near-simultaneous edits to the same recipe's ingredients could each read the pre-both-changes sum and the second commit would silently discard the first's contribution.

See `supabase/migrations/20260901000000_security_hardening.sql` and `20260902000000_recalc_recipe_kcal_locking.sql`.

**Why:** Requested directly (address the full issue #34 checklist in one PR).

**Deviation (scope decision, not a spec deviation):** The issue also suggested a `group_members`-derived Dexie mirror so `fetchMyGroups` (called live from ~7 places) works offline. Skipped for this PR — `RequireGroupMember` (the new route guard added by this same PR, see `src/routes/RequireGroupMember.tsx`) deliberately fails open when its own `fetchMyGroups` call errors (e.g. offline), which removes the one scenario that would otherwise justify a cache. Revisit only if `fetchMyGroups` latency/offline-availability becomes an observed real problem.

**Why:** Calibrated for "solo/small personal project" scale, per the issue's own framing — a new Dexie table, pull scope, and invalidation-on-leave/kick logic is real new surface for a Medium-severity, not-yet-observed problem.

**Not yet verified:** Same restrictions as every other entry above (org policy: must not connect to databases; no browser automation available). Verification here is `tsc -b`, `oxlint`, and `vite build` only. The three new migrations (`20260831000000_log_entries_rls_and_updated_at.sql`, `20260901000000_security_hardening.sql`, `20260902000000_recalc_recipe_kcal_locking.sql`) have not been pushed or run against any Supabase project — see `supabase/README.md` for the manual verification steps this PR adds. Automated tests for `src/sync/outbox.ts`'s new discard/ordering behavior, suggested by the issue, were explicitly deferred by the user to a later pass once the MVP is complete — not added here.

Also out of the PR diff entirely: the issue's `.env.prod` finding (a stray `/auth/v1/callback` suffix on `VITE_SUPABASE_URL` breaking every prod-build request) is in a gitignored file and can't be fixed via a tracked change — it needs a manual local edit outside this PR.

---

## Ticket 13 — Group settings screen

**Deviation:** None, really — noting the absence for the record. `groups`' RLS already has owner-only `update`/`delete` policies (schema.md), `group_members`' `delete` is already owner-restricted (docs/pending-deviations.md, Ticket 2), and deleting a group already cascades to `group_members`/`ingredients`/`recipes`/`log_entries` via the existing `on delete cascade` foreign keys — so rename/edit-description/delete-group are plain client `update`/`delete` calls against `groups` (`updateGroup`/`deleteGroup`, `src/features/groups/api.ts`), and removing a member is a plain `delete` against `group_members` (`removeGroupMember`), none needing a new RPC. `routes.md` already specifies `/groups/:groupId/settings` verbatim (unlike Tickets 6/7/8/11, which had to invent undocumented creation/settings routes ad hoc) — this ticket is simply the first to build the screen that path was always meant to host.

**Deviation:** Added a `RequireGroupOwner` guard (`src/routes/RequireGroupOwner.tsx`), nested inside the existing `RequireGroupMember` on `/groups/:groupId/settings` specifically (`src/App.tsx`) — `RequireGroupMember` only confirms membership, not role, and the ticket's first acceptance criterion ("non-owners cannot access this screen") needs the stricter check. Mirrors `RequireGroupMember`'s own shape exactly: a live `fetchMyGroups` check (not Dexie — groups aren't mirrored there, per Ticket 11), fails open on error for the same reason `RequireGroupMember` does (RLS's owner-only `update`/`delete` policies on `groups`/`group_members` are the real security boundary; this guard only redirects away from a URL a member shouldn't be looking at). A non-owner member hitting the URL is redirected to that group's own `/pantry`, not `/groups` — they're still a valid member of the group, just not its owner, so bouncing them back into the group's regular screens reads better than ejecting them out to the group list entirely.

**Deviation:** The only discoverable entry point to `/groups/:groupId/settings` is a new gear icon on `GroupCard` (`src/features/groups/GroupCard.tsx`), shown only for `role === 'owner'` — same "hidden for non-owners" requirement as the route guard above, and the same "would otherwise be reachable only by typing the URL" pattern already used for `/groups`, `/sync-status`, and invite generation (docs/pending-deviations.md, Tickets 9/11).

**Deviation:** `GroupForm` (`src/features/groups/GroupForm.tsx`, previously used only by `CreateGroupDialog`) gained an optional `initialValues` prop so the settings screen's rename/description editing reuses it directly rather than duplicating its two fields — same "one form component, `initialValues` makes it double as an editor" pattern `IngredientForm`/`IngredientDetail` already established. Its pending-submit label also changed from the create-only `'Creating…'` to `'Saving…'`, matching `IngredientForm`'s own generic wording (accurate for both create and edit, unlike a hardcoded create-specific verb), and its `submitting` reset moved from the `catch` block into a `finally` — `CreateGroupDialog` never needed this fix because it unmounts the form on success, but `GroupSettings` keeps it mounted after a successful save, and without the `finally` the button would stay stuck on "Saving…" forever (the exact bug shape fixed for `RecipeDetail` in Ticket 7's own deviation log).

**Deviation:** Group membership rows aren't resolved to display names anywhere in `schema.md`/`rpcs.md` — reused `fetchProfileNames`/`useProfileNames` (`src/features/profiles/`, added in Ticket 12 for ingredient/recipe creator attribution) to show each member's name in the settings screen's member list, the same RLS-safe "caller is necessarily a member of any group whose membership rows they're looking at" reasoning Ticket 12 already established.

**Why:** All of the above are the minimum needed to satisfy this ticket's acceptance criteria (owner-only access, rename, edit description, remove a member, delete the group with correct cascade) given a route this is the first ticket to build, reusing existing RLS/schema/component patterns rather than introducing new ones. Ownership transfer was left out entirely, per the ticket's own explicit "out of scope" line.

**Not yet verified:** Same database-connection restriction as every prior ticket (org policy: must not connect to databases) — not exercised against a live Supabase project, and no browser automation tooling available in this environment to drive the real auth-gated app. Verification here was `tsc -b`, `oxlint`, and `vite build`, all clean (three new instances of the pre-existing `set-state-in-effect` lint warning already present elsewhere in the codebase, in `RequireGroupOwner` and `GroupSettings`' two data-loading effects). Manual follow-up needed — see `supabase/README.md` under "Ticket 13 manual verification."

---

## Ticket 14 — Ingredient and recipe copy/fork

**Deviation (security fix):** `rpcs.md`'s own text for `copy_ingredient`/`copy_recipe` claims "RLS on the underlying insert enforces the caller must be a member of the target group" — that's incorrect for a `security definer` function, which runs as the function owner and bypasses RLS entirely on the tables it touches. Deployed as originally documented (no membership check), any authenticated caller could pass an arbitrary `p_target_group_id` and copy an ingredient or recipe straight into a group they don't belong to. `supabase/migrations/20260903000000_copy_ingredient_recipe_rpcs.sql` adds an explicit check to both functions — `is_group_member` (the same `security definer` helper already used by `group_members`'/`profiles`' RLS policies, from the Ticket 11 RLS-recursion fix) — raising an exception when `p_target_group_id` is non-null and the caller isn't a member. `find_ingredient_match` needed no such fix: it was never `security definer` in the spec, so its plain `select` is already correctly gated by the existing "read own or group ingredients" RLS policy — a caller who isn't a member of the target group just gets zero rows back, which is what makes it silently fall through to "no match, create a fresh copy" for that ingredient rather than ever exposing another group's data.

**Deviation:** `copy_recipe`'s `rpcs.md` example still selects/inserts a `servings` column. `recipes.servings` was renamed to `weight_g` in Ticket 12's own follow-up ("servings -> weight"), whose deviation log entry explicitly flagged this exact gap ("whoever implements `copy_recipe` will need to update that example to `weight_g` first"). The deployed function uses `weight_g`.

**Why (both above):** Necessary for the RPCs to be both secure and correct against the schema as it actually exists today; `rpcs.md`/`schema.md` are left unedited per this ticket's standing instruction.

**Deviation:** `routes.md`/`design-system.md` don't describe a "Copy to…" entry point anywhere. Added as a plain outlined button on `IngredientDetail`/`RecipeDetail` (`src/features/pantry/IngredientDetail.tsx`, `src/features/recipes/RecipeDetail.tsx`), alongside the existing "Delete" button — matching "detail one tap away" (design-system.md) and the precedent of putting item-level destructive/secondary actions on the detail screen rather than the list card. `RecipeDetail`'s "Copy to…" button is disabled while the screen has unsaved changes (`isDirty`) — copying always reads the last-persisted recipe/ingredients (`savedRecipe`/`savedIngredients`, refetched via `fetchRecipeIngredients`), since `recipe_ingredients` isn't staged client-side the way the recipe's own fields are (Ticket 10's "recipe_ingredients... require connectivity" split); copying while dirty would silently copy something other than what's on screen.

**Deviation:** The "Copy to…" target list (`src/features/copy/useCopyTargets.ts`) is Personal plus every group the caller belongs to (via `fetchMyGroups`, same live-Supabase pattern as `ContextSwitcher`/Ticket 11 — groups aren't mirrored in Dexie), **excluding** whichever context the item is already in. Not specified by the ticket, which only lists three supported directions (group→group, group→personal, personal→group) — copying into the exact same context isn't one of them, so it's left off the list entirely rather than allowed and left unexplained.

**Deviation:** `copy_ingredient`/`copy_recipe` write straight to Supabase (like every other RPC-backed write in this codebase — `create_group`, `accept_group_invite`, etc.), bypassing Dexie and the outbox entirely. Without further action, a copy into a group other than the one currently being viewed wouldn't show up in that group's pantry/recipes list until the next periodic pull (up to 60s, per Ticket 10's `useSyncEngine` interval) or a reconnect. Both `copyIngredient`/`copyRecipe` (`src/features/copy/api.ts`) call `pullScope` (`src/sync/pull.ts`, already exported and used internally by `useSyncEngine` — no new sync logic added) for the target scope immediately after a successful copy, so the copy appears in the target context right away.

**Deviation:** The recipe-copy confirmation step always prompts on a genuine match (same name AND same unit — `find_ingredient_match` returning any row), not conditionally based on whether `kcal_per_unit` also matches. `rpcs.md`'s "Confirmation UI contract" paragraph reads as if the prompt itself might be conditional on `kcal_per_unit` differing ("If `kcal_per_unit` differs, present two options..."), but this ticket's own acceptance criteria are unconditional ("Copying a recipe whose ingredient has an exact name+unit match... prompts the user"). Read `rpcs.md`'s `kcal_per_unit` comparison as governing what to _display_ in that prompt (both values shown side by side, a warning banner surfaced only when they actually differ — `ConflictStep` in `src/features/recipes/CopyRecipeDialog.tsx`), not whether to show it at all.

**Deviation:** The recipe-copy flow resolves ingredient conflicts one at a time in a fixed sequence (a `Conflict[]` queue, `CopyRecipeDialog`) rather than one combined screen — not specified either way; chosen to keep each decision (and its kcal comparison) legible on a mobile-width screen rather than cramming an arbitrary number of conflicts into one dialog. Nothing is written server-side (no partial `copy_ingredient`/`copy_recipe` calls) until every conflict in the queue is answered and the single `copy_recipe` RPC call fires — cancelling or hitting an error at any point up to and including that final call leaves no partial copy behind, so there's no cleanup path to build.

**Deviation (fixes from the code-reviewer sub-agent pass):** The first draft of the migration only checked `p_target_group_id` membership — both `copy_ingredient` and `copy_recipe` are `security definer`, so they also bypass RLS on the _source_ read (`where id = p_ingredient_id` / `p_recipe_id`), not just the target-context insert. As drafted, any authenticated caller could pass an arbitrary source id belonging to another user or group entirely and have it copied into their own context — a cross-tenant data-exfiltration path, not just a target-side authorization gap. Both functions now also check source access (owns it personally, or `is_group_member` of its group) before doing anything else, raising `'Ingredient/Recipe not found or not accessible'` otherwise.

A second, independent gap in the same draft: `copy_recipe` trusted the client-supplied `use_existing_id` in `p_ingredient_resolutions` with no server-side re-verification that it actually resolves to a visible, genuinely-matching (same name+unit) ingredient in the target context — `find_ingredient_match`'s whole purpose was being re-derived client-side and never re-checked server-side. A modified client could pass an arbitrary ingredient id as `use_existing_id`; because `recalculate_recipe_kcal` (schema.md) then runs with RLS bypassed too, that ingredient's `kcal` would leak into the new recipe's `total_kcal` as an observable side channel, and the client's own `fetchRecipeIngredients` join (`src/features/recipes/api.ts`) would throw on a `null` embed if RLS ever did block it on a subsequent read. `copy_recipe` now re-verifies `use_existing_id` server-side with the same predicate `find_ingredient_match` uses, falling back to a fresh `copy_ingredient` call (exactly as if no match had been supplied) if it doesn't check out — never raising, since a stale/bad match here isn't a caller error, just something to route around silently.

Also fixed, lower severity: both functions now raise explicitly (rather than silently returning a null id) if the source `insert ... select` matches zero rows, so a bad/deleted id slipping past the access check can't read as a successful no-op copy to the client; `find_ingredient_match`'s `kcal / quantity` now guards a zero `quantity` (no `check` constraint exists on `ingredients.quantity`) so one bad row doesn't throw for every unrelated caller who happens to look up a match against it; `CopyRecipeDialog`'s `handleConflictAnswer` gained a `useRef` guard (checked synchronously, not via `useState`, since a rapid double-tap can fire both click events before React commits the first's state update) against firing `copy_recipe` twice on a fast double-tap of the last conflict's buttons, plus a `disabled` prop on those buttons as a secondary, render-timed defense; and `copyIngredient`/`copyRecipe` (`src/features/copy/api.ts`) now swallow a `pullScope` failure that happens _after_ the RPC has already committed, rather than surfacing it as a failed copy — the RPC succeeding is what matters for correctness, and a failed post-copy pull just means the target context picks it up on the next periodic sync instead of immediately.

**Why:** All fixed after the `code-reviewer` sub-agent's pass flagged them — the two access-control gaps (source-side authorization, unverified `use_existing_id`) as critical/security findings, the rest as medium/low correctness and UX gaps. Same reasoning as every prior security-relevant fix in this doc (e.g. issue #34's audit): `security definer` means RLS isn't running at all inside the function, so every externally-supplied id needs its own explicit check — it's not something "RLS already handles."

**Not yet verified:** Same database-connection restriction as every prior ticket (org policy: must not connect to databases) — the new migration hasn't been pushed or exercised against a live Supabase project, and no browser automation tooling available in this environment to drive the real auth-gated app. Verification here was `tsc -b`, `oxlint`, and `vite build`, all clean (one new instance of the pre-existing `set-state-in-effect` lint warning already present elsewhere in the codebase, in `useCopyTargets`). Manual follow-up needed: push the migration; copy an ingredient personal→group, group→personal, and group→group and confirm each lands correctly and shows up immediately in the target's pantry; copy a recipe whose ingredients have no match in the target and confirm fresh copies are created silently with no prompt; copy a recipe with a genuine name+unit match and confirm both "use existing" (links to the existing ingredient, no new row) and "add as new" (creates a second, independent ingredient) work; confirm a same-name-different-unit ingredient is never offered as a selectable match and is silently copied fresh instead; attempt (e.g. via a modified client call) to copy into a group the caller isn't a member of and confirm the RPC raises rather than silently succeeding.

---

## Ticket 16 — Navigation animations

**Deviation:** Built the four-tab bottom navigation bar (`src/components/BottomNav.tsx`) itself, not just the animation logic the issue text describes. `design-system.md`'s bottom nav was explicitly deferred across Tickets 6 and 11 ("no nav bar exists yet regardless — Ticket 16", "expected to land incrementally... fully wired by Ticket 16"), and there's no literal "switching bottom tabs" to animate without one. Progress (Ticket 18) has no real screen yet either, so `src/routes/ProgressPage.tsx` is a placeholder ("Progress tracking is coming soon") purely so the fourth tab leads somewhere instead of 404ing — Ticket 18 owns the real weight/BMI content. BottomNav only renders on the four tab-root screens (personal or group-scoped Pantry/Recipes/Log, plus Progress); detail screens, `/groups`, `/sync-status`, and group settings hide it, matching the native "root tab bar, pushed screens don't have one" pattern routes.md implies but doesn't spell out.

**Deviation:** Classification is a 3-way `push` / `pop` / `tab` split (`src/routes/navigationTransition.ts`), not the single `isPush` boolean in `frontend-architecture.md`'s sample. That sample's `initial`/`exit` both key off one flag, which can't produce the acceptance criteria's two _different_ directions (push enters from the right; pop's exit is to the right, i.e. it must enter from the left) — a single boolean only distinguishes "sliding" from "not sliding," not which way. Classification itself is structural (is the next path a descendant of the previous one → push; the reverse → pop; both resolve to one of the four tab roots → tab; otherwise → push), not react-router's `useNavigationType()` — every "back" affordance in this app (`AppHeader`'s `onBack`, `IngredientDetailPage`/`RecipeDetailPage`/etc.) calls `navigate(path)` with a hardcoded destination, not `navigate(-1)`, so the browser history stack would report every one of those as a `PUSH`, not a `POP`; only comparing the actual pathnames gives the right answer regardless of how the navigation was triggered.

**Deviation (superseded — see the "janky" fix entry below):** The first implementation kept `AnimatePresence` but passed the transition variant through its `custom` prop (read via function-form `variants`) rather than baking it into the `motion.div`'s own `initial`/`exit` props directly as the doc sample does, specifically so a push-then-pop round trip (list → detail → back) would exit in the correct opposite directions instead of both using the direction the detail screen happened to enter with. That mechanism turned out not to work reliably in practice — see below — and `AnimatedAppShell` no longer uses `AnimatePresence` at all.

**Deviation:** FABs (`PantryList`, `RecipeList`, `DailyLog`, `GroupList`) are now wrapped in a new `FloatingPortal` (`src/components/FloatingPortal.tsx`), rendering into a `#floating-root` div added to `index.html` as a sibling of `#root`. This is the actual mechanism behind design-system.md's "FAB positioned relative to the screen root" bug this ticket calls out: Framer Motion animates `x` via a CSS `transform`, and per the CSS spec any transformed ancestor becomes the containing block for its `position: fixed` descendants — so once a page's content sits inside `AnimatedAppShell`'s animated wrapper, every FAB on it would resolve its `fixed` positioning against that wrapper instead of the viewport, permanently (not just mid-transition, since Framer Motion keeps an inline `transform` applied even at rest). Portalling out to a node that is never a descendant of the animated wrapper sidesteps the problem entirely rather than special-casing each page's layout. Pantry/Recipes/Log's FABs also moved from `bottom: 24` to `bottom: 80` to clear the new BottomNav; Groups' FAB is unchanged (`/groups` isn't a tab root, so no nav bar sits under it there).

**Deviation (fix from the code-reviewer sub-agent pass):** `classifyTransition` originally only classified `pop` for a literal parent/child pathname pair, so a "back" affordance that returns to a bottom-tab root without being that root's literal URL parent — `SyncStatusPage`'s back button (`/sync-status` → `/pantry`), `LogsPage`'s (`/logs` → `/log`), `GroupLogsPage`'s (`/logs/groups` → `/log`), `GroupSettingsPage`'s (`/groups/:id/settings` → `/groups/:id/pantry`) — fell through to the `'push'` default and slid in from the right instead of exiting to the right, violating the acceptance criteria. Fixed with one more rule: destination is a bottom-tab root and the origin isn't → `pop`. Safe as a general rule (not a per-route special case) because `BottomNav` only renders on tab-root screens, so the only way to arrive at one from a non-tab screen is that screen's own explicit back action, never a fresh forward push.

**Deviation (fix from manual testing — "the animation is janky"):** Replaced the `AnimatePresence`-based implementation entirely with a hand-rolled overlapping transition (still in `src/routes/AnimatedAppShell.tsx`, no other files affected). Two independent problems were found by instrumenting the actual DOM `transform`/`opacity` values frame-by-frame (a rAF sampler in a throwaway Playwright harness), not visible from a screenshot alone:

1. `mode="wait"` sequences exit-then-enter with nothing on screen moving together — the outgoing screen and incoming screen never share the stage, so a "slide" reads as a stutter (old vanishes, pause, new appears) rather than one continuous motion. Native stack navigation animates both screens simultaneously, one sliding fully off as the other slides fully on.
2. Worse: `AnimatePresence`'s documented mechanism for updating an _already-unmounting_ child's exit animation via its own `custom` prop did not reliably land in time in practice. Sampling the real transform values showed the outgoing screen's exit target evaluating to a no-op (using the direction it _entered_ with, not the one removing it — invisible, since the two happened to cancel out in the reproduced case), while the incoming screen played an unwanted two-phase animation (sliding to -48px first, then jumping to +48px and correcting back to 0) — a visibly stray extra motion cycle. This is what actually produced the reported jank, not merely the `mode="wait"` gap.

Fixed by dropping `AnimatePresence` for this case and tracking the outgoing screen's rendered content and the transition's direction directly in local state (`current`/`outgoing`, both plain values fixed at the moment of navigation — never a variant recomputed after the fact for an element no longer in the render tree). Both screens render simultaneously, absolutely positioned within a `position: relative; overflow-x: hidden; min-height: 100vh` container, and slide a full `x: ±100%` (not a 48px nudge — barely readable as a slide) using an iOS-style ease-out curve; the outgoing one removes itself via `onAnimationComplete` once its animation finishes. Verified via the same rAF-sampling technique that push/pop now produce one continuous, correctly-directioned motion with no gap and no correction cycle, that a rapid push+pop round trip doesn't leave a stray absolutely-positioned element behind once settled, and that a screen's own component state survives being the _outgoing_ side of a single transition (only cleared on an actual full route unmount, e.g. popping all the way back to a sibling route — the same as this app's un-animated routing already does).

**Deviation (fix from manual testing — repeated `group_members` fetches while navigating):** `fetchMyGroups` (Ticket 11's live, uncached Supabase read — `select=role,groups(*)&user_id=eq...`) was called independently, with no caching or sharing, from ~9 different components: `ContextSwitcher`, `LogPage`, `LogsPage`, `GroupList`, `GroupSettings`, `GroupLogPicker`, `AddLogEntryDialog`, `useCopyTargets`, and the route guards `RequireGroupMember`/`RequireGroupOwner`. Pantry and Recipes are leaf routes that fully unmount/remount on every navigation (standard React Router behavior for two different matched routes, animated or not), so `ContextSwitcher` — rendered on both — refetched on every mount; `LogPage` did the same for its own needs. Before this ticket, reaching a second one of Pantry/Recipes/Log required typing a URL, so this was rarely noticed. BottomNav (this ticket's own addition) makes it one tap, which turned the same live query into something firing on every single tab switch — reported directly by manual testing of this PR.

Added a shared, deduplicated cache (`src/features/groups/useMyGroups.ts`, a small Zustand store + `useMyGroups(userId)` hook) and switched the read-only call sites to it: `ContextSwitcher`, `LogPage`, `LogsPage`, `AddLogEntryDialog`, and `useCopyTargets` (the last simplified to a pure derivation with no `useState`/`useEffect` of its own, now that the underlying fetch is already cached elsewhere — also removes a race-condition guard that's no longer needed once there's no async fetch left in this specific hook to race). `api.ts`'s `createGroup`, `updateGroup`, `deleteGroup`, and `acceptGroupInvite` each call `invalidateMyGroups()` on success so every mounted reader picks up the change reactively (the hook re-fetches on its own if it notices the cache went stale while it's still mounted), with no caller needing to remember to invalidate manually. `removeGroupMember` does not invalidate — it's only ever used (`GroupSettings`) to remove a _different_ member, never the caller's own membership, so it can't make the caller's own cached list stale.

**Deliberately left unconverted:**

- `RequireGroupMember`/`RequireGroupOwner` — route guards that must see a genuinely fresh membership/ownership check on every group entry, not whatever another screen happened to cache seconds or minutes earlier (see issue #34's audit, which is why these exist at all). Their own fetch is already keyed on `[userId, groupId]`, so it doesn't refire on a same-group tab switch anyway — the repeated-fetch problem here was specific to `ContextSwitcher`/`LogPage`, not these guards.
- `GroupSettings` — its `fetchMyGroups` call explicitly doubles as "this screen's own defense-in-depth check that the caller still owns the group" (see its own comment, Ticket 13) on top of `RequireGroupOwner`; sharing a cache that could be stale by the time this screen is reached would weaken that second check. Also not a BottomNav destination, so not part of the reported hot path.
- `GroupList` and `GroupLogPicker` — both have their own bespoke error-message UI on fetch failure (a specific `Alert`, not just falling back to an empty list like every other call site); `useMyGroups` swallows fetch errors into `[]` to match the _majority_ of call sites' existing `.catch(() => setGroups([]))` behavior, which would have silently dropped these two screens' distinct error messaging. Neither is a BottomNav destination either.
- `useSyncEngine` — deliberately refetches membership every 60s pull cycle by design, per its own comment ("so a newly joined group starts pulling without needing a full reload"); it isn't triggered by navigation at all, so it isn't part of what the user reported.

**Verification:** Same database-connection restriction as every prior ticket (org policy: must not connect to databases) — the real app's auth/onboarding gate depends on a live Supabase session, so the actual authenticated screens weren't exercised end-to-end. Unlike some earlier tickets, browser automation _was_ available this time (Playwright via a temporary local install, not committed): built a throwaway harness (`dev-preview.html` + `src/dev-preview-main.tsx`, deleted before committing, never touching `src/lib/supabase.ts`'s data calls) that mounted `AnimatedAppShell` and `BottomNav` against fake in-memory routes and list data. Confirmed via that harness: push (list → detail) enters from the right, pop (detail → list) exits to the right; a tab switch (Pantry → Recipes) only fades, with no horizontal offset, and `BottomNav`'s active tab updates correctly with no pill background; the FAB's bounding box is pixel-identical (`right: 16` / `bottom: 80` from the viewport) before, during, and after an animated transition, confirming `FloatingPortal` actually fixes the positioning bug rather than masking it; no console/page errors during any transition. `tsc -b` and `oxlint` are also clean. Manual follow-up once a real environment is available: re-check the same push/pop/tab-switch/FAB behavior on the live Pantry/Recipes/Log/Groups screens with real data and real group-context switching (`/groups/:groupId/pantry` etc.), since the harness used flat fake routes rather than the app's actual group-prefixed URL structure.

The `useMyGroups` cache fix (above) was verified the same database-connection-free way, but with Playwright's own request interception instead of fake data — a harness mounted the real `ContextSwitcher` component and used `page.route('**/rest/v1/group_members**', ...)` to fulfill the request locally (the exact endpoint the user reported, never forwarded to the real Supabase project) while counting how many times it was hit. Confirmed: 1 call on first mount, still 1 after 5 simulated tab-switch remounts, 2 after an explicit `invalidateMyGroups()` + remount (simulating a `createGroup`/`acceptGroupInvite`/etc. success), and still 2 after 3 more remounts — i.e. exactly the "one fetch per genuine change, not per navigation" behavior intended. `LogsPage` and `useCopyTargets` lost their own `useEffect`/`useState` entirely (now pure derivations from the cached value), which also happens to remove two of the pre-existing `set-state-in-effect` lint warnings.

---

## Ticket 17 — Profile screen and logout

**Deviation:** Replaces `AppHeader`'s inline theme-toggle and logout `IconButton`s (added in Ticket 6 explicitly as a stand-in "expected to be replaced by the avatar-icon pattern once Ticket 17 lands") with a single persistent avatar icon that navigates to the new `/profile` screen, per `design-system.md`'s "Profile/account access" pattern. The avatar icon renders the caller's own `profiles.avatar_url` via the existing `PhotoThumbnail` component (same generic placeholder tile as ingredient/recipe/avatar "no photo" states — `design-system.md`'s missing-photo section says this treatment is identical across all three, so the header icon deliberately isn't a circular MUI `Avatar`). Hidden on `/profile` itself, matching the existing `/groups` icon's own "hide on the screen it points to" convention.

**Deviation:** `/profile` reads/writes the caller's own `profiles` row via a direct Supabase call (`fetchMyProfile`/`updateMyProfile`, `src/features/profiles/api.ts`), not through Dexie/the outbox. `profiles` isn't part of `src/sync/pull.ts`'s pull scope (only `ingredients`/`recipes`/`log_entries` are pulled) and has no offline-editing requirement in this ticket — same reasoning already applied to `groups` (Ticket 11). Protected by the existing "update own row only" RLS policy (`profiles for update using (id = auth.uid())`, Ticket 2 migration) — no new policy needed.

**Deviation:** Added a shared cache for "my own profile" (`src/features/profiles/useMyProfile.ts`, a small Zustand store + `useMyProfile(userId)` hook), mirroring `useMyGroups.ts`'s shape (Ticket 16's fix for the same problem). `AppHeader` needs the caller's own profile for its new avatar icon and is rendered by every tab-root/detail page, each of which fully unmounts/remounts on navigation (`AnimatedAppShell`'s documented behavior) — without a shared cache, the avatar icon would refetch `profiles` on every single tab switch, the identical bug class Ticket 16 just fixed for `group_members`. `updateMyProfile` (`api.ts`) invalidates this cache itself on success — mirroring `createGroup`/`updateGroup`/`deleteGroup`'s "invalidation lives in the mutation, not the caller" pattern, not `GroupSettings`' inline call — so a future direct call site can't forget to invalidate.

**Deviation (fix from the code-reviewer sub-agent pass):** The cache's first draft left `profile: undefined` on a fetch failure — indistinguishable from "never fetched yet." Every mounted `useMyProfile` consumer's effect is keyed on `[userId, profile, inflight]`; when `inflight` flipped from a promise to `null` on that failure, the effect re-ran, saw `profile === undefined && !inflight`, and called `ensureLoaded` again — whose own dedup guard also saw both falsy and didn't short-circuit. Net effect: an unrecoverable fetch failure (network blip, expired/refreshing auth token, replication delay right after signup) would retry the `profiles` request continuously with no backoff for as long as any consumer stayed mounted — which, via `AppHeader`, is nearly always. Fixed by adding a distinct `error: boolean` field to the cache (mirroring `useMyGroups`' `[]`-on-error fallback, which is a _defined_ value the dedup guard already treats as resolved) — the dedup guard and the retry effect both now check it, so a failure is cached as a stable, non-retrying state until an explicit `invalidateMyProfile()` call (wired to a "Try again" button on the Profile screen's new error state, replacing what would otherwise have been a permanent spinner).

**Deviation:** The "Photo URL (optional)" field on the profile form follows the same plain-string pattern already used by `IngredientForm`/`GroupForm`/`RecipeForm` — no real upload flow exists anywhere in this codebase yet (`frontend-architecture.md`'s client-side compression/R2-upload flow is undocumented as implemented for any entity), so avatar editing isn't held to a different standard than ingredient/recipe photos already are.

**Why:** All four exist to satisfy this ticket's acceptance criteria (view/edit name/height/avatar, a working logout confirmation flow) using the exact avatar-icon entry point Tickets 6/9/11 already anticipated and explicitly deferred to this ticket, without introducing an upload flow or an offline-write path this ticket doesn't call for.

**Not yet verified:** Same database-connection restriction as every prior ticket (org policy: must not connect to databases) — not exercised against a live Supabase project, and no browser automation tooling available in this session to drive the real auth-gated app. Verification here was `tsc -b`, `oxlint`, and `vite build`, all clean, with no new lint warnings introduced. Manual follow-up needed: log in, open `/profile` via the header avatar icon from each of the four tabs and confirm it slides in as a push (not a tab-switch fade); edit name/height/avatar URL and confirm "Save changes" persists and the header avatar updates immediately without a reload; toggle dark mode from this screen and confirm it matches the prior header toggle's behavior (persisted via `CssVarsProvider`'s `modeStorageKey`); log out with an empty outbox and confirm it proceeds immediately to `/login`; populate a pending outbox item and confirm the count-specific warning appears and Dexie is cleared entirely on confirmed logout.

**Deviation (added after initial review — birthdate editable too):** Requested directly: this ticket's own scope line ("Weight/goal editing — that's Progress, profile is account-level info only") is superseded for birthdate specifically — it's now editable from `/profile`'s form alongside name/avatar/height. Being a plain `profiles` column, it folds into the existing `ProfileInput`/`updateMyProfile` path unchanged — added to the form using the same MUI X `DatePicker` (`disableFuture`, `minDate` 120 years back) as onboarding's `AboutYouStep`, for the same reasons that step already documents. Does not recompute `profiles.daily_kcal_target`, even though birthdate (age, via Mifflin-St Jeor) feeds that calculation in `src/features/onboarding/calorieCalc.ts` — recomputing it is a materially bigger feature (rerunning the onboarding BMR/TDEE/goal-adjustment pipeline outside the onboarding flow itself) than "make this field editable," and isn't implied by that request; left for a future ticket, most likely alongside Progress (Ticket 18), which is the screen that actually surfaces the target.

A weight-editing addition ("log today's measurement" against `weight_logs`) was also built and briefly landed here in the same follow-up, then reverted at the user's direction before merging — weight stays entirely Progress's (Ticket 18) to own, matching this ticket's original scope text as written. No trace of that attempt remains in the shipped code (the `weight_logs` API functions, the `WeightLog` type, and a `(user_id, logged_at)` unique-constraint migration written to support it were all removed again); mentioned here only so a future Ticket 18 implementer knows an upsert-on-unique-constraint approach for "one weight entry per day" was already prototyped and can be revisited rather than re-discovered from scratch.

**Why:** Birthdate: requested directly, after this ticket had already landed with it treated as out of scope per its own text. Weight: requested directly, then un-requested directly once the user reconsidered where it belonged.

**Not yet verified:** Same database-connection restriction as above. Manual follow-up needed, in addition to the checks already listed above: edit birthdate and confirm it persists and round-trips correctly through the `DatePicker`.

---

## Ticket 18 — Progress screen (weight/BMI tracking)

**Deviation:** Adds `@mui/x-charts` (`LineChart`) as a new dependency for the weight-trend chart — `frontend-architecture.md`'s stack table doesn't list a charting library, and none existed anywhere else in the codebase to reuse. Chosen over a general-purpose option (e.g. Recharts/Visx) because it's from the same MUI family/major version line as the already-installed `@mui/x-date-pickers@^7.29.4` (installed at `^7.29.1`, the closest matching release), keeping this a same-vendor addition rather than a second charting ecosystem, and its peer deps confirm React 19 + MUI v6 support.

**Deviation:** `weight_logs` reads/writes go through a shared Zustand cache (`src/features/progress/useWeightLogs.ts`), mirroring `useMyProfile.ts`'s (`ensureLoaded`/`invalidate`) shape — same pattern Tickets 16/17 introduced for `group_members`/`profiles`. Progress is a BottomNav tab root (`routes.md`), and `AnimatedAppShell` fully unmounts/remounts a route's tree on every navigation, so an uncached per-mount fetch would re-pull the caller's whole weight history on every tab switch — the identical bug class those two tickets already fixed elsewhere. `LogWeightDialog`'s successful insert calls `addWeightLog()` to append into this cache directly rather than triggering a refetch.

**Deviation:** Added a `weight_logs.weight_kg` range check constraint (`supabase/migrations/20260904000000_weight_logs_check_constraint.sql`, `check (weight_kg > 0 and weight_kg < 500)`) — not present in `schema.md`. RLS on this table (`phase1_schema.sql`) only restricts _which_ rows a caller can write, not the values within them, and the new "log weight" write path had no server-side value validation at all otherwise (the dialog's HTML `min`/`max` is a UI affordance only). Bounds are intentionally generous — outside any real adult human weight — purely to reject typos/garbage, not to second-guess legitimate values.

**Design note (not a spec deviation):** `fetchWeightLogs` windows to the last 180 days rather than the caller's entire history, to keep the fetch and the chart's point-scale x-axis from growing unbounded over years of daily entries. Nothing in `schema.md`/`routes.md` specifies a retention window either way.

**Why:** All three deviations exist to satisfy this ticket's acceptance criteria (chart, BMI, goal display, ability to log a new entry) safely and consistently with how the last two tickets already solved the same two problem shapes (tab-root refetch storms, unvalidated writes) elsewhere in the app, without inventing a third pattern for either.

**Not yet verified:** Same database-connection restriction as every prior ticket (org policy: must not connect to databases) — not exercised against a live Supabase project, and no browser automation tooling available in this session to drive the real auth-gated app. The new check constraint migration has not been applied anywhere (not pushed via `supabase:push:dev`/`prod`) for the same reason. Verification here was `tsc -b`, `oxlint`, and `vite build`, all clean, with no new lint warnings introduced. Manual follow-up needed: apply the new migration; log a first-ever weight entry and confirm the chart shows a single marker (not the "log to start your trend" placeholder); log a second entry and confirm the chart renders a two-point trend line with no layout jump; confirm BMI/current-weight/goal figures match `profiles.height_cm`/`goal_weight_kg`/`goal_type` for a real seeded account; confirm leaving Progress for another tab and returning does not refetch `weight_logs` (e.g. via the network tab); confirm a same-day second log entry (onboarding's seeded entry plus a manual log) is ordered correctly as "current weight" rather than showing the earlier value.

**Deviation (requested directly — chart range dropdown):** Added a range selector (`src/features/progress/chartRanges.ts`) to the "Weight trend" card: 7/14/30/60/180 days, defaulting to 7 (changed from an initial default of 180 — the prior, unfiltered view — per direct follow-up feedback). Filters the already-fetched `weight_logs` client-side rather than refetching per selection — the fetch window (`api.ts`'s `WEIGHT_HISTORY_DAYS`) is derived from `Math.max(...WEIGHT_CHART_RANGE_DAYS)` (still 180) rather than the default, so it always covers every selectable range regardless of which one loads first. Not in `routes.md`/`design-system.md`; not a deviation from either, just scope beyond the ticket's original text, requested directly.

**Deviation (fix, found via manual testing of this PR — chart line/marks were invisible):** Reported directly: selecting a different range in the new dropdown appeared to do nothing. Root cause had nothing to do with the dropdown itself (confirmed via a no-database harness — see below — that the underlying data filtering was already correct): `WeightChart.tsx`'s original `series[].color` was a raw `'var(--mui-palette-primary-main)'` string, but `theme/theme.ts`'s `createTheme` doesn't set `cssVariables: true`, so that CSS variable is never actually defined anywhere in the page. An undefined `var()` with no fallback makes `stroke` invalid, which computes to its initial value (`none`) — so the trend line was always invisible, in every range, and the marks fell back to a barely-visible `background.paper`-colored fill with the same invalid stroke. This wasn't specific to the dropdown feature; it dated back to this ticket's original chart implementation and just hadn't been noticed by inline code review or `tsc`/`oxlint`/build (none of which check rendered pixel output). Fixed by adding `theme.ts`'s `primaryAccent` (a light/dark literal-hex export, same shape as its existing `shadows` export, sourced from the same constants the theme's own `colorSchemes.{light,dark}.palette.primary.main` use, so they can't drift) and having `Progress.tsx` resolve it via the `useColorScheme()`/`resolvedMode` pattern it already uses for `shadows`, passed into `WeightChart` as a new required `color` prop — replacing the broken CSS variable with a plain, always-defined hex string.

**Why:** Range dropdown: requested directly. Color fix: requested indirectly (reported as "the dropdown isn't working"), root-caused to a pre-existing rendering bug unrelated to the dropdown itself.

**Verified without a database:** Same database-connection restriction as above for the rest of this ticket, but this specific fix _was_ browser-verified — a throwaway Playwright harness (temporary `dev-preview.html` + `src/dev-preview-main.tsx`, deleted before committing, mounting only `WeightChart`/`chartRanges`/`theme.ts` — none of which import `src/lib/supabase.ts` — against locally-generated fake `WeightLog[]` data, never touching a real Supabase project) confirmed: before the fix, the line's computed `stroke` was `none` and `--mui-palette-primary-main` was empty everywhere in the page; after the fix, the line's computed `stroke` correctly resolved to `rgb(141, 161, 1)` (`#8DA101`, light mode) and `rgb(167, 192, 128)` (`#A7C080`, dark mode), with screenshots confirming a clearly visible connecting line and ring-style marks in both modes, changing distinctly between range selections (e.g. 36 points at 180 days vs. 2 at 14 days).

**Deviation (fix, found via further manual testing — x-axis dates weren't reflecting the selected range):** Reported directly, after the color fix above: "it's still just showing 5 [dates]" regardless of dropdown selection, with the expectation that the axis update "regardless if it had data on the previous dates or not." Root cause: `WeightChart`'s x-axis used `scaleType: 'point'` — a categorical scale with exactly one evenly-spaced slot per data point, with no notion of the actual calendar distance between them. For a sparse dataset (few entries clustered in a short window, e.g. onboarding's single seeded entry plus one or two manual logs), the axis only ever showed however many points existed, at even spacing, regardless of which dropdown range was selected — a 7-day and a 180-day view could render identically if all the caller's entries happened to fall in the same few days. Fixed by switching to `scaleType: 'time'` with explicit `min`/`max` set to the selected range's actual boundaries (`Progress.tsx`'s `rangeCutoff` and today, passed down as new `rangeStart`/`rangeEnd` props, parsed into local-midnight `Date`s the same way `formatShortDate` already did for tick labels) — the axis now always spans the true selected calendar window, leaving visible empty space where there's no data, rather than stretching sparse points to fill whatever width was available.

**Deviation (requested directly — default range changed to 7 days):** `DEFAULT_WEIGHT_CHART_RANGE_DAYS` changed from 180 to 7 — a week is a more immediately useful first-load view than the full history.

**Why:** Both requested directly in the same follow-up round as the color fix above.

**Verified without a database:** Same throwaway-harness approach as the color fix, this time with only 3 fake entries clustered in the last 3 days (deliberately sparse, to reproduce the reported symptom) — confirmed via the axis's rendered tick labels that selecting 7 / 30 / 180 days correctly changed the visible date span (e.g. "Aug 16–Aug 23" → "Jul 26–Aug 23" → "Mar 1–Aug 1") even though the same 3 data points were the only ones ever plotted.

**Deviation (requested directly — edit goal from Progress):** Added `updateGoal` (`src/features/progress/api.ts`) and `EditGoalDialog`/`EditGoalForm` (`src/features/progress/EditGoalDialog.tsx`) so `goal_type`/`goal_weight_kg` can be edited from this screen, not just displayed. The goal card is now always shown (previously hidden entirely when no goal was set, with no way to add one) — reads "No goal set" / "Set goal" when `profile.goal_type` is null, "Goal: <type> to <weight> kg" / "Edit" otherwise. Reuses onboarding's `GoalStep` shape exactly (`SelectableCard` per `GOAL_TYPES` option, `isGoalWeightValid` for the same "goal weight must be below/above current weight" rule, goal weight hidden entirely for `'maintain'`) rather than inventing a second goal-editing UI. `updateGoal` writes directly to `profiles` under the same "update own row only" RLS policy `updateMyProfile` (`features/profiles/api.ts`) already relies on, and calls the existing `invalidateMyProfile()` so this screen's own goal display picks up the change immediately, same invalidation pattern already used everywhere else a `profiles` field is mutated. Initially did not touch `goal_pace`/`daily_kcal_target` — see the follow-up entry directly below, where that was requested directly and is no longer deferred.

**Why:** Requested directly. `Profile.tsx`'s own comment already named this screen as the intended owner of goal editing ("Weight/goal editing is explicitly out of scope, owned by Progress"), so this fills a gap this ticket's original scope (display-only) left open rather than introducing a new ownership question.

**Deviation (requested directly — show and recompute daily_kcal_target):** `EditGoalDialog` is now a two-step flow — Goal (as above), then a new Calorie target step reusing onboarding's actual `CalorieTargetStep` component and `calorieCalc.ts` functions verbatim, rather than a second implementation of the same BMR → TDEE → goal-adjusted-preset logic. Re-derives BMR/TDEE from data already on the profile (`sex`, `activity_level`, `birthdate` → age, `height_cm`) plus the caller's latest logged weight, instead of re-asking for any of it. `calorieCalc.ts` gained `CalorieSelection` (moved from `CalorieTargetStep.tsx`, which now just re-exports it — the type had to live somewhere both files could import from without a cycle) and `resolveCalorieTarget` (extracted from `OnboardingStepper.handleFinish`'s inline branching, which now calls the same shared function — avoids two copies of the exact "which DB columns does this selection map to" logic staying in sync by hand). The calorie step pre-fills from the profile's existing `goal_pace`/`daily_kcal_target` (unlike onboarding, which always starts blank) so editing an unrelated field (e.g. just the goal weight) doesn't force re-confirming a calorie target that isn't changing. `Progress.tsx`'s goal card now shows `daily_kcal_target` as a second line ("N kcal/day target") beneath the goal summary. If the profile is missing an input needed to recompute (shouldn't happen post-onboarding, but not assumed), the calorie step shows an info message and keeps the existing `goal_pace`/`daily_kcal_target` unchanged rather than blocking the save.

**Deviation (fix, found via manual testing of this change — stale calorie selection survived a goal-type switch):** Switching goal type from lose/gain to maintain (or back) without touching the calorie step left the previous pick (e.g. `'steady'`) sitting in state even though it no longer corresponds to any option maintain's `CalorieTargetStep` renders (a single "Maintain your weight" card, no presets) — nothing appeared selected, but `resolveCalorieTarget` still received the stale value, found no matching preset, and returned `null`, which `handleSave` treated as "nothing changed," silently keeping the old target. Meanwhile the step's own validity check didn't know the selection was stale, so "Save goal" stayed enabled with no visible selection. Fixed with a derived `effectiveSelection` (`isSelectionValidForGoalType`) that masks a selection that doesn't match the current goal type, used everywhere the raw selection previously was (`CalorieTargetStep`'s `selection` prop, the validity check, and the save handler) — masking rather than clearing the underlying state means flipping goal type back and forth restores an earlier valid pick instead of losing it permanently.

**Why:** Both requested directly in the same follow-up round.

**Verified without a database:** Same restriction as the rest of this ticket, but the full two-step flow _was_ browser-verified — a throwaway harness rendered the real `EditGoalDialog` (not a duplicate) against a fake `Profile`, confirming: goal weight and calorie selection both pre-fill correctly from existing profile values; the calorie step recomputes from current weight rather than showing the stale stored `daily_kcal_target`; the "Maintain weight" single-option layout and custom-target validation (disabled/enabled "Save goal" on invalid/valid input) both render correctly; and, after the fix above, "Save goal" is correctly disabled the moment a goal-type switch invalidates the prior calorie selection, then correctly re-enables once either a new selection is made or the goal type is switched back to one where the original selection is valid again. The harness never invoked the real `updateGoal`/Supabase write (the "Save goal" button was never clicked) — confirmed via network-request logging that only local dev-server module loads occurred, no request reached any backend. Manual follow-up needed: exercise the actual save against a real account, including a genuinely incomplete profile (to check the "not enough profile info" fallback path).

**Not yet verified:** Same database-connection restriction as the rest of this ticket. The interactive form logic (goal-type selection, conditional goal-weight field, validation message, submit-button enable/disable) _was_ verified via a throwaway, database-free harness duplicating `EditGoalForm`'s JSX with a stubbed submit handler (real `EditGoalDialog` wasn't rendered directly, since it imports `./api` → `src/lib/supabase.ts`) — confirmed selecting "Lose weight" then entering a goal weight above the fake current weight (80kg) disables "Save goal" with the expected error text, and correcting it to a valid lower value re-enables it. Not verified: the actual `updateGoal` Supabase write and `invalidateMyProfile()` refresh against a real account.

---

## Ticket 15 — Photo upload (R2 + client-side compression)

**Deviation (requested directly — avatar upload beyond ticket scope):** This ticket's literal scope is ingredient/recipe photo upload only, but real photo upload (not a plain URL field) was added to `/profile` (`ProfileForm.tsx`) and the onboarding flow (`AboutYouStep.tsx`, new — onboarding previously had no avatar field at all) as well. Both reuse the exact same `PhotoUpload` component (`src/components/PhotoUpload.tsx`), `uploadPhoto` helper (`src/lib/photoUpload.ts`), and `get-upload-url` Edge Function as ingredients/recipes, via a third `entity: 'avatar'` value. This supersedes Ticket 17's earlier entry (line ~486 above) noting the profile avatar field was a plain-string placeholder pending a real upload flow — that flow now exists.

**Deviation:** Extends `schema.md`'s documented R2 path convention (`ingredient-photos/{uuid}.webp`, `recipe-photos/{uuid}.webp`) with a third prefix, `avatar-photos/{uuid}.webp`, for the avatar deviation above. Avatars share the same R2 bucket as ingredient/recipe photos (confirmed directly) rather than a separate bucket — same public, UUID-obscured, low-sensitivity reasoning schema.md already applies to ingredient/recipe photos.

**Deviation:** Onboarding's avatar save is a best-effort call made _after_ `completeOnboarding`'s RPC succeeds (`OnboardingStepper.handleFinish`), not folded into the `complete_onboarding` SQL RPC/migration. A failure saving the avatar doesn't block finishing onboarding — it's swallowed (not surfaced as a blocking error) since the avatar is optional/cosmetic and remains editable afterward from `/profile`. Chosen over extending the RPC to avoid a DB migration for a purely cosmetic field, especially given this session can't apply/verify migrations against a live project (see "Not yet verified" below). `updateMyProfile` (`src/features/profiles/api.ts`) was widened from `ProfileInput` to `Partial<ProfileInput>` so this call can send just `{ avatar_url }` without resending `name`/`height_cm`/`birthdate` that the RPC already just wrote. `onboarding/api.ts`'s `fetchProfileName` was deleted (no longer used) — `OnboardingStepper` now seeds both `name` and the pre-existing OAuth `avatar_url` via `fetchMyProfile` instead.

**Deviation:** `RecipeDetail.tsx` and `ProfileForm.tsx`/`Profile.tsx` each previously rendered a separate, standalone `PhotoThumbnail` preview alongside a text field for the same value. Since `PhotoUpload` already renders its own thumbnail/preview, these were collapsed into one element rather than showing the same photo twice — `RecipeDetail`'s hero preview and inline `photo_url` field became a single `PhotoUpload`; `Profile.tsx`'s standalone avatar preview above the form was removed since `ProfileForm`'s new `PhotoUpload` already shows one.

**Deviation (requested directly — deterministic object keys, replacing this ticket's original "orphaned objects accepted" design):** R2 object keys are now named by entity id (`{prefix}/{ingredient-or-recipe-id}.webp`, or the caller's own user id for avatars) instead of a random UUID, so re-uploading a photo for an existing ingredient/recipe/avatar overwrites the previous R2 object in place rather than leaving it orphaned — R2's `PUT` to an existing key replaces it natively, no explicit delete call needed. A brand-new ingredient/recipe (no row yet, i.e. the create flow — `CreateIngredientDialog`/`CreateRecipeDialog`/`AddRecipeIngredientDialog`'s "New ingredient" path) has no id to key off yet, so `get-upload-url` falls back to a random UUID for that one-time first upload; once the row exists, the next edit's upload switches to id-based naming and starts overwriting. Because the returned `photo_url`/`avatar_url` needs to change on every upload (so `RecipeDetail`'s dirty-check, `IngredientForm`'s/`ProfileForm`'s submit, etc. all still see a new value to save) even though the underlying R2 key doesn't, the Edge Function appends a `?v=<timestamp>` cache-busting query string to the URL it returns — this also means a browser/CDN cache keyed on the old URL+bytes can't serve stale image content after a photo is replaced, at the cost of every re-upload getting a "new" stored URL pointing at the same, now-overwritten object.

**Deviation (security, required by the change above):** Letting a client-supplied id determine which R2 object gets overwritten is only safe if that id is actually authorized — ingredient/recipe ids are visible in the URL (`/recipes/:recipeId` etc.), so without a check, any authenticated user could pass any other user's/group's ingredient or recipe id and overwrite its photo. `get-upload-url` now takes an optional `id` in its request and, for `entity: 'ingredient' | 'recipe'`, verifies it via an RLS-scoped Supabase client (the caller's own JWT forwarded as the `Authorization` header) doing `select('id').eq('id', id)` against the corresponding table before signing — confirmed by reading `supabase/migrations/20260819000000_phase1_schema.sql` that the `select` and `update` RLS policies for both tables use the byte-identical `using` predicate, so "this row is visible to the caller" is exactly equivalent to "this caller may update this row," not just an approximation. A row not found/visible returns 403. `entity: 'avatar'` never accepts a client-supplied id at all — it's always the authenticated caller's own `auth.uid()`, so a user can only ever overwrite their own avatar.

**Deviation (fix, found during this ticket's own code-reviewer pass):** The first draft of `get-upload-url` relied solely on `supabase/config.toml`'s `verify_jwt = true` to enforce `rpcs.md`'s "must be authenticated" contract. That's insufficient on its own — `verify_jwt` only checks that the `Authorization` header carries _some_ validly-signed Supabase JWT, and the public `anon` key (shipped in every client bundle, trivially extractable) is itself one, so it doesn't distinguish a real signed-in user from anyone holding the anon key. Fixed by adding an explicit `supabase.auth.getUser(token)` check inside the function body (using the platform-injected `SUPABASE_URL`/`SUPABASE_ANON_KEY` env vars, not custom secrets), rejecting the anon key and any missing/invalid token with 401 before generating a presigned URL.

**Deviation (fix, same pass):** The presigned PUT is now signed with `content-type: image/webp` as part of the SigV4 signature (the client's fetch call already sends exactly that header), constraining the presigned URL to only accept a webp upload within its 5-minute window rather than an arbitrary file type. This doesn't validate the actual body bytes or enforce a size limit — R2 presigned PUTs don't support size-limit conditions the way S3 POST policies do, and adding that would need a materially different (POST-policy-based) upload mechanism, judged out of scope for this ticket. Accepted residual gap: a party with a valid presigned URL could PUT up to 300s worth of arbitrarily large `image/webp`-labeled bytes.

**Deviation:** The Edge Function's CORS response header is `Access-Control-Allow-Origin: *` rather than scoped to this app's specific origins — there's no existing origin-allowlist convention anywhere else in this codebase to extend, and the real access-control boundary is the auth check above, not CORS (CORS only restricts which browser-run origins can read the response; it doesn't gate the request from reaching the function). Tightening this to explicit dev/prod origins is straightforward future work once those origins are fixed and known.

**Deviation (fix, found during this ticket's own code-reviewer pass — onboarding upload race):** The first draft's onboarding wiring let the user advance past the "About you" step (or hit "Get started") while an avatar upload was still in flight, since `AboutYouStep`'s `PhotoUpload` wasn't wired to `OnboardingStepper`'s navigation the way every other consumer (`IngredientForm`, `RecipeForm`, `RecipeDetail`, `ProfileForm`) already gates its own submit button. Navigating away unmounts `AboutYouStep` mid-upload; when the upload later resolved, its `onChange` fired on state that either no longer existed or wasn't read again — meaning the avatar could finish uploading to R2 (creating a real orphaned object, not just the already-documented "replaced photo" case) but never reach `profiles.avatar_url`, with no error shown. Fixed by adding an `avatarUploading` state in `OnboardingStepper` (via `AboutYouStep`'s new `onAvatarUploadingChange` prop) and disabling both "Continue" and "Get started" while it's true, matching the other four forms' pattern exactly. `PhotoUpload.tsx` itself also gained a mount-ref guard around its async upload as a defensive backstop (matching the `cancelled`-flag convention already used in `RecipeDetail.tsx`/`useProfileNames.ts`/etc.), in case some future consumer doesn't gate navigation the way all five current ones do.

**Why:** All of the above were either requested directly (avatar upload, same bucket) or follow directly from minimizing new surface area given this session's DB-connection restriction (no RPC/migration growth, no manual JWT-role code beyond the standard `auth.getUser` check, no object-deletion logic) while still meeting the ticket's acceptance criteria, except the three "fix" entries, which were real bugs/gaps caught by this ticket's own `code-reviewer` subagent pass before the PR was opened.

**Not yet verified:** Same database-connection/deployment restriction as every prior ticket in this doc — this session cannot create Cloudflare resources or connect to a database (org policy), so none of the following has been exercised against a live setup: the R2 bucket has not been created; the bucket's own CORS policy (distinct from the Edge Function's CORS handling — must allow `PUT` and the `content-type` header from the app's dev/prod origins) has not been configured; the five `R2_*` secrets have not been set via `supabase secrets set`; the `get-upload-url` function has not been deployed (`npm run supabase:deploy-functions:dev`/`:prod`, added by this ticket); the exact `aws4fetch` presign call shape has not been confirmed against a real R2 bucket; the RLS-scoped ownership check for `entity: 'ingredient' | 'recipe'` (a `select` against the live table using the caller's forwarded JWT) has only been verified by reading the RLS policy SQL directly, not by exercising it against a real project with two different users/groups; and the `?v=<timestamp>` cache-busting query string's effectiveness assumes whatever CDN/cache sits in front of the public R2 URL includes the query string in its cache key (true for a default Cloudflare config, but e.g. a "Cache Everything, ignore query string" page rule on a custom domain would defeat it) — not verified against the actual bucket/domain configuration once one exists. Manual follow-up: complete the setup above, then upload a photo through each of the five surfaces (ingredient, recipe, avatar via `/profile`, avatar via onboarding, and the `RecipeDetail` inline edit path), confirm the compressed WebP appears correctly in card/detail views, confirm re-uploading a photo for the same ingredient/recipe/avatar actually overwrites (check the R2 bucket's object list doesn't grow per re-upload) and that the new photo displays immediately rather than a stale cached one, and confirm removing a photo reverts to the generic placeholder tile.

**Deviation (requested directly — defer ingredient/recipe upload to submit-time):** Uploading immediately on file pick (the original design above) meant a photo was already sitting in R2 the moment it was picked, regardless of whether the surrounding form was ever actually submitted — cancelling out of a create dialog, or navigating away from an edit screen without saving, left an uploaded-but-never-referenced photo behind with no id scheme able to fix it, since the upload happened before "submit" was even a relevant concept. Fixed for ingredients and recipes only by adding `src/components/DeferredPhotoUpload.tsx`, which stages a picked file locally (a blob-URL preview, revoked on replace/unmount) and hands the raw `File` to the parent form via a new `onFileSelected` callback instead of uploading it — the actual `uploadPhoto()` call now happens inside `IngredientForm`/`RecipeForm`/`RecipeDetail`'s own submit/save handlers, immediately before the create/update call, using the same try/catch and `submitting`/`saving` state those handlers already had (an upload failure surfaces through the existing error `Alert`, no new error-handling path needed). `PhotoUpload.tsx` (the original immediate-upload component) still exists, simplified (dropped the now-always-unused `entityId` prop) — it's now used only by `ProfileForm.tsx`/`AboutYouStep.tsx`.

**Deviation (requested directly — avatar deliberately excluded from the above):** Avatar upload was NOT rewritten to defer — by the time a user reaches `/profile` or onboarding's avatar picker, their own user id already exists (created at signup), so the "id doesn't exist yet" problem that motivated deferring for ingredients/recipes doesn't apply. Accepted residual risk, not fixed: picking a new avatar still uploads immediately (overwriting the deterministic `avatar-photos/{userId}.webp` object) even if the surrounding form's Save/Get-started is never clicked — a cancelled avatar edit can still leave the _displayed_ avatar changed if the old cached URL happens to be refetched, since the underlying object was genuinely overwritten. This is a known, explicitly-scoped-out trade-off, not an oversight.

**Deviation (requested directly — delete-time R2 cleanup):** Deleting an ingredient/recipe previously only deleted the Postgres row — its R2 photo (keyed by the entity's own id) was never removed, so every deletion left a permanently orphaned object. Added a new `delete-photo` Edge Function (`supabase/functions/delete-photo/index.ts`), called from `deleteIngredient`/`deleteRecipe` (`src/features/pantry/api.ts`/`src/features/recipes/api.ts`) — best-effort (swallowed on failure, e.g. offline; never blocks the actual delete) and, critically, called _before_ the row is removed from Dexie/the outbox, not after — the function's ownership check (an RLS-scoped `select`) needs the row to still exist to verify the caller could see it, and running it after the row was gone would make every legitimate deletion incorrectly fail.

**Deviation (security, required by the above — copy/fork reference check):** `copy_ingredient`/`copy_recipe` (Ticket 14) copy `photo_url` verbatim as a string into the new row rather than giving a copy its own independent photo — a copy's `photo_url` can point at its _source's_ R2 object. Naively deleting `{prefix}/{id}.webp` whenever any row is deleted would silently break every copy still referencing the original's photo. Fixed by having `delete-photo` check, via a service-role client (bypassing RLS — a copy can live in a group the deleting caller has no access to, so an RLS-scoped check would miss it and wrongly conclude "safe to delete"), whether any _other_ row anywhere still has a `photo_url` containing this key before actually deleting from R2; if so, the R2 delete is skipped (not an error) but the row delete still proceeds normally. This was an explicit choice among three options presented directly (the others being: leave copy/fork's photo-sharing behavior alone and skip automatic cleanup entirely, or make copies own an independent photo by rewriting the copy RPCs) — the user chose this one specifically to avoid touching the existing, already-shipped copy/fork feature's behavior at all.

**Deviation (required by deferring upload to submit-time):** `createIngredient`/`createRecipe` (`src/features/pantry/api.ts`/`src/features/recipes/api.ts`) changed from generating their own `id` internally to taking it as a required first parameter — `CreateIngredientDialog.tsx`/`CreateRecipeDialog.tsx`/`AddRecipeIngredientDialog.tsx`'s `NewIngredientForm` now each generate `crypto.randomUUID()` up front (via `useState(() => crypto.randomUUID())`, so it doesn't change across re-renders) and pass the same id to both the form (for staging a photo upload against it) and the eventual create call — this was already exactly how these functions generated ids internally, just moved one level up so the id is available before the row exists. `IngredientForm`'s `ingredientId`/`RecipeForm`'s new `recipeId` prop both became required (previously optional/absent) since every caller now always has one, real or pre-generated.

**Deviation (required by the above — `get-upload-url`'s id becomes required, with a two-tier existence check):** Once upload always happens with a known id, the "no id → random UUID" fallback became dead code for `entity: 'ingredient' | 'recipe'` and was removed — `id` is now required for those two entities (400 if missing). This surfaced a subtlety the original ownership check didn't handle: a create-flow's id is a freshly generated one that legitimately doesn't exist in Postgres yet at upload time (upload now happens _before_ the row is created), and a caller-scoped RLS query can't tell "doesn't exist anywhere" apart from "exists, but hidden from this caller by RLS" — both return nothing. Fixed with a two-tier check (`supabase/functions/_shared/photoAuth.ts`'s `existsAnywhere`/`isVisibleToCaller`): a service-role query first checks whether the id exists _at all_; only if it does does the existing RLS-scoped visibility check run (rejecting with 403 if the caller can't see it — protecting against someone supplying a different, existing entity's id to overwrite its photo). A brand-new id, invisible to the existence check because nothing has been created under it yet, is allowed through with no further check — there's nothing to protect yet. `avatar` is unaffected (still always keyed by the caller's own `auth.uid()`, never client-supplied).

**Deviation:** `get-upload-url` and the new `delete-photo` share auth/ownership logic (CORS headers, the `getUser` check, the RLS-scoped visibility check, a service-role client, and the new existence/reference-check helpers) via `supabase/functions/_shared/photoAuth.ts`, imported by relative path from each function — Supabase's documented convention for code shared across Edge Functions. This didn't exist before because there was previously only one function; a second one made the duplication real rather than premature.

**Why:** All of the above were requested directly in this round of follow-up (defer ingredient/recipe upload; explicitly keep avatar on immediate upload; add delete-time cleanup; the reference-check approach for copy/fork, chosen among three options presented directly) or are load-bearing consequences of implementing those requests correctly (the id-becomes-required change and its two-tier existence check; the shared Edge Function module, once genuine duplication existed).

**Deviation (fix, found during this round's own code-reviewer pass):** `CreateIngredientDialog.tsx`/`CreateRecipeDialog.tsx`'s pre-generated `pendingId` was originally a plain `useState(() => crypto.randomUUID())` — generated once per component mount, not once per dialog _open_. Both dialogs are rendered unconditionally by their parent list screens (`PantryList.tsx`/`RecipeList.tsx`, only toggling the `open` prop) and so never actually unmount between attempts; MUI's `Dialog` unmounts/remounts `IngredientForm`/`RecipeForm` on close/reopen, but not the wrapper component that owned `pendingId`. Result: every "Add ingredient"/"Add recipe" attempt in one page visit after the first reused the same id — a second successful create would fail outright (Dexie's primary key already exists), and if a photo was staged for that second attempt, its upload (which happens _before_ the now-failing create call, per the deferred-upload design) would have already silently overwritten the first ingredient's/recipe's photo in R2. `CreateRecipeDialog` had the identical latent bug but was masked in practice by `RecipeList`'s `onCreated` always navigating away (unmounting/remounting the dialog) — fixed defensively there too rather than relying on that navigation behavior never changing. Fixed by regenerating `pendingId` whenever `open` transitions to `true`, adjusted directly during render (comparing against a tracked `prevOpen`) rather than in an effect — the same "reset state when a value changes" pattern already established in `RecipeDetail.tsx`'s baseline reseed.

**Deviation:** `get-upload-url`'s two-tier existence check means any authenticated caller can request (and receive) a valid presigned upload URL for `entity: 'ingredient' | 'recipe'` with an arbitrary UUID that doesn't yet exist, upload something to it, and never actually create the corresponding row — that object is then permanently orphaned, since `delete-photo` is only ever triggered by a real row's deletion. Real ids are 122-bit random `crypto.randomUUID()` values, so this can't be used to guess or collide with another user's real in-flight id (RLS's insert policy remains the actual authority over who may claim a given id), but it is a superset of the already-documented "no rate limit, no size limit" residual risk — a different flavor (attacker-initiated flooding) than the ones already described (legitimate-user-cancelled uploads). Accepted as-is for the same reasons those were: no existing rate-limiting convention anywhere in this codebase to extend, and out of proportion to this ticket's scope.

**Why (this round's fix and note):** The `pendingId` staleness was a real, reachable bug caught by this round's own `code-reviewer` subagent pass before pushing — not a hypothetical. The unauthenticated-flooding note is a transparency addition, not a code change, surfaced by the same review pass.

**Not yet verified:** Same restriction as every entry above — this session cannot deploy Edge Functions or connect to a database. Specifically new to this round: `delete-photo` has not been deployed (`npm run supabase:deploy-functions:dev`/`:prod`, updated to include it) or exercised against a live project; the two-tier existence check in `get-upload-url` and the reference check in `delete-photo` have only been reasoned through by reading code and RLS policy SQL, not exercised against a real project with actual create/edit/copy/delete sequences across two different users/groups; `SUPABASE_SERVICE_ROLE_KEY` (used by both functions' service-role client) is platform-auto-injected, same as `SUPABASE_URL`/`SUPABASE_ANON_KEY` — no new secret needs to be set, but this has not been confirmed against a live deployed function either. Manual follow-up once deployed: create an ingredient, cancel without submitting after picking a photo, confirm nothing was uploaded to R2 at all; create one for real, confirm its photo is keyed by its real id from the first upload (no transitional random-UUID object); create a second ingredient in the same page visit right after (the fixed bug's exact repro case), confirm it succeeds and gets its own distinct photo; copy an ingredient with a photo, delete the original, confirm the copy's photo still displays (the reference check should have skipped the R2 delete); delete an ingredient with no copies, confirm its R2 object is actually gone.

**Deviation (requested directly — "remove photo" trash icon also deletes the R2 object, everywhere it appears):** Removing a photo without deleting the whole entity previously only cleared the `photo_url` field — the R2 object stayed, permanently orphaned. `delete-photo` was extended to accept `entity: 'avatar'` alongside `ingredient`/`recipe` (previously scoped to only those two, since avatar had no delete trigger at all — removing a photo without deleting the account is exactly such a trigger, just not one initially considered). Avatar needs neither the ownership check nor the reference check the other two entities require: the id is never client-supplied (same as upload — always the caller's own `auth.uid()`), so a caller can only ever delete their own avatar, and avatars are never shared/copied across rows the way `copy_ingredient`/`copy_recipe` can share an ingredient/recipe photo.

For `IngredientForm.tsx`/`RecipeDetail.tsx` (ingredient/recipe — deferred-upload consumers), the R2 delete only fires when going from a saved photo to none (`initialValues?.photo_url`/`savedRecipe.photo_url` was set, the final value being submitted/saved is null), and only _after_ the corresponding field update has already succeeded, not before — deleting from R2 first and then having the save itself fail would leave the row still pointing at a now-deleted object. `RecipeForm.tsx` needed no change — confirmed create-only (no call site ever passes `initialValues`), so there's never an existing photo to clean up there.

**Deviation (fix, found during this round's own code-reviewer pass — avatar removal is deferred too, unlike its upload):** The first draft made `PhotoUpload.tsx`'s trash-icon click call `deletePhoto('avatar')` immediately, fire-and-forget, reasoning it matched avatar's already-accepted immediate-upload trade-off. That reasoning doesn't actually hold: an unwanted immediate _upload_ still leaves the stored URL pointing at _some_ valid image (worst case, the wrong one) if Save is never clicked — but an immediate _delete_ can leave the stored `avatar_url` pointing at _nothing_, a strictly worse, genuinely new failure mode. Concretely reachable, not hypothetical: `ProfileForm.tsx` has no Cancel button and lives on a persistent route — clicking remove then simply navigating away without saving left the row's `avatar_url` referencing a just-deleted object (broken image, no recovery short of re-uploading). Worse, `OnboardingStepper.handleFinish` only ever called `updateMyProfile` when `avatarUrl` was truthy — it had no branch that persisted an explicit removal at all, so _finishing onboarding normally_ after removing a pre-filled (Google SSO) avatar still left the row pointing at the deleted object.

Fixed by removing the `deletePhoto` call from `PhotoUpload.handleRemove` entirely (it now only calls `onChange(null)`, exactly as before this round) and instead deferring the R2 delete to each consumer's own submit handler, mirroring `IngredientForm.tsx`/`RecipeDetail.tsx` exactly: `ProfileForm.tsx` checks `initialValues.avatar_url && !avatarUrl` after `onSubmit` succeeds; `OnboardingStepper.tsx` gained a new `originalAvatarUrl` baseline (captured alongside `avatarUrl` when the profile is first fetched, so it doesn't rerender on typing) purely to distinguish "explicitly removed a pre-filled avatar" from "never had one"/"unchanged" — `handleFinish` now persists a real change (`avatarUrl !== originalAvatarUrl`, including an explicit `null`, not just a truthy new value) and only then, if that change was a removal, deletes the R2 object.

**Deviation (fix, same pass — tightened types):** `uploadPhoto`/`deletePhoto` (`src/lib/photoUpload.ts`) gained overload signatures (`(file, 'avatar')` / `(file, 'ingredient' | 'recipe', id: string)`, and the `deletePhoto` equivalent) instead of one loosely-typed `id?: string` accepting any entity — the original single signature let a future `deletePhoto('ingredient')` call (no id) compile cleanly and only fail at request time with the Edge Function's 400. `PhotoUpload.tsx` also dropped its `entity` prop entirely (hardcoding `'avatar'` internally) rather than keeping it typed as all three `PhotoEntity` values — it's avatar-only with no scenario where it should ever be anything else, so the prop was dead configuration surface that could silently misroute if ever passed `'ingredient'`/`'recipe'` (no id supplied, same 400 risk). Both `ProfileForm.tsx`/`AboutYouStep.tsx` stopped passing `entity="avatar"` accordingly.

**Why:** Requested directly (trash icon should clean up R2 everywhere, following the same principle already established for entity deletion above), and the two fix entries were real bugs/type-safety gaps caught by this round's own `code-reviewer` subagent pass before pushing.

**Not yet verified:** Same restriction as every entry above. Manual follow-up once deployed: remove an existing ingredient/recipe's photo (not the whole entity) and confirm the R2 object is gone after saving; remove and then cancel without saving, confirm the R2 object is untouched (still there, since the delete is deferred to a successful save); remove an avatar photo from `/profile`, save, confirm the R2 object is gone and `avatar_url` is actually null; remove an avatar from `/profile` and navigate away _without_ saving, confirm both the row and the R2 object are untouched; as a Google SSO user with a pre-filled avatar, remove it during onboarding and finish normally, confirm the profile's `avatar_url` ends up null (not still pointing at the pre-filled value) and its R2 object is gone.

---

## Meal-type log categorization and per-meal kcal targets (requested directly, no ticket number)

**Deviation:** `log_entries` gained an optional `meal_type` column (`supabase/migrations/20260905000000_log_entries_meal_type.sql`) — `text check (meal_type in ('breakfast', 'lunch', 'dinner', 'snack'))`, nullable, no default, reusing the plain-text-with-check-constraint pattern `profiles.goal_type` used before Ticket 5 converted it to a proper enum, rather than a new Postgres enum type — this column doesn't need `ingredient_unit`-style reuse across multiple tables the way that one did. `LogIngredientStep`/`LogRecipeStep` both gained a new shared `MealTypeSelector` (an optional exclusive `ToggleButtonGroup` — clicking the already-selected option again clears it back to `null`, read as "unset") so a log entry can optionally be tagged with a meal when it's created; `EditLogEntryDialog` got the same selector so an entry's meal can be corrected afterward. `DailyLog.tsx` (shared by `/log` and `/groups/:groupId/log`) now groups today's entries into Breakfast/Lunch/Dinner/Snack/Uncategorized sections instead of one flat list, each rendered only when it has entries.

**Deviation:** `MealType` (`'breakfast' | 'lunch' | 'dinner' | 'snack'`) was extracted into a new `src/types/meal.ts` (alongside `MEAL_TYPES`/`MEAL_TYPE_LABELS`) rather than living in `src/types/log.ts` where it was first added — the per-meal kcal target work below needs the same type on `profiles`, and putting it under the log feature's own type file would have made that a backwards cross-feature-into-log import. `types/log.ts` re-exports it (`export type { MealType }`) so no existing `import type { MealType } from '../../types/log'` call site needed to change.

**Deviation (requested directly — optional per-meal kcal breakdown of the daily target):** `profiles` gained `meal_breakdown_enabled boolean not null default false` plus nullable `breakfast_kcal_target`/`lunch_kcal_target`/`dinner_kcal_target`/`snack_kcal_target` numeric columns (`supabase/migrations/20260906000000_meal_kcal_breakdown.sql`) — four flat columns rather than one jsonb blob, matching the existing flat-column style for the rest of the calorie-target fields on this table, since the meal set is fixed. `complete_onboarding`'s RPC signature grew from 10 to 15 args (dropped and recreated, same as its earlier 5→10 growth in Ticket 5's own migration, with `search_path = public` preserved) to persist all five columns atomically with the rest of onboarding.

The shared `CalorieTargetStep` (used by both `OnboardingStepper`'s onboarding flow and Progress's `EditGoalDialog`) gained a "Break down by meal" switch, disabled until the current selection resolves to a valid daily total (`calorieCalc.ts`'s new `getResolvedDailyKcal`), and — when on — four per-meal fields (`MealBreakdownFields.tsx`) with a live "kcal left to allocate"/"kcal over" indicator. Each field is individually optional (blank counts as 0 toward the sum), but the four together must sum to exactly the resolved daily total for the switch to be considered valid (`isMealBreakdownValid`) — both `OnboardingStepper`'s step-advance gate and `EditGoalDialog`'s Save button are disabled otherwise. Turning the switch off in `EditGoalDialog` leaves the last-saved per-meal numbers in the database untouched rather than clearing them — `updateGoal` (`progress/api.ts`) only includes the four target columns in its update payload when the switch is on, so a caller who briefly disables and re-enables it gets their old breakdown back rather than starting blank.

**Deviation (requested directly — group log info card replaced with a per-member breakdown):** `/groups/:groupId/log`'s info card (previously "kcal logged today" for the group as a whole) was replaced with a new `GroupMemberKcalCard`, listing every member of the group with their own `logged today / their own daily target` and, for members who have the switch on, their own per-meal remaining/over amounts — a daily target is inherently personal, so a single aggregate number for the group didn't mean anything. This needed two new small data-fetching pieces: `useGroupMembers` (`src/features/groups/useGroupMembers.ts`), a single-slot shared cache for "this group's member list" mirroring `useMyGroups`' own shape (Ticket 16) — without it, `/groups/:groupId/log` being a BottomNav tab-root route that fully remounts on every navigation would refetch `group_members` on every tab switch, the exact bug `useMyGroups` was added to fix; and `fetchMemberKcalProfiles`/`useMemberKcalProfiles` (`features/profiles`), which batch-fetches just the columns this card needs (not a full profile row) for a list of member ids, mirroring the existing `fetchProfileNames`/`useProfileNames` shape and relying on the same "select own row or a fellow group member's row" RLS policy. `removeGroupMember` now also calls a new `invalidateGroupMembers()` so an already-mounted group log picks up a member's removal.

`getMealKcalTargets` (`types/profile.ts`, added alongside the migration above) was typed against `Pick<Profile, 'breakfast_kcal_target' | ...>` rather than the full `Profile`, specifically so `GroupMemberKcalCard`'s narrower `MemberKcalProfile` (from `fetchMemberKcalProfiles`, which doesn't select every profile column) can reuse it without needing to satisfy `Profile`'s full shape.

**Deviation (reverted — this session's own `code-reviewer` pass, before pushing):** A later request asked for `/groups/:groupId/log` and `/groups/:groupId/logs` to show every group member's own log entries regardless of context — i.e. query `log_entries` by `logged_by anyOf (group member ids)` instead of `group_id = groupId`, so a member's personal entries and other-group entries would show up too, not just what they logged into this specific group. Implemented, then reverted after the review pass found it couldn't actually work: `log_entries`' own `"read own or group log entries"` RLS policy (Ticket 2's migration) only grants visibility into a row where `logged_by = auth.uid()` OR `group_id` is one of the _caller's own_ groups — there is no clause granting visibility into a fellow member's personal or other-group entries. Compounding that, the sync engine (`src/sync/pull.ts`/`useSyncEngine.ts`) only ever pulls "my own personal entries" and "this one group's entries, any author" per scope — a fellow member's personal/other-group entries are never even written into the caller's local Dexie cache to query in the first place, regardless of RLS.

Net effect of the reverted version: other members' personal/other-group entries still never appeared (blocked by both RLS and sync scope), while the _viewer's own_ personal entries and other-groups' entries — already present locally — leaked into every group's log and per-meal breakdown unconditionally, since `logged_by = self` always matches. This also silently contradicted `supabase/README.md`'s existing signed-off QA criterion ("Confirm `/groups/<id>/log` still only shows that group's own entries"). Given the review flagged this needs explicit sign-off (properly supporting it would mean a new RLS policy letting any group-mate see _all_ of a caller's entries, including private personal ones — a real product/privacy decision, not just a query change), the choice was put back to the requester directly rather than decided unilaterally; reverting to the original `group_id = groupId` filter (both `fetchTodayLogEntries` and `fetchAllGroupLogEntries` in `src/features/logging/api.ts`, and their call sites in `DailyLog.tsx`/`AllTimeLog.tsx`) was chosen. `GroupMemberKcalCard`'s per-member membership list/kcal targets (previous deviation entry above) are unaffected by this revert — only which `log_entries` rows feed its "logged today" sums changed back.

**Deviation (fix, found during this session's own `code-reviewer` pass):** `entry.meal_type` is typed `MealType | null`, but a `log_entries` row cached in Dexie before this feature shipped — and never re-pulled since, because `pull.ts`'s incremental cursor only re-fetches rows past their `updated_at` — actually has `meal_type === undefined` at runtime on such a row, not `null`. The meal-sectioning code in `DailyLog.tsx` and the per-meal consumed-kcal sums in both `DailyLog.tsx` and `GroupMemberKcalCard.tsx` used strict `entry.meal_type === key`/`=== meal`, which a legacy `undefined` row matches neither a real meal nor the `null` "Uncategorized" bucket — such entries would silently vanish from the categorized list and from every per-meal total (while still counting toward the raw "logged today" total, which sums unconditionally). Fixed by normalizing every comparison to `(entry.meal_type ?? null) === key`.

**Deviation (fix, same pass):** `isMealBreakdownValid`'s `sumMealKcalTargets(targets) === dailyTotal` was a strict equality check on `Number()`-parsed form strings — a typed/pasted non-integer value could make the sum land a fraction of a kcal off the daily total purely from binary floating-point rounding, even when the real-arithmetic sum is exact, incorrectly blocking a save that should have been valid. Fixed by comparing `Math.round(sumMealKcalTargets(targets)) === Math.round(dailyTotal)` instead — consistent with every display of a kcal value elsewhere already rounding via `.toFixed(0)`.

**Why:** All of the above were requested directly, except the "reverted" entry (a real design/security question surfaced by review and resolved by asking rather than assuming) and the two "fix" entries (real bugs caught by this session's own `code-reviewer` subagent pass before pushing).

**Not yet verified:** Same database-connection restriction as every entry above (org policy: must not connect to databases) — neither new migration (`20260905000000_log_entries_meal_type.sql`, `20260906000000_meal_kcal_breakdown.sql`) has been pushed to a live project. Verification here was `tsc -b` and `oxlint`, both clean. Manual follow-up once pushed: log an entry with a meal picked and confirm it appears under the right section on `/log`; log one with no meal picked and confirm it appears under "Uncategorized"; turn on the meal breakdown switch during onboarding and in `EditGoalDialog`, confirm the four fields must sum exactly to the daily total before the step/save button enables; turn the switch off in `EditGoalDialog` without clearing the fields, save, then re-open and turn it back on, confirming the previous numbers are still there; as a member of a group with the switch on, confirm `/groups/:groupId/log`'s info card shows every member's own target and per-meal remaining amounts, and that it still only reflects entries actually logged into that group (not personal or other-group entries).

---

## Community pantry (requested directly, no ticket number)

**Deviation:** Added a third ingredient visibility tier alongside the existing personal/group model (`schema.md`'s "Ownership pattern" note) — a **community** ingredient, contributed by any user and readable by every authenticated user regardless of anyone's opt-in switch, editable/deletable only by its creator. Rather than a parallel table, this is `ingredients.is_community boolean not null default false` plus a `check (not is_community or group_id is null)` constraint, deployed by `supabase/migrations/20260907000000_community_pantry.sql`. This reuses the entire existing table/RLS/Dexie/outbox/sync machinery: a community row is, structurally, just a `group_id is null` personal-shaped row, which means the existing insert/update/delete policies' `(group_id is null and created_by = auth.uid())` branch _already_ grants exactly "creator-only write" with zero policy changes — only the **select** policy (`"read own or group ingredients"`) needed widening, with `or is_community = true`.

**Deviation:** `profiles.community_pantry_enabled` and `groups.community_pantry_enabled` (same migration) are the two opt-in switches — personal and per-group — gating whether community ingredients are pulled into that context's pantry list, recipe-ingredient picker, and log-entry picker. Both live directly on `/pantry`/`/groups/:id/pantry` (`PantryList.tsx`), each an immediate-save `Switch` (a `Paper` block styled like Profile's pre-existing Dark Mode switch, not a form field), not on `/profile` or the group settings screen — the requester tried folding these into `ProfileForm.tsx`/`GroupForm.tsx` first, then decided a pantry-screen switch that takes effect immediately reads better than one that only saves along with unrelated form fields the user wasn't necessarily changing. `setCommunityPantryEnabled` (`profiles/api.ts`) and `setGroupCommunityPantryEnabled` (`groups/api.ts`) are small standalone setters for this, each just a partial `.update(...)` plus the existing cache invalidation (`invalidateMyProfile`/`invalidateMyGroups`) — mirroring `updateMyProfile`/`updateGroup`'s own shape, but not routed through either full form. In group context the switch (and the toggle itself, via RLS's existing "owner manages group" policy) is owner-only — `PantryList.tsx` only renders it when the viewer's own membership role is `'owner'`, same reasoning as the settings-gear icon being owner-only elsewhere; a non-owner member sees the effect of the switch (community ingredients appearing or not) without being shown a control they couldn't use. `PantryList.tsx` holds a `pendingCommunityEnabled` local override so the switch reflects the toggle immediately rather than waiting on the profile/groups cache to re-fetch after invalidation. Community ingredients themselves are always pulled into Dexie for every signed-in user unconditionally (`sync/pull.ts`'s new `pullCommunityIngredients`, alongside the personal/group scopes in `useSyncEngine.ts`) regardless of either switch — the switches gate _usage_ (what shows in a pantry list, what's selectable when adding to a recipe or logging), not the underlying sync, since the new community-pantry browsing page (`/community-pantry`) must be able to list every community ingredient for every user, opted in or not.

**Deviation:** `IngredientCard.tsx` gained an `isCommunity` prop rendering a small "Community" chip, and now always resolves `creatorName` for a community ingredient (previously `creatorName` was fetched/shown in group context only — a community ingredient's creator is meaningful information regardless of which pantry list it's surfaced in). `IngredientDetail.tsx` derives `canEdit`/`isCommunity` from the loaded ingredient row itself (`ingredient.is_community`, `ingredient.created_by === userId`), not from the route's own `groupId` — a community ingredient can be reached from `/pantry`, any `/groups/:id/pantry`, or `/community-pantry` alike, and permission has nothing to do with which of those it was opened from. A non-creator viewing a community ingredient's detail gets a read-only summary (no editable form, no Delete button) instead of the normal `IngredientForm`; "Copy to…" stays available to everyone regardless of creator, per the requester's explicit choice to allow forking a community ingredient into one's own, independently-editable personal or group pantry row (reusing the existing `copy_ingredient` RPC — only its own source-access check needed widening, see below). `useCopyTargets`/`CopyIngredientDialog` needed one real client-side change for this: their existing "exclude whichever context the item is already in" logic is wrong for a community ingredient, which isn't actually "in" whichever personal/group screen it happened to be opened from — a new `isCommunitySource` flag skips that exclusion entirely when copying from one.

**Deviation:** `copy_ingredient`/`copy_recipe`'s source-access check (`20260903000000_copy_ingredient_recipe_rpcs.sql`'s own hardening, re-declared in the new migration rather than editing that file) gained an `or is_community = true` branch so copying _from_ a community ingredient is permitted for any caller — it doesn't widen what's readable (community ingredients are already select-able by everyone per the RLS change above), only what `copy_ingredient` itself, an explicit write action, will act on.

**Deviation (requested directly — delete-usage warning for a widely-shared ingredient):** `check_ingredient_usage` (`rpcs.md`) is a plain `stable` function, not `security definer` — it only ever sees recipes the caller can already read via their own RLS. For a community ingredient potentially used by other users' private recipes, that means a creator could get a false all-clear when deleting one that's actually in active use elsewhere. Rather than making the existing function privileged (which would leak other users' recipe names/existence to the deleting caller — a real exposure, not just a UX gap), added a second, `security definer` function, `check_community_ingredient_usage`, that returns only a total `count(*)` across every recipe referencing the ingredient, no names. `DeleteIngredientDialog.tsx` calls both for a community ingredient and shows them together — named recipes the caller can already see (from the existing function) plus the privileged total — so the caller learns "this affects more than you can see" without ever being shown a recipe belonging to someone they have no relationship to.

**Deviation (requested directly — navigation/UI polish):** `GroupsPage.tsx` and `CommunityPantryPage.tsx` (`/groups` and `/community-pantry`) gained a back arrow, falling back to `/pantry` — same fixed-path convention `SyncStatusPage.tsx`/`ProfilePage.tsx` already use for a header-icon-reachable screen with no single parent tab to derive a back path from; both had been missing one since neither was a form/detail screen with an obvious "back to what" answer at the time they were built. Separately, the community-pantry `Switch` on `PantryList.tsx` was shrunk (`size="small"`, 13px label, tighter `Paper` padding) and its container's padding increased to a flat `p: 1` (8px all around) — which surfaced a real bug: `FormControlLabel` ships with a default `margin-left: -11px` (meant to align a checkbox/radio's own padding with surrounding list text elsewhere in MUI), which was silently canceling out that padding and pinning the switch to the container's left edge regardless of how much padding was set. Fixed with an explicit `ml: 0` override on the `FormControlLabel`.

**Deviation (fix, found during this session's own `code-reviewer` pass, before pushing):** `check_community_ingredient_usage` (above) was `security definer` with no check that `p_ingredient_id` actually refers to a community ingredient — since it bypasses RLS by design, any authenticated caller could pass an arbitrary ingredient id (someone else's private personal ingredient, or another group's ingredient with no relationship to the caller) and learn its cross-user recipe-usage count, an information disclosure the existing RLS-scoped `check_ingredient_usage` deliberately can't produce. The client only ever calls it when `isCommunity` is true (`DeleteIngredientDialog.tsx`), but that's not an enforcement boundary — the RPC itself needed the check. Fixed by joining `ingredients` and requiring `is_community = true` in the `where` clause, so a non-community id now just returns 0 rather than leaking anything.

**Deviation (fix, same pass):** `CreateCommunityIngredientDialog.tsx` was a near-verbatim copy-paste of `CreateIngredientDialog.tsx` (including its `pendingId`-regeneration lifecycle comment, duplicated word-for-word) rather than parameterizing the existing dialog — the same drift risk `createIngredient` itself was already deliberately designed to avoid by taking `isCommunity` as a plain argument rather than needing a separate function. Removed; `CreateIngredientDialog` now takes an optional `isCommunity` prop (defaulting `groupId` to `null` and switching its title/submit label to "Add to community pantry"), and `CommunityPantryList.tsx` uses it directly.

**Why:** All of the above were requested directly (the community pantry feature itself; where its two opt-in switches live, including the later move off `ProfileForm`/`GroupForm` onto `PantryList.tsx`; allowing copy-from-community; the global-count-only usage check; and the back-arrow/switch-sizing polish), except the two "fix" entries immediately above (real issues caught by this session's own `code-reviewer` subagent pass after the initial push) — see the three-question exchange this session had with the requester before finalizing the plan (entry point placement on `/pantry`/`/groups/:id/pantry` rather than a header icon; allow-copy; count-only global usage check).

**Known limitation, not fixed here (flagged by the same review pass):** `sync/pull.ts` has no delete/tombstone handling for any table — a locally-cached row from a since-deleted server row simply lingers until some other write against it eventually fails. This predates this feature (it already applied to a group's own ingredients/recipes for its members), but `pullCommunityIngredients` widens its blast radius from "a group's own members" to "every signed-in user of the app": a deleted or un-community'd ingredient can now linger in _every_ user's local `/community-pantry` list, recipe-ingredient picker, and log picker indefinitely. Properly fixing this means designing soft-delete/tombstone propagation for the sync layer generally, a real architectural change out of scope for this feature — noted here rather than silently accepted, matching the review's own framing ("worth a follow-up... rather than leaving it purely implicit"). **Fixed below (Delete-sync reconciliation).**

**Deviation (requested directly — card indicator redesign):** `IngredientCard.tsx`'s community indicator changed from an inline `Chip` next to the ingredient name to a `2px` card border in `secondary.main` plus a small tab overlapping the card's own top-left corner reading "Community", in the same color as the border — reads as a property of the whole card rather than its title. The card also gained a `showCommunityIndicator` prop (default `true`) so a list made up entirely of community ingredients can suppress it as pure noise — `CommunityPantryList.tsx` passes `false`; `PantryList.tsx` (which mixes community rows into a personal/group list) leaves it at the default. `CommunityPantryList.tsx`'s and `PantryList.tsx`'s own `Stack spacing` was bumped from `1.5` to `1.75` to give the tab (which pokes `top: -9`) clearance from the row above at tighter spacing.

**Deviation (requested directly — extend the same indicator to recipe ingredient lines):** `RecipeIngredientsList.tsx`'s `RecipeIngredientRow` gained the identical border+tab treatment for a community ingredient used in a recipe, so a recipe's own ingredient list surfaces the same signal `IngredientCard.tsx` already does. This needed `is_community` plumbed one level further than it previously went: `RecipeIngredientDetail` (`types/recipe.ts`) gained an `is_community: boolean` field, `fetchRecipeIngredients` (`recipes/api.ts`) now selects and maps it from the joined `ingredients` row, and `RecipeDetail.tsx`'s `handleAddIngredient` now carries it over from the picked `Ingredient` when staging a new draft row (both places a `RecipeIngredientDetail` is constructed). `RecipeIngredientsList.tsx`'s own `Stack spacing` was bumped `1.25` → `1.5` for the same top-overhang clearance reason as above.

**Not yet verified:** Same database-connection restriction as every entry above (org policy: must not connect to databases) — `20260907000000_community_pantry.sql` (including the `check_community_ingredient_usage` fix above) has not been pushed to a live project. Verification here was `tsc -b` and `oxlint`, both clean; see `supabase/README.md`'s new section for the full manual QA checklist once a human pushes this migration.

---

## Ingredient/recipe detail actions menu (requested directly, no ticket number)

**Deviation:** `IngredientDetail.tsx` and `RecipeDetail.tsx` each replaced their two standalone bottom action buttons ("Copy to…"/"Delete ingredient" and "Copy to…"/"Delete recipe") with a single `MoreVert` icon-button menu containing "Copy" and "Delete" — `IngredientDetail.tsx`'s button sits top-right of the ingredient card (editable form or, for a non-creator viewing a community ingredient, the read-only summary); `RecipeDetail.tsx`'s sits top-right of the photo-thumbnail row rather than its fields `Paper`, per the requester's explicit placement call. All previously-existing gating carried over unchanged: `IngredientDetail.tsx`'s Delete item is still hidden unless `canEdit` (community-ingredient creator-only permission, see "Community pantry" above), Copy remains available regardless; `RecipeDetail.tsx`'s Copy item still carries `disabled={isDirty}` (Ticket 14 — copying always uses the last-persisted version, so an unsaved edit copying silently would be surprising).

**Deviation (fix, found during this session's own `code-reviewer` pass, before pushing):** `IngredientDetail.tsx`'s Delete menu item's icon was missing `fontSize="small"`, rendering at MUI's default 24px next to the Copy item's 20px in the same menu — a visible size mismatch, and inconsistent with `RecipeDetail.tsx`'s equivalent icon, which already had it. Fixed to match (`fontSize="small"` on both files' Copy and Delete icons; Copy in `text.primary`, Delete in `error`, Delete's label text in `error.main`).

**Why:** Requested directly (consolidate the two standalone buttons into a menu; the recipe menu's placement next to the photo rather than the fields card). The icon-size fix was caught by this session's own `code-reviewer` subagent pass before pushing.

**Not yet verified:** No database change in this entry — pure client-side UI. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: open an ingredient/recipe detail, confirm the menu opens/closes correctly (item click, backdrop click, Escape) and both actions still work and still respect the same permission/dirty-state gating the removed buttons had.

---

## IngredientDetail layout parity with RecipeDetail (requested directly, no ticket number)

**Deviation:** `IngredientDetail.tsx` no longer reuses `IngredientForm` for editing — it now inlines its own Name/Quantity+Unit/Kcal fields and stages them client-side (`name`/`quantity`/`unit`/`kcal`/`photoUrl`/`pendingPhotoFile` local state, diffed against a `savedIngredient` baseline), mirroring exactly how `RecipeDetail.tsx` has never reused `RecipeForm` for its own edit path. `IngredientForm` becomes create-only as a result (used only by `CreateIngredientDialog.tsx`/`AddRecipeIngredientDialog.tsx`'s "New ingredient" step) — the same role `RecipeForm.tsx` already had before this change, so the two entity types are now symmetric: a shared *Form component for create, an inlined staged-draft page for edit. This gives `IngredientDetail.tsx` the same section order and interaction model `RecipeDetail.tsx` already had: photo row (with the actions menu pinned to its top-right corner) → metadata → live stat tiles (kcal, kcal per unit — computed from the draft fields, not the last-saved row, so they update as the user types) → fields card (or the read-only summary for a non-creator viewing a community ingredient) → actions menu → an explicit "Save changes" button disabled until the draft is both valid and dirty. Behavior is otherwise unchanged — saves still go through `updateIngredient`'s existing Dexie+outbox path — just staged locally until Save is clicked instead of submitting immediately, matching Recipe's existing pattern.

**Deviation (requested directly — detail-page photo size):** The photo shown on `IngredientDetail.tsx`/`RecipeDetail.tsx` (both the editable `DeferredPhotoUpload` and, for a read-only community-ingredient view, `PhotoThumbnail`) was bumped from `size={120}` to `size={200}`. Separately, `IngredientForm.tsx`/`RecipeForm.tsx` (the create-only dialogs) had their own photo picker bumped from `size={120}` to `size={180}` — kept slightly smaller than the detail pages' since a create dialog has less room than a full-page detail view.

**Why:** Both requested directly (first "make IngredientDetail's UI mimic RecipeDetail's overall layout", then a follow-up photo-size increase applied consistently across both create and detail surfaces).

**Not yet verified:** No database change in this entry — pure client-side UI/architecture. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: edit an existing ingredient (with and without a photo), confirm Save is disabled until something actually changes and re-enables correctly after a save; remove a photo and Save, confirm the R2 object is deleted the same way `RecipeDetail.tsx`'s save handler already does; view a community ingredient you didn't create and confirm the read-only summary (no Save button, no editable fields) still renders correctly at the new photo size.

---

## AddLogEntryDialog ingredient autocomplete fix (requested directly, no ticket number)

**Deviation (fix — reported directly as "options seem to get duplicated and matching items which don't match what I typed"):** The ingredient `Autocomplete` in `AddLogEntryDialog.tsx` was missing `getOptionKey`, unlike the recipe `Autocomplete` immediately below it, which already had one. Without it, MUI falls back to keying each rendered option by its label text; since `formatIngredientLabel` renders `"<quantity> <unit> <name>"`, two ingredients sharing quantity/unit/name (easy to hit once community/group ingredients are mixed into one cross-context list) collided on the same React key, causing options to visually swap/duplicate while filtering. Fixed by adding `getOptionKey={(option) => option.id}`, matching the recipe branch. Also added a name-only `filterOptions={createFilterOptions({ trim: true, stringify: (option) => option.name })}` (again matching the recipe branch already there) so a typed number can't spuriously match against the quantity/unit prefix instead of the ingredient's actual name.

**Why:** Reported directly as a bug. Not fixed on the recipe `Autocomplete`'s sibling in `AddRecipeIngredientDialog.tsx`'s `ExistingIngredientForm` (also missing `getOptionKey`, same latent risk if two ingredients share a name) — flagged to the requester but left alone since it wasn't part of what was reported.

**Not yet verified:** No database change in this entry — pure client-side UI. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: with two ingredients that share the same name/quantity/unit (e.g. a personal one and a community one), open "Log an entry", type a filter that matches both, and confirm both appear distinctly and stay stable while typing further.

---

## Logging step header + rate indicator (requested directly, no ticket number)

**Deviation:** `LogIngredientStep.tsx`/`LogRecipeStep.tsx` each gained a "kcal per unit"/"kcal per gram" rate line, shown above the quantity-eaten field so the user can judge an amount before typing anything (previously only the _resulting_ total kcal was shown, below the field, after entering a quantity). This was then restructured into a name-prominent header: the item's own name at `fontSize={18} fontWeight={500}`, with the rate as a `fontSize={13}` subtitle directly below it — replacing the previous single line that showed `formatIngredientLabel`/`formatRecipeLabel`'s `"<quantity> <unit> <name>"` string. `formatIngredientLabel`/`formatRecipeLabel` are consequently no longer used in either file.

**Why:** Requested directly, in two steps (first the rate indicator, then the header restructure).

**Not yet verified:** No database change in this entry — pure client-side UI. Verification was `tsc -b` and `oxlint`, both clean.

---

## Shared kcal-per-unit helper and app-wide `.toFixed(2)` normalization (requested directly, no ticket number)

**Deviation:** New `src/lib/kcal.ts` exports `kcalPerUnit(kcal, quantity)` (safe divide — `quantity <= 0` resolves to `0` rather than `Infinity`/`NaN`) and `formatKcalPerUnit(kcal, quantity)` (the same, `.toFixed(2)`-formatted). These replace a `quantity > 0 ? kcal / quantity : 0` calculation that had been independently duplicated across `IngredientCard.tsx`, `IngredientDetail.tsx`, `LogIngredientStep.tsx`, `LogRecipeStep.tsx`, `RecipeCard.tsx`, `RecipeDetail.tsx`, `RecipeIngredientsList.tsx`, and `CopyRecipeDialog.tsx` — call sites needing the raw number for further math (scaling by a quantity eaten, diffing against another item's rate) use `kcalPerUnit`; everything else uses `formatKcalPerUnit` directly. `features/copy/api.ts`'s `IngredientMatch.kcal_per_unit` is unchanged — it's a rate already computed server-side (`find_ingredient_match`), not a client-side `kcal`/`quantity` pair, so there's nothing for the new helper to compute there.

**Deviation (requested directly — display consistency):** Every rendered kcal value app-wide was normalized to `.toFixed(2)`, replacing what had become an inconsistent mix of `.toFixed(0)`, `.toFixed(2)`, and a few entirely unformatted raw numbers (e.g. `IngredientCard.tsx`'s total-kcal figure, `IngredientDetail.tsx`'s "kcal" stat tile, onboarding's calorie-preset display). This touches ingredient/recipe totals, log entry/day/all-time totals, per-meal remaining/over amounts, daily and per-meal kcal targets, and onboarding calorie presets — every one of these now reads `.toFixed(2)`, including whole-number-by-convention fields like `daily_kcal_target` (so, e.g., a 2000 kcal/day target now displays as "2000.00"). Controlled `TextField` `value=` bindings (e.g. the Kcal field on `IngredientDetail.tsx`'s own form) were deliberately left untouched — those are live-typed strings, not display values, and forcing `.toFixed(2)` on them would fight the user's own typing.

**Why:** Both requested directly.

**Not yet verified:** No database change in this entry — pure client-side formatting. Verification was `tsc -b` and `oxlint`, both clean.

---

## "Move to community" ingredient action (requested directly, no ticket number)

**Deviation:** `IngredientDetail.tsx`'s actions menu gained a "Move to community" item (a `GroupsIcon`, positioned above "Delete"), shown only when a new `canMoveToCommunity = !isCommunity && ingredient?.created_by === userId` is true — the ingredient's _creator specifically_, not `canEdit`'s broader "any group member may edit a group ingredient" rule. Clicking it opens a new `MoveIngredientToCommunityDialog.tsx` (patterned after the existing simple-confirm dialogs, e.g. `CopyIngredientDialog.tsx`, rather than `DeleteIngredientDialog.tsx`'s usage-check variant — moving to community has no analogous "used in N recipes" check to run first) warning that the move can't be undone. Confirming calls a new `moveIngredientToCommunity(id, userId)` in `pantry/api.ts`, which sets `group_id: null` and `is_community: true` in the same Dexie update + outbox-enqueued patch (both fields together, satisfying the `ingredients_community_no_group` check constraint in one write rather than risking a transient state with only one set), plus `updated_at`/`updated_by` the same way `updateIngredient` already stamps them. `handleMoveToCommunity` deliberately does _not_ re-baseline the staged draft fields (name/quantity/unit/kcal/photo) from the returned row — those aren't part of this patch, so doing so would silently discard an unsaved in-progress edit; the live Dexie query (`ingredient`) already reflects the new `is_community`/`group_id` reactively, which is all `canEdit`/`isCommunity`/`ItemMetadata` need.

**Deviation (fix, found during this session's own code-review pass, before pushing):** The menu item was initially gated by `canEdit && !isCommunity`, which — since `canEdit` is `!isCommunity || created_by === userId` — is true for _any_ group member on a group ingredient they didn't create, not just its creator. That's fine for Delete (the "delete own or group ingredients" RLS policy really is group-wide), but wrong here: the "update own or group ingredients" policy has no separate `with check`, so its `using` clause doubles as the check against the _new_ row too, and moving to community sets `group_id` to null while `created_by` stays whoever originally created it — the post-update check (`group_id is null and created_by = auth.uid()`) only passes when the mover _is_ that original creator. A non-creator group member triggering the old gating would have the Dexie write succeed optimistically (apparent success in the UI) while the outbox push to Supabase was permanently rejected by RLS, permanently diverging local and server state. Fixed by narrowing the gate to `canMoveToCommunity` (creator-only) as described above.

**Why:** Requested directly. The fix was requested implicitly, in the sense that a review was requested before pushing and this was a real correctness bug it caught, not a product decision.

**Not yet verified:** No database/migration change in this entry — pure client-side, reusing the existing RLS policy and outbox path. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: as the ingredient's own creator, open a personal ingredient's detail page and a group ingredient you created, use "Move to community" on each, confirm, and check the Table Editor that `group_id` is now null and `is_community` is true on the same row (not a new row); confirm the ingredient now appears on `/community-pantry` and is gone from its original pantry list; as a _different_ group member who did not create a given group ingredient, open its detail page and confirm the "Move to community" item is absent from the menu entirely; confirm the actions menu on an already-community ingredient never shows "Move to community"; separately, stage an unsaved edit (e.g. type a new Kcal value without hitting Save), use "Move to community", confirm, and confirm the typed value is still there afterward (not reverted) and the Save button is still enabled for it.

---

## PWA update prompt (requested directly, no ticket number)

**Deviation:** Previously, `vite.config.ts`'s `VitePWA` plugin was configured with `registerType: 'autoUpdate'` and left the default auto-injected `<script>` (a bare `navigator.serviceWorker.register('/sw.js')` on `window load`) as the only registration — no code ever called `registration.update()`, so an installed PWA left open in a single window (the normal way a standalone app is used) had no way to notice a new deploy short of the browser's own infrequent update heuristics. Fixed by switching to `registerType: 'prompt'` + `injectRegister: false` in `vite.config.ts`, and registering the service worker manually via `virtual:pwa-register/react`'s `useRegisterSW` in a new `src/pwa/UpdatePrompt.tsx`, mounted as a sibling of `<App />` in `main.tsx` (inside `ThemeProvider`/`LocalizationProvider`, so its `Snackbar` picks up the app theme, but outside `App`'s own auth/onboarding gating, so it applies regardless of login state). `UpdatePrompt` polls `registration.update()` every hour (`UPDATE_CHECK_INTERVAL_MS`) for as long as the app stays open, and shows a persistent, non-blocking `Snackbar` ("A new version of Forklore is available") with a "Reload" button once a new service worker is waiting; clicking it calls `updateServiceWorker(true)`, which posts `SKIP_WAITING` to the new worker and reloads once it takes control. A close (X) button dismisses the snackbar without reloading.

**Why:** Requested directly — "make sure users who installed this as a PWA can always get the new version whenever it is deployed". `registerType: 'autoUpdate'`'s normal behavior is a _forced, silent_ reload the instant a new service worker activates (no prompt), which was explicitly rejected in favor of a user-actioned prompt: this app has several forms with unsaved local draft state (`IngredientDetail.tsx`/`RecipeDetail.tsx`'s staged-draft-vs-`savedX`-baseline pattern, log entry dialogs), and a surprise reload mid-edit would silently drop that in-progress input.

**Not yet verified:** No database change in this entry — pure client-side PWA/build config. Verification was `tsc -b`, `oxlint`, and a production build (`npm run build:dev`), confirming `dist/index.html` no longer carries the old auto-injected `registerSW.js` script tag and `dist/sw.js` only calls `self.skipWaiting()` inside its `SKIP_WAITING` message handler (not unconditionally on install, which is what `registerType: 'autoUpdate'` had produced). Manual follow-up: install the app as a PWA, deploy a change, wait for (or trigger) an update check, confirm the reload snackbar appears and that clicking "Reload" picks up the new version (check the footer's `__GIT_HASH__`/`__APP_VERSION__` on `ProfilePage`); confirm dismissing via the close button doesn't reload and doesn't error; confirm a fresh install/first load still gets `onOfflineReady`-style precaching with no snackbar shown.

**Deviation (follow-up, requested directly — manual check button):** The hourly poll above only covers an app instance that's actually running; one left backgrounded (suspended, not fully closed) for longer than that can miss ticks since browsers throttle timers in that state. Added a "Check for updates" button to `Profile.tsx`, directly below the dark mode toggle, giving users an explicit way to check without waiting on the poll or fully closing/reopening the app. Since a second `useRegisterSW()` call in `Profile.tsx` would register a second, independent Workbox instance against the same service worker — duplicating `waiting`/`installed` listeners and risking two separate reload prompts for the same update — the single registration `UpdatePrompt.tsx` already owns is instead shared via a new `src/store/usePwaUpdateStore.ts` (Zustand, alongside `useAppStore`/`useSyncStore` — see frontend-architecture.md "Zustand stores"): `UpdatePrompt` mirrors its `registration` and `needRefresh` state into the store on registration/change; the button's `handleCheckForUpdate` calls a new `checkForPwaUpdate()` helper (reads the shared `registration`, calls `.update()`, tracks a `checking` flag the button uses for its disabled/"Checking…" state) and, if `needRefresh` is still false once that resolves, shows its own "You're on the latest version" snackbar — deliberately skipped when `needRefresh` is already true, since `UpdatePrompt`'s root-level reload snackbar is already showing in that case and a second message would be redundant. This "up to date" read is a best-effort snapshot, not a guarantee: `registration.update()`'s promise can resolve before a newly-found update finishes installing (installing means re-fetching/precaching every asset, which can take a few seconds), so on a slow connection the button could briefly report "up to date" moments before the real reload prompt appears on its own — accepted as a reasonable tradeoff over adding an artificial wait with its own race window.

**Why:** Requested directly — "it might be good to have a manual check as well" for the case of "what happens if the user... hasn't opened the app for a while", i.e. the exact gap the hourly-poll-while-open design doesn't cover.

**Not yet verified:** No database change — pure client-side. Verification was `tsc -b` and `oxlint`, both clean, plus a production build. Manual follow-up: with the app running an old deployed version, click "Check for updates" on `/profile` and confirm it shows "Checking for updates…" while in flight; confirm it then shows either the root-level "Reload" snackbar (update found) or "You're on the latest version" (none found), never both; confirm clicking it repeatedly doesn't spawn additional service worker registrations (DevTools → Application → Service Workers should show exactly one).

---

## Delete-sync reconciliation (requested directly, no ticket number — closes the "Community pantry" known limitation above)

**Deviation:** `sync/pull.ts`'s cursor-based pull (`select * where <cursorColumn> > lastSyncedAt`, merged via `bulkPut`) can only ever add or refresh rows — a row another user hard-deleted server-side never appears in that response, so it lingered in every other user's Dexie cache indefinitely (the "Known limitation, not fixed here" note under "Community pantry" above, and the underlying reason a deleted ingredient/recipe "lingers on the other user's session" reported directly). Fixed with a reconciliation pass rather than a tombstone table or Realtime subscription (both considered and rejected as bigger architectural changes than this bug warranted — see the three-option discussion this session had with the requester before implementing): each `pullTable()` call now also runs a new `reconcileDeletes()` (`sync/pull.ts`), which fetches just the `id` column for every row currently in that scope (cheap — no new columns, no cursor gating, since a delete doesn't bump any surviving row's `updated_at` for a cursor to key off), diffs it against the same scope's rows already in Dexie, and deletes any local row whose id is no longer present server-side. `pullCommunityIngredients()` got the same treatment as a fourth, separate case (its own `is_community = true` scope doesn't fit the generic per-owner/per-group shape, and `is_community` isn't an indexed Dexie field, so its local side filters in JS via `.filter()` instead of `.where()`).

This runs on every pull (every 60s, per `useSyncEngine.ts`'s `PULL_INTERVAL_MS`) for `ingredients`, `recipes`, and `log_entries`, in both personal and group scope, plus the community-ingredients pull — i.e. every table `pull.ts` actually caches in Dexie today. `groups`/`group_members` were explicitly scoped out: neither is Dexie-cached at all (`features/groups/api.ts` reads both live from Supabase on every call, per Ticket 11's own deviation entry above), so there's no stale local copy for a delete-reconciliation pass to have anything to reconcile — a delete there is already reflected correctly the next time either screen queries Supabase. Making `groups`/`group_members` Dexie-cached first (reversing that Ticket 11 decision) was raised and explicitly declined in favor of scoping this fix to the tables that actually have the bug.

**Deviation (fix, found during this session's own `code-review` pass, before pushing):** The first draft's `reconcileDeletes()` treated "id absent from the full server-scope id set" as "deleted by another user" — but a row can be absent from that set simply because its own optimistic insert hasn't reached Supabase yet (mid-backoff in `outbox.ts`'s retry chain, parked `waiting_for_connectivity`, or `failed`), since only the initial-mount pull awaits `drainPendingOutbox()` (`useSyncEngine.ts`) — a periodic (60s) or `online`-triggered pull does not. Concretely reachable, not hypothetical: create a recipe/ingredient/log entry (or move an ingredient to the community pantry) while offline or slow enough that the outbox write is still in flight when the next pull fires, and reconciliation would delete the just-created local row out from under the user before their own write ever synced. Fixed by adding `pendingOutboxIds()`, which reads every outbox item (any status) for the table and excludes those payload ids from the stale-candidate set before deleting — `reconcileDeletes` and `pullCommunityIngredients` both route through a new shared `deleteStaleLocalRows()` helper that applies this exclusion (and uses Dexie's `bulkDelete` instead of one `.delete()` call per stale id, also flagged by the same review pass).

**Why:** Requested directly, after being asked to explain why deletes don't propagate to other users' sessions the way creates/updates do. Three fix approaches (this id-diff reconciliation, a tombstone table + trigger, a Supabase Realtime subscription) were presented with tradeoffs; this one was chosen as the smallest change that fixes the reported symptom without a migration, matching the poll-based model the rest of `frontend-architecture.md` already documents. The fix entry above was a real correctness bug caught by this session's own `code-review` pass before pushing, not a product decision.

**Not yet verified:** No database/migration change in this entry — pure client-side, reusing the existing RLS-scoped `select` queries `pull.ts` already made (just projecting `id` instead of `*`, with the cursor filter dropped). Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: with two accounts sharing a group, have one delete a group ingredient/recipe/log entry and confirm it disappears from the other account's session within one pull cycle (≤60s, or immediately after a manual `online` event/reload) without needing a logout; same check for a personal ingredient/recipe entry deleted on a second device signed into the same account; same check for a community ingredient deleted (or un-community'd) by its creator, confirmed gone from a different user's `/community-pantry` list, recipe-ingredient picker, and log picker; confirm a delete of one record doesn't affect unrelated cached rows in the same scope (e.g. deleting one of three group ingredients only removes that one locally).

---

## Optional ingredient brand field (requested directly, no ticket number)

**Deviation:** `ingredients` gains a new nullable `brand text` column (`supabase/migrations/20260908000000_ingredient_brand.sql`), added to `types/ingredient.ts`'s `Ingredient` and `pantry/api.ts`'s `IngredientInput`. `IngredientForm.tsx` (create — Pantry's FAB dialog and `AddRecipeIngredientDialog.tsx`'s "New ingredient" step, which both already reuse this component) gained a plain optional "Brand" `TextField` between Name and the Quantity/Unit row. `IngredientDetail.tsx`'s inline edit path (which does not reuse `IngredientForm` — see this file's own "recurring pattern" note in CLAUDE.md) got the same field staged in local state (`brand`, seeded from `ingredient.brand ?? ''` in `applyIngredientBaseline`), folded into the existing dirty-check and `updateIngredient` call, and also shown (when non-empty) in the read-only branch a non-creator sees on a community ingredient. Both forms submit `null` for a blank/whitespace-only brand, never `''`, so an unset brand round-trips the same way as `photo_url`'s existing null-vs-unset handling.

`copy_ingredient` (`rpcs.md`, both its `20260903000000` original and `20260907000000`'s community-pantry widening) explicitly lists the columns it copies rather than `select *`, so `brand` was added to that column list too in the same migration — otherwise a copied ingredient would silently lose its brand. `find_ingredient_match` was deliberately left untouched: it matches on name+unit only (per its own existing doc comment), and brand isn't part of that rule — two identically-named, identically-unit'd ingredients with different (or missing) brands still surface as a match, same as before this change.

**Why:** Requested directly — "update the ingredients table so it accepts an optional brand column."

**Not yet verified:** New migration (`20260908000000_ingredient_brand.sql`, unpushed per this repo's standing "must not connect to databases" policy — see CLAUDE.md). Verification so far was `tsc -b` and `oxlint`, both clean. Manual follow-up once pushed: create a new ingredient with a brand via the Pantry FAB dialog and via a recipe's "Add ingredient → New ingredient" step, confirm the Table Editor's `brand` column has the typed value on both; create one with the Brand field left blank, confirm the column is `null` (not `''`); open an existing ingredient's detail page, add/change/clear a brand and hit "Save changes," confirm it persists and that clearing it back to blank sets the column to `null`; copy an ingredient that has a brand set (`CopyIngredientDialog`) into another personal/group/community context and confirm the new row keeps the same brand; as a non-creator viewing a community ingredient's read-only detail page, confirm its brand (when set) is shown under the name.

---

## IngredientCard subtitle: brand replaces "Added by" (requested directly, no ticket number)

**Deviation:** `IngredientCard.tsx`'s subtitle previously appended `· Added by {creatorName}` in group/community context (Ticket 12's "quantity+who added it" pattern from `design-system.md`). Requested directly, following the "Optional ingredient brand field" entry above, to instead show the ingredient's brand there: `IngredientCard` dropped its `creatorName` prop entirely and now appends `· {ingredient.brand}` (when set) to the quantity/unit subtitle, unconditionally (not gated on group/community context — a brand is a property of the ingredient itself, not tied to who added it). `PantryList.tsx` and `CommunityPantryList.tsx` both had their now-dead `useProfileNames`-based `creatorNames` computation and the `creatorName` prop pass-through removed, since `IngredientCard` no longer reads it. `IngredientDetail.tsx`'s fuller `ItemMetadata` block (creator/updater name + dates, gated on `groupId`/`isCommunity`) is untouched — this only affects the compact list-card subtitle. `RecipeCard.tsx`'s own separate `creatorName` prop (recipes have no brand field) is also untouched.

**Why:** Requested directly, following the "Optional ingredient brand field" addition above — "remove the added by text and replace it with the brand."

**Not yet verified:** No database/migration change in this entry — pure client-side. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: on a group pantry or `/community-pantry` list, confirm a card for an ingredient with a brand set shows `· <brand>` instead of `· Added by <name>`; confirm a card for an ingredient with no brand shows just the quantity/unit (no trailing `· `); confirm `IngredientDetail.tsx`'s fuller creator/updater metadata block is unaffected.

---

## Ingredient brand shown on recipe ingredient rows (requested directly, no ticket number)

**Deviation:** `RecipeIngredientDetail` (`types/recipe.ts`) gained a `brand: string | null` field, alongside its existing name/unit/kcal/quantity/is_community shape inherited from the linked ingredient. `fetchRecipeIngredients` (`features/recipes/api.ts`) selects `ingredients.brand` in its join and maps it through; `RecipeDetail.tsx`'s `handleAddIngredient` (the optimistic in-memory add, before the page-level Save) copies it from the `Ingredient` the same way it does `name`/`unit`/etc. `RecipeIngredientsList.tsx`'s per-row subtitle, previously just `{kcalContribution.toFixed(2)} kcal`, now appends `· {item.brand}` when set — same `· ` separator convention as `IngredientCard.tsx`'s brand subtitle.

Also, that row's remove `IconButton`'s `DeleteOutlineIcon` now has `color="error"` (previously unstyled/inherited) — requested directly, matching the red delete affordance already used elsewhere (e.g. `IngredientDetail.tsx`'s actions-menu "Delete" item).

**Why:** Requested directly, following the earlier ingredient-brand work — "add it on RecipeIngredientsList beside the kcal count" and "change the Trash icon color to red."

**Not yet verified:** No database/migration change in this entry — reuses the `ingredients.brand` column added by the "Optional ingredient brand field" entry above. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: open a recipe with at least one ingredient that has a brand set, confirm its row shows `· <brand>` next to the kcal figure; add a new ingredient with a brand from "Add ingredient → New ingredient" and confirm the freshly-added row shows it immediately (before Save); confirm a row for an ingredient with no brand shows just the kcal figure; confirm the row's remove (trash) icon renders in red.

---

## Log entry dialog autocomplete options mimic RecipeCard/IngredientCard layout (requested directly, no ticket number)

**Deviation:** `AddLogEntryDialog.tsx`'s ingredient/recipe `Autocomplete`s previously rendered each dropdown option as a plain two-line `Stack` (combined "quantity+name"/"weight+name" label on top, group label below) with no thumbnail or kcal figure. Requested directly to match `RecipeCard.tsx`/`IngredientCard.tsx`'s card layout instead: both `renderOption`s are now a flex row — `PhotoThumbnail` on the left, name (title) + a subtitle line (quantity/unit or weight_g, the same `groupLabel` helper, and for ingredients the brand when set) in the middle, and the item's kcal total + `formatKcalPerUnit` rate right-aligned, same as the two card components. `getOptionLabel` (`formatIngredientLabel`/`formatRecipeLabel`, `formatItemLabel.ts`) is untouched — it still drives the text field's post-selection display value and the default filter's stringification, which is a separate concern from how each dropdown row renders. Unlike the two card components, the option row has no `bgcolor`/`boxShadow`/`borderRadius`/community-tab styling — this is a plain `<li>` inside MUI's Autocomplete popper, and adding card-level elevation there would visually conflict with the popper's own hover/selected-state styling.

**Why:** Requested directly — "update the layout of the items in the autocomplete so it mimics the layout of RecipeCard.tsx."

**Deviation (follow-up, requested directly — quantity+unit beside the name):** The ingredient option's quantity+unit (previously in the subtitle line, `"{quantity} {unit} · {group}"`) moved up beside the name in the title row instead — a muted (`text.secondary`) `Typography`, same `fontSize={14}` as the (bold) name, in a `flex`/`alignItems: baseline` row. The name keeps `noWrap` (ellipsis-truncates on a long name) while the quantity+unit gets `flexShrink: 0`, so a long ingredient name truncates instead of ever pushing the quantity/unit out or clipping it. The subtitle line below now reads just `"{group}{brand}"`. The kcal-per-unit figure on the right (`.../{option.unit}`) still shows the unit there too, unchanged.

**Deviation (follow-up, requested directly — same treatment on the recipe option):** The recipe option's `weight_g` (previously in its subtitle line, `"{weight_g} g · {group}"`) got the identical treatment — moved beside the name in the same `flex`/`alignItems: baseline` row, same muted `fontSize={14}` `Typography`, `flexShrink: 0`, name `noWrap`. Its subtitle now reads just `"{group}"` (a recipe option never showed a brand line, so there's nothing else left in it).

**Deviation (follow-up, requested directly — drop the thumbnail):** Both options' `PhotoThumbnail` was removed entirely (the `<Box component="li">` row now starts directly with the name/quantity/group text block, no leading image tile) — the `PhotoThumbnail` import was dropped from the file as unused.

**Why:** Requested directly — "move the unit so that it is beside the ingredient name but the text should be subtle and it should not be cut off if the ingredient name is long" (confirmed, via a clarifying question, to target this same autocomplete's ingredient option row rather than `IngredientCard.tsx` or `RecipeIngredientsList.tsx`'s row), then "include the quantity," "make the font size the same as the ingredient name," "do the same for the autocomplete for the recipe," and finally "remove the photo from both" as follow-ups.

**Not yet verified:** No database change — pure client-side. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: open "Log an entry," confirm the Ingredient dropdown's options show no thumbnail — just a name + quantity/unit (same size, muted, beside the name) title row, a group (+ brand, for a branded ingredient) subtitle, and kcal/kcal-per-unit on the right; switch to Recipe and confirm the same shape (no thumbnail, weight_g in grams beside the name instead of quantity/unit, group-only subtitle); confirm selecting an option still fills the text field with the existing "1 sachet Milo"/"100g Fried Chicken" label format, unchanged; type/select an ingredient or recipe with a very long name and confirm the name truncates with an ellipsis while the quantity/weight next to it stays fully visible, never cut off.

---

## Shared IngredientAutocompleteOption component (requested directly, no ticket number)

**Deviation:** The ingredient option row built up across the entries above (name + quantity/unit beside it, an optional second line, kcal/kcal-per-unit right-aligned, no thumbnail) is now a standalone component, `src/features/pantry/IngredientAutocompleteOption.tsx` — takes `liProps` (the `{ key, ...props }` MUI's `renderOption` hands back, minus `key` itself, which stays on the component instance the way any mapped/rendered-in-a-callback element needs it), `ingredient: Ingredient`, and an optional `subtitle: string` for whatever second line the caller wants (or none). `AddLogEntryDialog.tsx`'s ingredient `Autocomplete` now calls it with `subtitle={groupLabel(...) + (brand ? ' · ' + brand : '')}` (unchanged output from before this refactor). `AddRecipeIngredientDialog.tsx`'s `ExistingIngredientForm` — the recipe detail page's "Add ingredient → From pantry" step, previously a bare `getOptionLabel`-only `Autocomplete` with no custom `renderOption` at all — gained the same row via a new `renderOption`. Also added `getOptionKey` there to match the pattern already used by `AddLogEntryDialog`'s ingredient `Autocomplete`.

**Deviation (fix, requested directly — "the group doesn't seem to be visible"):** The first draft's `subtitle` here was just `ingredient.brand ?? undefined`, reasoned (incorrectly) as "no group label needed, this picker is already scoped to one context." That's true for two same-context ingredients, but `fetchIngredients(userId, groupId, communityEnabled)` also merges in every community ingredient when opted in (docs/pending-deviations.md, "Community pantry") — so the option list can mix this context's own rows with community ones, and with no label at all a same-named community ingredient and a context-owned one were indistinguishable. Added a local `groupLabel(isCommunity)` (mirrors `AddLogEntryDialog`'s own `groupLabel`, but simpler: since every non-community option here is already known to belong to this one context, it only needs to pick "Community" vs. this context's own name — `membership?.group.name` in group context, "Personal" otherwise — not a full per-group lookup across every group the caller is in). `subtitle` is now `groupLabel(option.is_community) + (brand ? " · " + brand : "")`, same composition `AddLogEntryDialog` uses.

The recipe-option row (`AddLogEntryDialog.tsx`'s other `Autocomplete`, for logging a recipe) was left inline, not extracted — `AddRecipeIngredientDialog.tsx` has no recipe picker to share it with, so there was nothing to reuse it against yet.

**Why:** Requested directly — "Use that same renderValue layout for the autocomplete in AddRecipeIngredientDialog.tsx. Ideally make it a reusable component."

**Not yet verified:** No database change — pure client-side. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: on a recipe's detail page, "Add ingredient" → "From pantry" → confirm the dropdown options now show name + quantity/unit beside it (no thumbnail) and, on the line below, "Personal"/the group's name (or "Community") plus the brand when set, with kcal/kcal-per-unit on the right — same shape as `/log`'s ingredient picker; with community pantry opted in for that context, confirm a community ingredient's row reads "Community" (not the context's own name) and a context-owned one reads "Personal"/the group name; confirm selecting one and submitting still adds it to the recipe correctly; re-check `/log`'s "Log an entry" ingredient picker still renders identically to before this refactor (group + brand subtitle).

---

## Log entry kcal preview: bigger, success-colored (requested directly, no ticket number)

**Deviation:** `LogIngredientStep.tsx` and `LogRecipeStep.tsx` — the "quantity/amount eaten" step of `AddLogEntryDialog.tsx`'s flow — each show a live-computed kcal figure directly below the quantity `TextField` as the user types. Previously a small muted line (`fontSize={12}`, `text.secondary`, matching the rate subtitle above it); changed in both to `fontSize={20}`, `fontWeight={500}` — bigger, to read as a positive confirmation of what's about to be logged rather than blend in as another muted detail line.

**Deviation (follow-up, requested directly — the app's own green, not MUI's success green):** The color went through three passes: first a plain `color="success.main"` (MUI's default, unthemed success green — this theme has no custom `success` palette entry, see `theme.ts`); then, misreading "use the subtle green text color" as "soften that same success green," `sx={{ color: 'success.main', opacity: 0.75 }}`; corrected, after "No, I'm talking about primary.main," to a plain `color="primary.main"` — this Everforest theme's actual primary color already _is_ a muted olive/sage green (`#8DA101` light / `#A7C080` dark, `theme.ts`), which is what "subtle green" meant all along, not MUI's separate, more saturated `success` palette softened via opacity.

**Deviation (fix, found while applying the above — reformatter noise):** `LogRecipeStep.tsx` had been silently reformatted on disk to Prettier's defaults (double quotes, 80-col rewrap) between two turns — the exact "some editor in this workflow occasionally auto-reformats a touched file on save" failure mode this file's own CLAUDE.md warns about — and in the process the file's `color` had reverted from `success.main` back to the original `text.secondary`, undoing that turn's change entirely. Caught via `git diff --stat` (a 74-line diff for what should've been a 2-line color change, same as `LogIngredientStep.tsx`'s actual 2-line diff); fixed by rewriting the file by hand in its pre-reformat style (single quotes, ~100–110 col lines) with only the genuine color change applied on top, rather than letting the reformat noise ride into any future commit.

**Deviation (follow-up, requested directly — the number more prominent than "kcal"):** The figure was one `Typography` with a single `fontWeight={500}` across the whole `"<n>.<nn> kcal"` string. Split into the number and the unit word as two nested `<Box component="span">`s inside the same (now weight-less, `fontSize={20}` `color="primary.main"` only) `Typography` — the number gets `fontWeight: 700`, `" kcal"` gets `fontWeight: 400` — so the actual figure reads as the prominent part and "kcal" as a lighter-weight unit label beside it, same "number bigger than its unit" relationship `RecipeCard.tsx`/`IngredientCard.tsx`'s stat blocks already use via separate `Typography`s (just done in-line here since it's one sentence, not two stacked lines).

**Deviation (follow-up, requested directly — reposition beside the rate subtitle):** The live kcal figure moved from its own line below the quantity `TextField` up to the top of the step, sharing a row with the name + rate subtitle block instead — the outer `Box` (name `Typography` stacked over the rate `Typography`) is now a `flex`/`justifyContent: space-between` row, that two-line block on the left and the live kcal figure on the right. This puts it directly above-right of the quantity field it reflects, rather than below it.

**Deviation (follow-up, requested directly — center against the full two-line block, not just the subtitle):** First pass nested the kcal figure _inside_ the subtitle row (`alignItems: baseline` against the rate text alone), so it sat low, level with just the subtitle line. Corrected to `alignItems: center` on the outer row instead — the kcal figure is now a direct sibling of the name+subtitle `Box` (not nested inside the subtitle's own row), vertically centered against the combined height of both lines together.

**Deviation (follow-up, requested directly — full name+unit / group+brand ↔ kcal+rate layout):** The top block became a two-sided flex row, both sides two lines tall, mirroring `IngredientAutocompleteOption.tsx`'s own name-beside-quantity/unit treatment:

- Left: name + the ingredient's/recipe's own quantity+unit ("1 sachet"/"500 g") beside it in subtle (`text.secondary`, same `fontSize` as the name) text, name `noWrap` so a long one truncates instead of pushing the quantity/unit out — exactly `IngredientAutocompleteOption`'s pattern. Below that, a new subtitle line: the group this item is from ("Personal"/the group's name/"Community") plus, for an ingredient, its brand when set (`{groupLabel}{brand ? " · " + brand : ""}`) — a recipe has no brand field, so its subtitle is just the group label.
- Right: the live kcal figure (unchanged), with the rate subtitle ("kcal per unit"/"kcal per gram," previously under the name on the left) moved underneath it instead.

`AddLogEntryDialog.tsx` gained a `groupLabel` prop on both `<LogIngredientStep>`/`<LogRecipeStep>`, passed as `groupLabel(selectedIngredient.group_id, selectedIngredient.is_community)` / `groupLabel(selectedRecipe.group_id)` — reusing the same `groupLabel` helper the dialog's own Autocomplete option rows already call, rather than duplicating group-name-lookup logic inside the two step components (which don't otherwise have access to `useMyGroups`'s data).

**Deviation (follow-up, requested directly — spacing between the two sides):** The outer row's two children (the left name/quantity/group block and the right kcal/rate block) had no explicit `gap`, relying only on `justifyContent: space-between` — fine at the dialog's usual width, but with a longer name/group/brand line on the left and nothing forcing minimum breathing room, the two sides could sit close together. Added `gap: 2` (16px, MUI's default 8px spacing unit ×2 — this theme doesn't override `spacing`) to that row.

**Why:** Requested directly — "the kcal counter below the input should be bigger and using the success color," then "use the subtle green text color," then "No, I'm talking about primary.main," then "The number and kcal text should have different fontWeights so that the number is more prominent," then "Move this to the top-right of the input field so it is inline with the name subtitle," then "It needs to be vertically center aligned on both the name and the subtitle," then "Move the current subtitle so it's under the kcal computation. Then beside the name of the recipe/ingredient render the total unit in a subtle text and below this add a subtitle to show the group this was from and the brand," then "Add a 16px gap between the two flexbox children on top of the input field" — each a follow-up/correction on the same element.

**Not yet verified:** No database change — pure client-side. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: open "Log an entry," pick an ingredient → confirm its name has the ingredient's own quantity+unit beside it (subtle, truncating on a long name instead of cutting the unit off), a group (+ brand, when set) line below that, and on the right the live kcal figure with the "kcal per unit" rate now underneath it, the whole row vertically centered top-to-bottom with visible spacing between the two sides even with a long name/group line; type a quantity and confirm the kcal figure updates live; repeat for a recipe via "Amount eaten" (weight_g beside the name, group-only subtitle, no brand line, "kcal per gram" rate under the kcal figure).

---

## Shared IngredientKcalHeader, reused on AddRecipeIngredientDialog's "From pantry" step (requested directly, no ticket number)

**Deviation:** The name+quantity/unit+group/brand ↔ kcal+rate header block built up across the entries above (`LogIngredientStep.tsx`) is now its own component, `src/features/pantry/IngredientKcalHeader.tsx` — takes `ingredient: Ingredient`, `groupLabel: string` (resolved by the caller, same as `IngredientAutocompleteOption`'s `subtitle`), and `kcal: number` (the caller's own live-computed value for whatever quantity is currently being typed). `LogIngredientStep.tsx` was refactored to render it instead of its inline JSX (its `Box`/`Typography` imports, and the `formatKcalPerUnit` import — now only used inside the header component — were dropped as unused).

`AddRecipeIngredientDialog.tsx`'s `ExistingIngredientForm` — the recipe detail page's "Add ingredient → From pantry" step — gained the same header, rendered above its ingredient `Autocomplete` (only once `selected` is non-null; nothing to show before a pick), with `kcal={kcalPerUnit(selected.kcal, selected.quantity) * parsedQuantity}` — the same live-scaling formula `LogIngredientStep` uses, scaled by this form's own "Quantity" field instead of "Quantity eaten." Unlike `LogIngredientStep` (which only ever mounts after a separate selection step in `AddLogEntryDialog`), this form's Autocomplete and header coexist on one screen — picking a different ingredient, or changing the quantity, updates the header live without leaving this step.

**Why:** Requested directly — "Mimic the layout that's on top of the input field to AddRecipeIngredientDialog.tsx place it on top of the ingredient selection autocomplete."

**Not yet verified:** No database change — pure client-side. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: on a recipe's detail page, "Add ingredient" → "From pantry," pick an ingredient from the dropdown → confirm a header appears _above_ the Autocomplete showing its name+quantity/unit, group (+ brand) subtitle, and a kcal figure (initially "0.00 kcal," since no quantity's been typed yet) with the rate underneath; type a quantity in the "Quantity" field below and confirm the header's kcal figure updates live; pick a different ingredient from the still-visible Autocomplete and confirm the header swaps to reflect the new selection; re-check `/log`'s "Log an entry" ingredient step still renders identically to before this refactor.

---

## Code-review fix: consolidate "<group> · <brand>" composition into IngredientAutocompleteOption (no ticket number)

**Deviation:** A `code-review` pass before committing this session's changes flagged that `groupLabel(...) + (brand ? " · " + brand : "")` was copy-pasted verbatim at both `IngredientAutocompleteOption` call sites (`AddLogEntryDialog.tsx`, `AddRecipeIngredientDialog.tsx`), while `IngredientKcalHeader.tsx` composed the same "group · brand" second line differently — internally, from `groupLabel` + the ingredient's own `brand` field, rather than taking a pre-composed string. Fixed by changing `IngredientAutocompleteOption`'s prop from `subtitle?: string` to `groupLabel: string` (required) and composing the brand suffix internally the same way `IngredientKcalHeader` does — both call sites now just pass the resolved `groupLabel(...)` string, with no per-call-site brand concatenation left to keep in sync.

Also fixed, same pass: `AddLogEntryDialog.tsx`'s recipe-option `renderOption` (the one row never extracted into a shared component — no second recipe picker exists yet to share it with, noted in the "Log entry dialog autocomplete options mimic RecipeCard/IngredientCard layout" entry above) still destructured MUI's option props as `...props` instead of `...liProps`, a naming leftover from before the ingredient row was extracted. Renamed for consistency; no behavior change.

**Why:** Surfaced by a `code-review` pass run before commit, at the user's request ("Have these changes reviewed. Once done commit and push."). The review's other findings (several `src/sync/pull.ts`/`useSyncEngine.ts` concurrency and duplication issues) were about already-committed code from a prior session (the "Delete-sync reconciliation" entry above, part of `eceef7b` on this branch already), not this session's diff — out of scope for this review and left untouched; worth a separate follow-up.

**Not yet verified:** No database change — pure client-side. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: same as the two entries above (`/log`'s ingredient picker and the recipe's "Add ingredient → From pantry" picker) — confirm both still show "<group> · <brand>" (or just "<group>" with no brand) identically to before this consolidation.

---

## log_entries reworked from snapshot to live-reference (requested directly, no ticket number)

**Deviation:** `log_entries` previously snapshotted `snapshot_name`/`snapshot_kcal`/`snapshot_quantity`/`snapshot_unit` once at insert time and never touched them again — schema.md's own words were "never reads live ingredient/recipe data," and `source_ingredient_id`/`source_recipe_id` were explicitly "soft breadcrumbs only." Requested directly: move away from that model so a logged entry can be easily updated against its source's current data, rather than being permanently stuck with whatever the source said (or a hand-typed correction) at the moment it was logged.

Reworked so `source_ingredient_id`/`source_recipe_id` are real references, not breadcrumbs: `name`/`kcal`/`unit` are now re-derived from the current source (kcal-per-unit × `quantity`, same formula `LogIngredientStep`/`LogRecipeStep` already used at creation) every time the entry is created _or its quantity is edited_, not just once. Since there were no production rows in this table yet (confirmed directly), the column rename (`snapshot_name` → `name`, `snapshot_kcal` → `kcal`, `snapshot_quantity` → `quantity`, now `not null` — no creation path ever produced a null one anyway — `snapshot_unit` → `unit`) was done as a straight `drop table … cascade` + recreate in `supabase/migrations/20260909000000_log_entries_rework.sql`, rather than a chain of `alter table … rename column`s — restated cleanly instead of accreting onto the old snapshot-era naming. The table's indexes and RLS policies (unchanged in substance — this migration doesn't touch access control) are recreated in the same migration; the update/delete policies, altered in place back in issue #34 to be owner-only, are recreated here under accurate names (`"update own log entries"` / `"delete own log entries"`) instead of keeping their stale `"... or group ..."` names.

`EditLogEntryDialog.tsx` — previously three freehand text fields (name/kcal/quantity), fully independent of the source — is reworked into a quantity-only edit: it loads the entry's current source ingredient/recipe live from Dexie (`fetchIngredient`/`fetchRecipe`, both already Dexie reads), shows the same live kcal-preview header used at creation time (`IngredientKcalHeader`, and a new `RecipeKcalHeader` extracted from `LogRecipeStep.tsx`'s previously-inline header JSX — now shared the same way `IngredientKcalHeader` already was between `LogIngredientStep`/`AddRecipeIngredientDialog`), and lets only the quantity and `meal_type` be edited; `name`/`kcal`/`unit` are recomputed from the live source on save, never typed by hand.

**Deviation (delete-behavior decision, requested directly):** Once a log entry's source has been deleted (`source_ingredient_id`/`source_recipe_id` both null via the existing `on delete set null`), there's nothing left to re-derive `name`/`kcal`/`quantity`/`unit` from — explicitly decided to fall back to the entry's last-refreshed values and **not** allow editing them at all at that point (no reintroduced freehand path), rather than letting the user hand-correct a now-orphaned entry. Only Delete remains available once detached — a follow-up request extended this to disable `MealTypeSelector` and the "Save changes" button too (not just the quantity/kcal/name fields), since there's nothing left on the form a save could actually persist once quantity can't be re-derived. This also covers, degenerately, any row from before this rework with neither source id set (not a real case today — every current creation path always supplies one).

**Deviation (follow-up, requested directly — reuse the same layout instead of a separate read-only block):** The detached state was initially rendered as its own hand-rolled `Box`/`Typography` block (name + an explanatory line), visually distinct from the live-source layout. Changed so the detached case renders the exact same `IngredientKcalHeader` + `TextField` structure as the live-source case, just fully disabled: `IngredientKcalHeader`'s prop type was widened from the full `Ingredient` to `Pick<Ingredient, 'name' | 'quantity' | 'unit' | 'brand' | 'kcal'>` (every existing caller already passes a full `Ingredient`, so this is non-breaking elsewhere), letting the detached branch pass a synthetic object built from the entry's own frozen `name`/`quantity`/`unit`/`kcal` instead of a real persisted row. "kcal per {unit}" reads correctly for a detached recipe-sourced entry too, since recipes are always logged in grams. The header's `groupLabel` stays a plain "Personal"/"Group" in every state (initially repurposed into a "Source deleted…" status line when detached, then reverted — see the next paragraph).

**Deviation (follow-up, requested directly — message placement/styling):** The "source deleted" explanation was first placed in the header's subtitle slot (in place of "Personal"/"Group"), then moved to the end of the dialog body instead — after the quantity field and `MealTypeSelector`, as its own line — and restyled from a plain `text.secondary` line to `color="error.main"`, `fontWeight={700}` (up from the header subtitle's normal weight), so it reads as a clear warning rather than blending in as another muted detail line.

**Why:** Requested directly, as a scoped, deliberate schema rework — not a bug fix. The three shape decisions above (quantity-only live-derived edit + keep `meal_type` editable while a source exists; full drop+recreate rather than incremental renames, since no prod data exists; frozen-and-fully-uneditable rather than freehand once detached) were each surfaced and confirmed directly rather than assumed, given the size of the change (schema, RLS policy names, offline sync types, and every log-entries UI component). The follow-ups (disable meal type/Save once detached; reuse one layout for both states; move+restyle the source-deleted message) were each requested directly after the initial version shipped.

**Not yet verified:** Same database-connection restriction as every entry above (org policy: must not connect to databases) — `supabase/migrations/20260909000000_log_entries_rework.sql` has not been pushed or run against any Supabase project. Verification here was `tsc -b` and `oxlint`, both clean; no automated test suite exists in this repo. Manual follow-up once pushed (see `supabase/README.md`): log an ingredient and a recipe from `/log`'s FAB, confirm both entries show correctly; edit the source ingredient's kcal value in `/pantry`, then open the log entry via `EditLogEntryDialog` and confirm the kcal preview immediately reflects the _new_ rate (not the value at logging time) even before changing the quantity; change the quantity and save, confirm the stored `kcal` updates accordingly and the entry's card/list totals recompute; delete the source ingredient, then reopen the same log entry and confirm it now shows the same header/quantity layout as before but fully disabled (quantity field, meal-type selector, and "Save changes" all non-interactive), a bold red "Source deleted — this entry is locked at its last known values." line at the bottom of the dialog body, and that its previously-logged kcal value is unchanged in `/log`/`/logs`; confirm deleting the entry still works from that fully-locked state; repeat the live-refresh check for a recipe-sourced entry, including the max-weight validation on its quantity field.

**Deviation (fix, found during a manual review pass — the automated `/code-review` skill run got stuck in an internal polling loop and never finished; see the feedback filed on that separately):**

- `EditLogEntryDialog.tsx`'s `useEffect` fetching the entry's source (`fetchIngredient`/`fetchRecipe`) had no `.catch` — a rejected read (Dexie failure) left `ingredient`/`recipe` at their initial `undefined` forever, so the dialog would sit on its loading spinner permanently instead of falling back to the locked state, unlike every other Dexie-read call site in this feature (`AddLogEntryDialog.tsx`'s own fetches already `.catch(() => setIngredients([]))`). Fixed by catching to `null` (same as "not found") on both fetches.
- Re-opening an already-logged recipe entry whose recipe had since had its `weight_g` reduced below the entry's own stored `quantity` immediately showed the quantity field as over-the-max and disabled the whole form, including "Save changes" — blocking even an unrelated `meal_type`-only correction, since the entry's untouched quantity was now invalid against the _new_ max through no action of the user's. Fixed by only evaluating the negative/exceeds-weight checks once the quantity field has actually been changed from `entry.quantity`'s own string value (a new `quantityChanged` guard) — an unedited quantity is grandfathered in regardless of what the source now allows, since re-saving it changes nothing about that number.
- A second, previously-unhandled lock case: `source_ingredient_id` remaining valid but that ingredient's own `unit` having changed since the entry was logged (ingredients' units are editable, `IngredientDetail.tsx`) was silently allowed through the live-derive path. The entry's stored `quantity` is a raw number entered under the _old_ unit; re-deriving kcal from the ingredient's current per-unit rate against that same raw number reinterprets it under the new unit and produces a wrong value with no warning — the app does no cross-unit conversion anywhere (`schema.md`), so there's no way to safely reconcile this. Added a `unitMismatch` check (`ingredient.unit !== entry.unit`) alongside the existing `detached` one, combined into a single `locked` flag that gates quantity/meal-type/Save exactly the same way `detached` already did, with its own explanatory message ("This ingredient's unit has changed since logging…") distinct from the source-deleted one. `liveKcal` and the submit payload's `name`/`kcal`/`quantity`/`unit` all now key off `canEditQuantity` (`!loading && !locked`) rather than the narrower `detached`, so both lock cases freeze the same way.
- `handleSubmit` guarded only on `isValid`, which is vacuously true whenever `canEditQuantity` is false (the quantity check short-circuits) — the disabled Save button was the only thing actually preventing a submit while locked. Added an explicit `|| locked` to `handleSubmit`'s own early return as defense-in-depth, not just the button's `disabled` prop.

**Why:** Surfaced by a manual line-by-line/cross-file review pass (the automated `/code-review` skill's background run did not complete — see the separately-filed feedback on that tool malfunction) done at the user's request before committing this branch's changes.

**Not yet verified:** No database change — pure client-side. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: change a recipe's weight below an already-logged entry's quantity, reopen that entry, confirm the quantity shows as invalid (red, over-max) but "Save changes" stays enabled for a meal-type-only edit, and that changing the quantity itself still correctly re-validates and blocks over-max; edit an ingredient's unit after logging against it, reopen an entry logged from it, confirm the whole form locks with the "unit has changed since logging" message and the kcal/quantity shown are the entry's last-saved values, not a live (and wrong) recomputation.

---

## log_entries: log an entry on a fellow group member's behalf (requested directly, no ticket number)

**Deviation:** `log_entries.logged_by` was overloaded — it meant both "who this entry counts against" (kcal totals, personal history) and, implicitly, "who performed the write," since the update/delete RLS policies checked `logged_by = auth.uid()` directly. There was no way for one group member to log an entry that counts against a different member. Requested directly: "User A logs an entry for User B."

Split into two columns in `supabase/migrations/20260910000000_log_entries_logged_for.sql`:

- **`logged_for`** (renamed from `logged_by`) — who the entry counts against. Chosen over the first-pass name `attributed_to` — "less descriptive" was the objection to `logged_by` itself once actor/target could differ (it reads as "who did the logging," i.e. the actor, not the target), so the replacement needed to unambiguously mean "who it's _for_."
- **`created_by`** (new) — who actually wrote it. Matches the `created_by` convention `ingredients`/`recipes` already use, instead of introducing a third naming style for "the actor" (schema.md's ownership-pattern section).

This was an `alter table` migration (rename + add + backfill), not the drop+recreate the prior `log_entries` rework used — that one was justified by "no production rows exist yet," which no longer holds. `created_by` is backfilled from `logged_for` for any existing row, accurate for every row logged before this feature existed (actor and attribution target were always the same person).

RLS reworked to make the delegation possible while keeping it scoped to actual group members:

- **insert**: `created_by = auth.uid()` always (nobody can forge another user's authorship) — the caller doing the writing is always themselves. For a personal entry (`group_id` null), `logged_for` must still equal the caller — there's no group to delegate within. For a group entry, `logged_for` can be any member of that same group, but the policy explicitly checks _that member's_ own group membership too (`group_id in (select group_id from group_members where user_id = logged_for)`), not just the caller's — a caller can't attribute an entry to someone outside the group they're posting it into.
- **update/delete**: reversed from owner-only (`logged_for = auth.uid()`) back to group-inclusive (any member of the entry's group, matching `ingredients`/`recipes`), decided directly ("Everyone within the group can edit it") — the owner-only shape was deliberately introduced in issue #34 on the reasoning that `log_entries` "represents an individual's own intake history, unlike ingredients/recipes." That reasoning no longer holds once one member can log for another: a group log entry is now a shared-group resource any fellow member can correct (e.g. fix a quantity typo in an entry logged on their behalf), the same as an ingredient or recipe.

App-side changes:

- `LogIngredientStep.tsx`/`LogRecipeStep.tsx` gained a `loggedFor`/`onLoggedForChange` prop pair, rendering a new `LoggedForSelector.tsx` (a `TextField select` of the item's group's members, via the existing `useGroupMembers`/`useProfileNames` hooks) directly below `MealTypeSelector` — only for a group-owned ingredient/recipe (`ingredient.group_id`/`recipe.group_id` set; a personal or community item has no group to delegate within), and only when that group actually has more than one member (nothing to pick between otherwise). `AddLogEntryDialog.tsx` owns the `loggedFor` state itself, defaulting to the caller and resetting to the caller on every selection change (so a picker choice from one group's item can't linger onto a different item from a different group with non-overlapping members), and passes `groupId === null ? userId : loggedFor` into `createLogEntry` — a personal entry always collapses back to the caller regardless of whatever the (hidden, in that case) picker state holds.
- `createLogEntry` gained an `actorUserId`/`loggedFor` parameter split (previously a single `userId`), matching the new columns.
- `LogEntryCard.tsx`'s single `loggerName` prop ("Logged by X") became two — `loggedForName` ("For X," shown when `logged_for` isn't the viewer) and `loggedByName` ("logged by X," shown only when `created_by` differs from `logged_for`, i.e. it really was logged on someone else's behalf, not just viewed by a different group member). `DailyLog.tsx`/`AllTimeLog.tsx` batch-fetch both ids' names in one `useProfileNames` call and compute both props; their card `onClick` handlers dropped the `entry.logged_by === userId ? ... : undefined` ownership gate entirely — every entry either query surfaces is already something the new group-inclusive update RLS lets the viewer edit (a personal one is always their own; a group one is editable by any fellow member), so the gate was pure dead weight once that RLS changed.
- `EditLogEntryDialog.tsx` gained a small, non-editable attribution line ("For X" / "logged by X," same wording/logic as the card) shown under the header whenever either differs from the obvious default. Reassigning `logged_for` or `created_by` after creation is explicitly **not** supported — out of the original request's scope ("User A logs an entry for User B" describes creation, not re-attribution), so `updateLogEntry`'s `LogEntryUpdateInput` still only covers `name`/`kcal`/`quantity`/`unit`/`meal_type`.
- `RemoveMemberDialog.tsx`'s doc comment now also notes the resulting edge case: a member removed from a group after having entries logged for them still has those entries (their `logged_for` untouched, per the comment's existing "never touches ingredients/recipes/log entries" guarantee), but the update/delete RLS now requires a _current_ member, so neither they nor anyone else outside the group can touch that entry going forward — not fixed here, just documented, since removing a member mid-history is an existing edge case this feature only sharpens, not one it introduces.
- `src/lib/db.ts` bumped to Dexie `version(4)`, re-declaring `log_entries`'s index list with `logged_for`/`created_by` in place of `logged_by`. `src/sync/pull.ts`'s `ownerColumn` type/config for `log_entries` renamed the same way.

**Why:** Requested directly, walked through as a design decision rather than assumed, given the size of the change (schema, RLS, offline sync, and the logging UI): naming (`logged_by`'s ambiguity once split → `logged_for`, picked over `attributed_to`), storage shape (ALTER, not drop+recreate, since real data now exists), and the update/delete ownership model (group-inclusive, "everyone within the group can edit it," matching `ingredients`/`recipes` rather than staying owner-only).

**Not yet verified:** Same database-connection restriction as every entry above (org policy: must not connect to databases) — `supabase/migrations/20260910000000_log_entries_logged_for.sql` has not been pushed or run against any Supabase project. Verification here was `tsc -b` and `oxlint`, both clean; no automated test suite exists in this repo. Manual follow-up once pushed (see `supabase/README.md`): as a member of a 2+-person group, open "Log an entry," pick a group-owned ingredient/recipe, confirm a "Log for" picker appears (defaulting to yourself) listing every member by name, pick a different member and log it; confirm the resulting entry appears on that group's shared log attributed to the picked member ("For <name> · logged by <you>" on the card), and — logged in as that other member — that it also appears on their own personal `/log`/`/logs` view; open that entry's edit dialog as the original logger and confirm the same attribution line appears and the entry is fully editable; open it as the _targeted_ member too and confirm they can also edit/delete it (group-inclusive RLS); confirm a personal (non-group) ingredient/recipe never shows a "Log for" picker at all and always logs to yourself; remove a member from the group who has entries logged for them, confirm those entries still show up in the group's history (name still resolves via `useProfileNames`) but attempting to edit them now surfaces the outbox's permission-denied message instead of silently succeeding.

**Deviation (follow-up, requested directly — extend delegation to community ingredients):** The "Log for" picker above only ever rendered for a _group-owned_ item (`ingredient.group_id`/`recipe.group_id` set) — a community ingredient's own `group_id` is always null (same as a personal item, `ingredients_community_no_group` check constraint), so it fell through to "always logs for yourself," same as any personal item, even though a community ingredient is visible to and usable by everyone, not actually private the way a personal item is. Asked directly: "I want community ingredients to be possible to be logged for someone else."

Community-ness (who can _see and use_ the ingredient) and a log entry's `group_id` (which shared log it lands on, and thus whose membership `logged_for` can be checked against) are separate concerns — a community ingredient still needs an actual group to delegate within, it just doesn't already have one of its own the way a group-owned item does. `AddLogEntryDialog` gained an optional `groupId` prop — the group screen it was opened from (`DailyLog` passes its own `groupId` through) — consulted only when the picked item is a community ingredient: `resolveGroupId()` returns that `groupId` instead of the item's own (null) one in that case, so the resulting entry lands on _that_ group's shared log rather than always personal, and `logged_for` can then be any of that group's members. A personal item, or a community ingredient picked from the personal `/log` screen (no group context to target), is unaffected — still always for yourself. Recipes have no community tier, so `LogRecipeStep`'s copy of this same plumbing is a no-op in practice (`resolveGroupId` just returns the recipe's own `group_id`), kept only so both step components share one prop shape rather than special-casing ingredients.

`LogIngredientStep`/`LogRecipeStep` no longer read `ingredient.group_id`/`recipe.group_id` directly to decide whether to render `LoggedForSelector` — they take an explicit `loggedForGroupId` prop instead, since a community ingredient's _own_ `group_id` can no longer be trusted to reflect where the entry is actually headed.

**Known edge case, not fixed here:** `AddLogEntryDialog`'s community-ingredient visibility is still cross-context — a community item appears in the list if _either_ the caller's personal profile _or_ any group they belong to has opted in (pre-existing, unrelated to this change), not gated on whether the _specific_ group screen you opened the dialog from has itself opted in. So it's possible to log a community ingredient into a group's shared log even if that group's own "use community pantry ingredients" switch (`/groups/:groupId/pantry`) is off, as long as some other group you're in has it on. Not a new hole this change introduces (the dialog's ingredient list was already this loose before `groupId` existed on it at all) — just newly reachable via a group's own log rather than only the personal one. Flagging rather than restricting, since narrowing it would be a product call (should a group's log only ever contain community items that group has actually opted into?) worth confirming rather than assuming.

**Why:** Requested directly, as a follow-up once the base "log for a group member" feature was in and the user asked why community ingredients specifically were excluded from it.

**Not yet verified:** No database change — pure client-side, reuses the same RLS from the migration above (a community ingredient's log entry is just an ordinary group-scoped `log_entries` row once `group_id` is set; the insert policy doesn't distinguish where the source ingredient came from). Verification here was `tsc -b` and `oxlint`, both clean. Manual follow-up: as a member of a 2+-person group with community pantry enabled (either on that group or on your own personal profile), open "Log an entry" from that group's own `/groups/:groupId/log` screen, pick a community ingredient → confirm the "Log for" picker now appears (previously didn't); pick a different member and log it → confirm the entry lands on that group's shared log (not personal) with the usual "For X · logged by Y" attribution; open `/log` (personal, cross-context) instead, pick the same community ingredient → confirm no "Log for" picker appears there and the entry logs to yourself, same as before this change.

**Deviation (follow-up, requested directly — meal-type selector reflects the target's own preference):** `MealTypeSelector` previously always rendered, regardless of anyone's `meal_breakdown_enabled` setting (`profiles.meal_breakdown_enabled`, the same flag `DailyLog`'s/`GroupMemberKcalCard`'s per-meal breakdown cards already gate on). Now that an entry can be logged for someone other than the caller, "always show it" stopped making sense — a member who's never turned on meal-type breakdown would still see the selector on entries logged for them. Asked directly: "this should be dynamically shown/hidden depending if the logged_for target has this enabled."

`LogIngredientStep`/`LogRecipeStep` gained a `mealBreakdownEnabled: boolean` prop, resolved by `AddLogEntryDialog` via a new `useMemberKcalProfiles([loggedFor])` lookup (the same hook `GroupMemberKcalCard` already uses for its own per-member breakdown) — re-fetches automatically whenever `loggedFor` changes, so switching who the entry is for immediately reflects that person's own setting, not the caller's. `EditLogEntryDialog` gained the equivalent lookup keyed on the entry's own (fixed, non-editable) `logged_for`. Both default to hidden (`?? false`) while the lookup is in flight or if it fails, same "pop in once resolved, hidden until then" treatment already used elsewhere in this dialog (`hasGroups`, community-pantry-driven list items) — no loading skeleton for a single optional field.

Also reordered `LogIngredientStep`/`LogRecipeStep`'s quantity → meal-type → "Log for" layout to quantity → "Log for" → meal-type, so the field that now visually depends on who's picked above it doesn't render _above_ that picker (a judgment call while making this change, not separately requested).

**Why:** Requested directly, as a follow-up once "log for a group member" made it possible for the caller's own `meal_breakdown_enabled` preference to no longer be the relevant one.

**Not yet verified:** No database change — pure client-side (reuses `fetchMemberKcalProfiles`, already RLS-covered by "select own row or fellow group member's row"). Verification here was `tsc -b` and `oxlint`, both clean. Manual follow-up: as a member with meal-type breakdown **off**, log a personal item → confirm no meal-type selector appears; turn it on in your profile settings and log again → confirm it now appears. In a 2+-member group where you have it on but a fellow member doesn't, open "Log an entry" from that group's log, pick a group item, and switch "Log for" between yourself and that member → confirm the meal-type selector appears/disappears live as the picker changes, with no page reload needed. Open an already-logged entry (via `EditLogEntryDialog`) that was logged for a member without breakdown enabled → confirm the selector is hidden there too, and that saving an unrelated field (e.g. quantity) doesn't touch its `meal_type`.

**Deviation (fix, found during a review pass before committing this session's changes):** `src/lib/db.ts`'s `db.version(4)` re-declared `log_entries`'s index list (`logged_by` → `logged_for`, plus `created_by`) via `.stores()` alone, with a comment claiming the next sync pull's `bulkPut` would naturally overwrite any pre-existing local row into the new shape. That claim was wrong: `sync/pull.ts`'s cursor only re-fetches rows whose `updated_at` changed since the last successful pull, so a row that hasn't been touched server-side since this shipped would never be re-fetched — and since IndexedDB only indexes documents that actually have the indexed property, such a row (still literally shaped `{logged_by: ...}`, no `logged_for`) would silently drop out of every `logged_for`-keyed query (`fetchTodayLogEntries`'s/`fetchAllLogEntries`'s personal-scope branch, `reconcileDeletes`'s personal branch) — not corrupted, just invisible, until something else happened to touch it server-side again. Fixed by adding an explicit `.upgrade()` transform to `version(4)` that renames `logged_by` → `logged_for` and backfills `created_by` from it on every existing local row, mirroring the server migration's own backfill exactly.

**Why:** Surfaced by a review pass run before committing this branch's accumulated changes, at the user's request ("Have the changes reviewed. Then push the changes").

**Not yet verified:** No new database change — this only affects the local Dexie upgrade path (the actual `created_by`/`logged_for` rename still lives entirely in `supabase/migrations/20260910000000_log_entries_logged_for.sql`, unpushed per org policy). Verification here was `tsc -b` and `oxlint`, both clean. Manual follow-up once this ships to a build with existing local data (i.e. not a fresh install): confirm a pre-existing log entry created before this session's changes still appears normally on `/log`/`/logs` after the app updates (no silent disappearance), and that its `created_by` correctly backfilled to match its own `logged_for` (equal, since delegation didn't exist when it was created) rather than being left undefined.

**Deviation (follow-up, requested directly — simplify the card's attribution line):** `LogEntryCard`'s subtitle previously showed up to three things — the time, "· For X" when `logged_for` wasn't the viewer, and "· logged by Y" when `created_by` also differed from `logged_for`. Asked directly to simplify: "just show who it's logged for then the time." `loggedByName` was dropped entirely (prop, and both call sites' now-unnecessary `created_by` half of their `useProfileNames` lookup — `DailyLog`/`AllTimeLog` now only fetch names for `logged_for`, not `created_by`), and the subtitle is now just `{loggedForName} · {subtitle}` when it's set, `{subtitle}` alone otherwise — no separate "For"/"logged by" wording.

**Deviation (fix, found while applying the above — reformatter noise):** `LogEntryCard.tsx` was silently reformatted on disk to Prettier's defaults (double quotes, 80-col rewrap) between the edit and this follow-up — the same "some editor in this workflow occasionally auto-reformats a touched file on save" issue this repo's `CLAUDE.md` warns about — and in the process the subtitle's wording had also drifted from `For ${loggedForName} · ` to just `${loggedForName} · ` (the "For " dropped). Treated the wording drop as the actual intended current state (per this session's own guidance to trust an externally-changed file rather than reflexively revert it) and rewrote the file by hand in its pre-reformat style (single quotes, ~100–110 col lines) with that wording preserved, rather than letting the reformat noise ride into any future commit.

**Deviation (follow-up, requested directly — always show the name, even for your own entries):** `DailyLog`/`AllTimeLog` initially still only passed `loggedForName` when `entry.logged_for !== userId`, matching the prior "For X"-wording behavior (no point naming the viewer to themselves). Changed to always pass it in group context (`groupId ? names[entry.logged_for] : undefined`, dropping the `!== userId` check) — every card on a group's shared log now shows whose entry it is, including your own, rather than only standing out when it's someone else's.

**Why:** Requested directly — "just show who it's logged for then the time," then "It should always show the name in logged_for even if it's the currently logged in users own log entry."

**Not yet verified:** No database change — pure client-side. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: on a group's log, confirm every card reads "<name> · <time>" — including your own entries, not just fellow members' — and that the personal (non-group) `/log`/`/logs` views still show no name at all, just the time.

**Deviation (fix, found while committing — `EditLogEntryDialog.tsx` had independently drifted to match the same simplification):** Before committing, `EditLogEntryDialog.tsx` was found to have changed on disk to already reflect this same "always show logged_for, no For/logged-by wording" behavior on its own attribution line (unprompted in this conversation — presumably picked up from the pattern set on `LogEntryCard.tsx`) — but reformatted to Prettier's defaults (double quotes, 80-col rewrap) in the process, the same known issue as the other entries above, and leaving two bits of dead weight behind: `useAppStore`'s `userId` (only ever used for the now-removed `!== userId`/`created_by !== logged_for` comparisons) and `useProfileNames`'s `entry.created_by` half of its lookup (fed a `loggedByName` display that no longer exists). Restored the repo's actual style and removed both, keeping the already-current behavior (`loggedForName = entry.group_id ? names[entry.logged_for] : null`, always shown, no per-viewer conditional).

**Not yet verified:** No database change — pure client-side. Verification was `tsc -b` and `oxlint`, both clean. Manual follow-up: open `EditLogEntryDialog` on a group entry (yours or a fellow member's) and confirm the attribution line reads the plain name only, shown in every case, with no dead-fetch console errors.

---

## Remove personal mode — require every account to belong to a group (requested directly, no ticket number)

**Deviation:** The `group_id is null` ("personal") ownership state is removed entirely from `ingredients` (except the pre-existing, orthogonal `is_community` exception, which still needs `group_id is null`), `recipes`, and `log_entries` — every account must now belong to at least one group, enforced as a mandatory final step of onboarding. This reverses the app's own history: Pantry/Recipes/Log were originally built personal-only (Tickets 6/7/8), and group support was retrofitted on top (Ticket 12); this change removes the personal leg of that duality rather than adding to it. Requested directly, not driven by any ticket.

Two new migrations, authored only (org policy: must not connect to databases):

- `supabase/migrations/20260911000000_backfill_personal_to_default_groups.sql` — for every user with a personal (non-community) row in `ingredients`/`recipes`/`log_entries`: creates a new owned group and reassigns their personal rows into it if they belong to no group yet, or reassigns into their earliest-joined existing group otherwise (a product decision, made directly rather than assumed — an existing group is reused instead of creating a redundant one). Bumps `updated_at` on every reassigned row so `src/sync/pull.ts`'s delta-based pull actually re-fetches them into clients that already have them cached with `group_id = null`. A user with zero groups and zero rows is left untouched — they're routed through the new mandatory onboarding step below on next login instead, which needs no data fix.
- `supabase/migrations/20260912000000_require_group_membership.sql` (must run only after the backfill above is verified) — `alter column group_id set not null` on `recipes`/`log_entries`; on `ingredients`, `group_id` stays nullable but `ingredients_community_no_group` (`20260907000000_community_pantry.sql`) is replaced with a biconditional check (`is_community` if and only if `group_id is null`); every RLS policy on `ingredients`/`recipes`/`recipe_ingredients`/`log_entries` has its `group_id is null` branch dropped (ingredients' insert/update/delete keep an explicit `is_community and created_by = auth.uid()` branch in its place, matching the original community-pantry design rather than leaning on the check constraint alone); `copy_ingredient`/`find_ingredient_match`/`copy_recipe` (`20260903000000_copy_ingredient_recipe_rpcs.sql`, since redeclared) drop their "`p_target_group_id` null means personal" branch and now require a real target group.

The onboarding flow (`src/features/onboarding/`) gains a mandatory final step, `CreateOrJoinGroupStep.tsx`, after the existing `CalorieTargetStep` — create a group (`GroupForm`/`createGroup()`, reused directly rather than through `CreateGroupDialog`'s dialog wrapper) or join one with a manual invite code (`previewGroupInvite`/`acceptGroupInvite`), confirmed directly as the preferred shape over a create-only step ("Create or accept invite" over "Create-only"). `useOnboardingGate` becomes two-part — `profileComplete` (the existing `daily_kcal_target != null` check) **and** `hasGroup` (a `group_members` row exists) — both required for `onboardingComplete`. `OnboardingStepper` jumps straight to the group step on mount if `profileComplete` is already true (a returning user who bailed before finishing), and renders it as an already-satisfied confirmation rather than forcing a redundant create/join if `hasGroup` is already true.

That last case — `hasGroup` already true before onboarding finishes — needed its own fix, found while designing this change, not assumed: today, `/invite/:inviteCode` sits behind `RequireAuth` (`src/App.tsx`), and `RequireAuth` hard-redirects an unauthenticated visitor to `/login` with **no preservation of the original URL** (no query param, no router state). A brand-new invitee who isn't signed up yet loses the invite code the instant they click the link, with no way back to it except re-finding the original email — which, once onboarding forces a create-or-join step, would mean they create their own throwaway group during onboarding and only _afterward_ stumble back into the group they were actually invited to, ending up in two groups instead of one. Fixed as part of this change:

- `/invite/:inviteCode` moves out from behind `RequireAuth` entirely — public, like `/privacy`/`/terms` — since `previewGroupInvite` is already a read-only, pre-membership RPC.
- `AcceptInvite.tsx` becomes auth-aware: logged-in behavior (preview → confirm → `acceptGroupInvite`) is unchanged; logged-out shows "Sign up to join" / "Log in" instead of "Join group," storing the invite code (new `src/lib/pendingInviteStorage.ts`, `localStorage`-backed, mirroring `activeGroupStorage.ts`) before navigating to `/signup` or `/login`.
- `signUpWithEmail` (`src/features/auth/api.ts`) gains an `emailRedirectTo` passthrough — when a pending code exists, the confirmation email link itself points at `/invite/:code`, since email confirmation is asynchronous and may be opened on a different device than the one that stashed the code. `signInWithGoogle`'s fixed `redirectTo: window.location.origin` becomes the same conditional target for the synchronous OAuth case.
- `LoginForm.tsx`'s synchronous email/password login (which doesn't navigate anywhere on success today) now checks for a pending code and navigates to `/invite/:code` if one exists.

Frontend routes/props collapse the personal/group duplication `docs/routes.md` previously documented as deliberate: `/pantry`, `/pantry/:ingredientId`, `/recipes`, `/recipes/:recipeId` are deleted from `src/App.tsx` (unreachable once every account always has a group); `/` redirects to the user's group pantry (their only group, or their last-picked one via the existing `activeGroupStorage.ts`, or their first, if they belong to several) instead of `/pantry`. Bare `/log`/`/logs` are deliberately **kept** — on reflection while implementing (not part of the original plan text, corrected in place) they were never actually personal, just historically mislabeled: they're cross-context aggregates ("everything logged across every group") the same way `/logs` already was, so deleting `/log` would have been a real feature loss unrelated to removing personal ownership. `groupId: string | null` props/params become `groupId: string` (required) throughout `src/features/pantry/`, `src/features/recipes/`, `src/features/logging/`, and their `api.ts` functions. `ContextSwitcher.tsx` drops its "Personal" menu item and only renders with 2+ groups (mirroring its old "hide with 0 groups" logic, shifted to "hide with <2"). `useCopyTargets.ts` drops the `{groupId: null, label: "Personal"}` entry. `src/sync/pull.ts` drops the "personal" `PullScope` entry — the pull loop only iterates the caller's group memberships now, one less scope to fan out per cycle. `weight_logs`/Progress are explicitly untouched — personal-only by prior design (Ticket 2), with no group-shaped equivalent, and outside the scope of this request.

A live UI surface was found to have quietly lost its only home in the process: `profiles.community_pantry_enabled` (the personal pantry's own community-pantry opt-in switch, "Community pantry" section above) was only ever editable from the now-deleted personal `/pantry` screen. Rather than inventing a new home for it (a product decision, not assumed), `setCommunityPantryEnabled` (`profiles/api.ts`) and the field's every remaining read (`AddLogEntryDialog.tsx`'s cross-context `communityEnabled` check, `AddRecipeIngredientDialog.tsx`'s per-context check) were removed — community-ingredient visibility now depends only on a group's own `community_pantry_enabled`. The `profiles.community_pantry_enabled` **database column itself is deliberately left in place**, not dropped — it's now permanently unreachable from the app (whatever value each row already has stays frozen), acceptable rather than worth a third migration in this already-large change, but worth a human's explicit sign-off rather than silently orphaning it.

**Why:** Requested directly — "get rid of the whole personal concept... this should be a required step during the onboarding process." The invite-before-onboarding handoff, the backfill-vs-no-backfill choice, and the create-or-join onboarding shape were all explicit decisions confirmed directly rather than assumed, given how much of this touches production data and RLS.

**Not yet verified:** Neither migration has been pushed or run against any Supabase project (org policy: must not connect to databases) — see `supabase/README.md` for the manual QA steps this needs once pushed. The two biggest "verify against real data" risks: (1) the backfill's "reuse the earliest-joined existing group" rule for a user who already belongs to a group but still has personal rows — confirm this matches actual intent rather than, say, always creating a dedicated new group regardless; (2) the `updated_at` bump on reassigned rows actually being sufficient for `src/sync/pull.ts`'s delta cursor to re-pull them into an existing client's local Dexie cache — this was reasoned from reading `pull.ts`, not exercised against a live pull. `tsc -b`, `oxlint`, and `prettier --check` are clean; no automated test suite exists in this repo. Suggested push order: backfill migration → verify every user has ≥1 group → constraint/RLS migration → verify → deploy the new frontend build (see `docs/pending-deviations.md`'s own migrations list is now current as of this entry).

**Deviation (fixes, found by a `/code-review` pass before this branch's changes were considered done):**

- `BottomNav.tsx`'s group-aware-tab fallback (the `fallbackGroupId` logic above) had been applied uniformly to Pantry, Recipes, _and_ Log — since a real group always resolves now, tapping Log from Progress/Profile no longer landed on bare cross-context `/log` at all, silently making it unreachable from the tab bar (the "kept `/log`" decision two paragraphs up was defeated by this). Split into two behaviors: Pantry/Recipes (`nav: "always-group"`) still always resolve a real group, since they have no bare route to fall back to; Log (`nav: "group-or-bare"`) only follows the _current_ route's own group, if any, exactly like every tab did before this change — never synthesizing a fallback group for it.
- `clearPendingInviteCode` (`pendingInviteStorage.ts`) was written but never called anywhere — a stashed code would linger in `localStorage` indefinitely, past the invite flow it was meant for, and could wrongly redirect a later, unrelated login/signup on the same device. `AcceptInvite.tsx` now clears it as soon as the page renders authenticated again (the handoff's job is done at that point, regardless of what the user does next).
- `ContextSwitcher.tsx`'s new `<2`-groups hide condition treated "still loading" (`groups === undefined`) the same as "confirmed only one group," so the chip would flash away and back for any 2+-group user on first render. Now only hides once `groups` has actually resolved.
- `CreateOrJoinGroupStep.tsx`'s join action relied entirely on the parent's `hasGroup` (via the shared `useMyGroups` cache) flipping true to show success — if that refetch was slow or itself failed, the button stayed stuck on a disabled "Joining…" with no way forward despite the join having actually succeeded. Added a local `joined` status so this step's own success doesn't depend on that refetch.
- `OnboardingStepper.tsx`'s `handleFinish` picked `groups?.[0]` (alphabetically first, per `fetchMyGroups`' sort) instead of the `resolveDefaultGroupId` convention (stored-then-first) `HomeRedirect`/`BottomNav` both use — inconsistent for the case where a user already had 2+ group memberships going into onboarding (e.g. accepted an invite earlier, or already belonged to one before revisiting `/onboarding` for the profile steps). Now uses the same shared helper.

**Why:** Requested directly — "have the changes reviewed."

**Not yet verified:** Same as above — these are code-level fixes, `tsc -b`/`oxlint`/`prettier --check` clean, no live click-through possible until the migrations are pushed.

**Deviation (follow-up, requested directly — clicking a `GroupCard` switches context):** `GroupCard.tsx` (`/groups`) previously had no click handler of its own — only its owner-only invite/settings icon buttons were interactive. Clicking the card itself now runs the same persist-and-navigate action as `ContextSwitcher.tsx`'s `selectContext` (`setStoredGroupId` + `navigate` into that group's pantry), so `/groups` doubles as a group picker, not just a management list. The two icon buttons now call `event.stopPropagation()` so tapping them doesn't also trigger the card's own navigation.

**Why:** Requested directly — "Update GroupCard.tsx so that clicking on the card itself changes the group context."

**Not yet verified:** No database change — pure client-side. `tsc -b`, `oxlint`, `prettier --check` clean. Manual follow-up: tap a group card's body → confirm it navigates into that group's pantry and the pick persists across a reload; tap the invite icon and the settings gear (owner-only) → confirm each still opens its own dialog/screen without also navigating into the card's group.

**Deviation (follow-up, requested directly — land on `/groups` instead of guessing a default group):** `resolveDefaultGroupId` (`src/lib/defaultGroup.ts`) previously fell back to the caller's alphabetically-first group membership whenever nothing was stored or the stored pick was stale. Changed to return `null` in both of those cases instead of guessing — it now only ever returns a group the user explicitly picked (via `ContextSwitcher` or, since the GroupCard change above, tapping a card on `/groups`). `HomeRedirect`/`useHomePath`, `BottomNav`, and `OnboardingStepper`'s `handleFinish` already treated a `null` result as "send to `/groups`" (built that way for the loading-state case), so this needed no changes at their call sites — the single change in the shared helper is what makes login, a fresh page load, and finishing onboarding all land on `/groups` to choose whenever nothing's been picked yet, instead of silently dropping the user into whichever group happens to sort first.

**Why:** Requested directly — "When a user logs in or after going through onboarding, take them to /group to select a group if there's nothing set yet in local storage."

**Not yet verified:** No database change — pure client-side. `tsc -b`, `oxlint`, `prettier --check` clean. Manual follow-up: log in on a fresh browser profile (or clear `localStorage`) as a user with 2+ groups → confirm landing on `/groups`, not a guessed one; tap a `GroupCard` → confirm it lands in that group and a reload now goes straight there; finish onboarding as a brand-new user → confirm landing on `/groups` showing the one group just created/joined; from `/progress` with nothing stored, tap the Pantry tab → confirm it also goes to `/groups` rather than guessing.

**Deviation (follow-up, requested directly — flesh out `/groups` now that it's a landing destination):** `GroupsPage.tsx` dropped its `onBack` prop entirely — no back arrow now renders on this screen's `AppHeader`. It previously always had one (falling back to the resolved default group, or `/groups` itself), but now that `/groups` is reachable as a genuine landing destination (see the entry above) rather than only a detour from another screen, a fixed "back" target doesn't make sense here the way it does for `SyncStatusPage`/`ProfilePage`. `GroupList.tsx` gained a page intro — a "Your groups" heading and a one-line subtitle ("Each group shares its own pantry, recipes, and log with everyone in it. Tap a group to switch into it, or start a new one.") above the card list, matching the descriptive-intro pattern `OnboardingStepper.tsx` already uses, since this page previously jumped straight into the list with no framing at all.

**Why:** Requested directly — "Flesh out /groups, add a title and subtitle to describe this page. Remove the back arrow on the app header."

**Not yet verified:** No database change — pure client-side. `tsc -b`, `oxlint`, `prettier --check` clean. Manual follow-up: visit `/groups` (both as a direct landing — e.g. via the "nothing picked yet" redirect — and via the header's Groups icon from another screen) → confirm no back arrow appears in either case, and confirm the new heading/subtitle render above the group list without disturbing the empty-state message, error alert, or the create-group FAB's positioning.

**Deviation (follow-up, requested directly — drop the bare cross-context log views and Pantry/Recipes' ContextSwitcher entirely):** Two more relics of the personal/cross-context model were removed once it became clear they weren't earning their keep:

- The "View log across all groups" button on `/groups/:groupId/log` (`DailyLog.tsx`) is gone. With it went the entire bare, cross-context tier this button led to: the `/log`, `/logs`, and `/logs/groups` routes (`src/App.tsx`), and the two components that existed only to serve them — `GroupLogsPage.tsx` and `GroupLogPicker.tsx` (both deleted outright, confirmed unused elsewhere first). `LogPage.tsx`/`LogsPage.tsx`/`DailyLog.tsx`/`AllTimeLog.tsx` — all previously shared between a bare and a group-scoped route — simplified to assume a real `groupId` unconditionally (dropping every `groupId ? ... : ...` branch, the personal-target kcal card, and the `hasGroups` plumbing that only existed to gate the now-deleted "View group logs" button). `fetchTodayLogEntries`/`AddLogEntryDialog` (`src/features/logging/api.ts`) dropped their `groupId: string | null` shape for a required `string`; `fetchAllLogEntries` (the cross-context all-time query) had no remaining callers and was deleted.
- `ContextSwitcher.tsx` (Pantry/Recipes' pill-chip group picker) is deleted entirely, along with its usage in `PantryPage.tsx`/`RecipesPage.tsx`. Switching groups now only happens via a `GroupCard` tap on `/groups` (see the "clicking a `GroupCard` switches context" entry above) — the chip's job is fully covered by that plus `BottomNav`'s/`useHomePath`'s existing default-group resolution, so keeping a second, in-screen picker doing the same thing was redundant once `/groups` itself became clickable.

`docs/routes.md` and `docs/mocks/design-system.md` updated to match — the "personal vs. group duplication" and "context switcher chip" sections both now describe what was removed and why, rather than a still-current design.

**Why:** Requested directly — "In /groups/[id]/log, remove the view logs across all groups button. Then delete the /log, /logs and /logs/groups page including their components unless these are being used elsewhere," followed by "Get rid of the context switcher component."

**Not yet verified:** No database change — pure client-side. `tsc -b`, `oxlint`, `prettier --check` clean. Manual follow-up: visit `/groups/:groupId/log` → confirm only "View `<group>`'s all-time history" remains, no second button; confirm `/log`, `/logs`, and `/logs/groups` all 404/redirect gracefully rather than crashing; confirm `/groups/:groupId/logs` still works and shows that group's history; confirm `/pantry`'s and `/recipes`' bottom tabs no longer show any chip below the header, and that BottomNav still lands in the right group when tapped from Progress/Profile.

## Weight is optional when creating a recipe (2026-08-28, requested directly)

**Deviation:** `RecipeForm.tsx` (the create-only dialog form, per this repo's create/edit split — see `CLAUDE.md`) no longer marks its "Weight" `TextField` `required`. Leaving the field blank at submit time is now treated as `0`: `handleSubmit` treats an empty `weight` string as `parsedWeight = 0` before the existing kg→g rounding logic runs, so `onSubmit`'s `weight_g` payload is `0` rather than `NaN`. The "Unit" select stays required (it already defaults to `g` and offers no blank option). `weight_g`'s column (`numeric not null`, no default — see the "servings → weight" entry above) is unaffected: `0` is a valid, non-null value, so no migration is needed for this change.

`RecipeDetail.tsx`'s own inline edit fields (a separate implementation per the create/edit split) were intentionally left untouched — this request was scoped to recipe creation, not editing an existing recipe's weight, and `RecipeDetail`'s existing `weightG > 0` validity check wasn't asked about.

**Why:** Requested directly — "When creating a new recipe, make the weight amount optional."

**Not yet verified:** No database change — pure client-side, and no migration needed since `0` already satisfies the `not null` column. `tsc -b`, `oxlint`, `prettier --check` clean. Manual follow-up: open "Create recipe," leave Weight blank, and confirm submitting succeeds with the recipe showing `0 g`/`0.00`/g kcal-per-gram on its card and detail screen rather than being blocked by a required-field error; confirm entering a weight (in either `g` or `kg`) still works exactly as before.
